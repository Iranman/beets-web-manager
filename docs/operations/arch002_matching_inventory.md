# ARCH-002 Matching/Confidence Inventory

Baseline inspected: `origin/main` / `7c9223123f8f257596bede80dc4a34dfbc2f3e8e`

This inventory was created before consolidating ARCH-002 matching behavior. It records the pre-existing implementations that independently normalize, score, threshold, rank, or explain artist/release-group/release/track/recording identity.

| Implementation | Caller(s) | Album/RG/Track | Inputs | Normalization | Score Formula | Threshold | AcoustID Use | AI Use | Output |
|---|---|---|---|---|---|---|---|---|---|
| `backend.matching_contract.build_recording_matching_decision` | Import Review recording attach, AI item suggestion, manual recording validation, music-format replacement evidence | Recording/track | Current item metadata, candidate recording, optional MB details, AcoustID fields, AI state | Local `_norm` with NFKD/casefold/combining removal, feature suffix stripping, non-alnum collapse | Field similarities plus identity checks; confidence score derived from title/artist/album/year/duration/release-group fields and MB identity signals | `_status`: `>=0.82 yes`, `>=0.68 fuzzy`; action gate also checks conflicts and deterministic identity | Structured AcoustID present/attempted/matched/status/provenance, but status vocabulary differs from album/file helpers | AI is modeled as support evidence; hallucinated/unverified AI can downgrade | `MatchingDecision` dict with evidence, conflicts, warnings, confidence, review/action flags |
| `backend.matching_contract.build_album_matching_decision` | `_folder_release_preflight`, album AI suggestion compaction, duplicate/release resolution gates, Wave 14 tests | Album/RG/release/track coverage | Current album/folder evidence, candidate release/RG metadata, optional local items and MB tracks | Same local `_norm` as recording contract | Album and artist similarity plus RG/release identity checks; inline per-track best-score matching and coverage counters | Track match cutoff `0.70`; field status `0.82/0.68`; action allowed requires no conflicts and identity/coverage checks | No per-track five-state AcoustID model; receives only summary-ish candidate/current fields | AI may contribute `ai_state`, but cannot alone authorize | `MatchingDecision` dict with RG identity fields, track coverage summary, conflicts, action flag |
| `backend.mb_alignment.album_track_norm` / `album_track_score` / `best_album_track_match` | `_folder_release_preflight`, `_album_mb_completeness`, app candidate-track comparison, engine MB track repair | Track alignment | Local item/file metadata, MB release track rows | Linear bracket-span stripper, NFKD/casefold, feature suffix stripping, non-alnum collapse, filename/title variants | `SequenceMatcher` over title variants plus small position and duration bonuses, capped at `1.0`; exact existing MB track ID forces high score | Common cutoffs `0.82` preflight, `0.72` repair | None in this scorer | None | Best MB track index and score |
| `backend.mb_alignment.summarize_mb_track_alignment` | `_album_mb_completeness`, diagnostics/tests | Album/RG track coverage | Beets items and MB tracks plus score function | Delegates to `best_album_track_match`/caller score function | Rank/displacement-style summary using best matches, extras/missing/duplicates | Default `match_threshold=0.72`, `repair_threshold=0.65` unless caller overrides | None | None | Track coverage summary: matched/missing/extra/duplicate groups |
| `backend.mb_alignment.greedy_album_track_alignment` | `transaction_engine.create_album_mb_track_repair_plan` | Track alignment | Local Beets rows and MB release tracks | Delegates to `album_track_score` by default | Greedy rows sorted by score; keeps one local per MB target but is not a global optimum | Default `threshold=0.72` | None directly; engine filters rows with a separate fingerprint helper | None | Alignment rows with local item, MB target, score, unmatched/missing lists |
| `backend.track_align.align_tracks` | `transaction_engine.create_confirmed_import_plan`, direct tests | Fresh import track alignment | Source files and MB tracks plus caller similarity function | Caller-supplied; tests use simple stem normalization | Iterates MB tracks and selects best unused local file by title similarity | `>=0.96 matched`, `>=0.82 fuzzy`; below is missing/extra | None here | None | Comparison rows with matched/fuzzy/missing/extra statuses |
| `backend.track_align.resolve_unmatched_via_acoustid` | `transaction_engine.create_confirmed_import_plan`, direct tests | Fresh import recording recovery | Existing `align_tracks` comparison and AcoustID lookup function | No text normalization; recording ID equality | Promotes an extra local file to a missing MB row when AcoustID returns the row's recording ID | AcoustID candidate score `>=80` | Confirms only missing rows; wrong/no results remain missing/extra; no structured conflict row | None | Mutated comparison rows with `acoustid_verified` |
| `backend.import_guard.existing_track_matches_target` | Missing-track import/replacement guards | Recording/track | Existing item, desired target metadata, fingerprint match flag | Uses caller-provided title score | Exact recording ID still needs title score unless fingerprint match; fuzzy title may authorize without hard ID | Exact MBID title floor `0.72`; fuzzy floor `0.90` | Boolean fingerprint `match`/mismatch, not full status | None | Boolean match/no-match |
| `backend.import_guard.release_track_matches_missing_target` | `transaction_engine.create_album_mb_track_repair_plan` target subset filter | Release track slot | Target track request and MB track row | Minimal normalized title comparison | Exact disc/track, exact MB track ID, or unique normalized title | Exact equality only | None | None | Boolean include/exclude |
| `backend.album_match.build_album_match_plan` | `_album_mb_match_plan`, tests | Album track repair/reconciliation | Local items, MB tracks, caller match function | Caller-supplied | For each item, caller returns MB row/score; duplicate MB targets keep highest score | Caller threshold, app uses `0.82` | Caller may include fingerprint guard | None | Matched/unmatched plan rows |
| `app._track_ai_norm` / `_track_ai_similarity` | `_score_track_ai_candidate`, track AI suggestion helpers | Recording/track candidate ranking | Local item fields and MB/AcoustID recording candidates | Local NFKD/casefold/feature stripping/non-alnum collapse, similar to but separate from `matching_contract._norm` | `SequenceMatcher` with token-overlap boost | Implicit via scoring, no single gate | AcoustID candidates get bonus/provenance before contract verification | AI can choose among candidates; ID must be in candidate set | Similarity floats used by candidate ranker |
| `app._score_track_ai_candidate` | `/api/items/<iid>/ai-suggest` | Recording/track | Local item, MB recording candidate, AcoustID provenance | `_track_ai_similarity` | Weighted title `.38`, artist `.27`, album `.10`, year `.05`, MB score `.10`, AcoustID bonus `.22` | Ranking only; confidence labels later | AcoustID is a numeric bonus, not identity status | AI final selection is constrained to generated candidates | Candidate `_match_score`, `_confidence_reasons`, final selection |
| `app._track_ai_release_match_score` / `_track_ai_best_linked_release` | Recording AI enrichment | Release evidence for recording | Linked MB releases and local album/artist/year | `_track_ai_similarity` | Album `.48`, artist `.24`, year `.28`, plus preferred release/type/country rankers | Ranking only | None | None | Best linked release and release-match score |
| `app._score_mb_release_candidate` | Album/folder AI suggestion and batch import | Album/RG/release candidate ranking | Folder evidence, MB release search result, AcoustID release-hit map | Local `_nk` NFKC/casefold/non-alnum collapse | Artist `.32`, album `.28`, year `.10`, track count `.14`, country `.05`, MB score `.05`, AcoustID `.06`, vinyl penalty | Ranking only; later confidence/gates use other helpers | AcoustID release hits are small score bonus and shortcut source | AI may choose among candidates; hallucinated IDs rejected/downgraded | Candidate `_match_score`, score components, selected release/RG |
| `app._folder_release_preflight` | Import review complete, album/folder AI, reimport/preflight checks | Album/RG/release/track coverage | Folder path or existing album, selected release ID, optional existing album ID | `_artist_key`, `_best_album_track_match`, folder/source scans | Artist similarity, source-track coverage, extras ratio, AcoustID release-hit mismatch, plus `build_album_matching_decision` veto | `0.82` preflight, `0.72` repair, minimum coverage ~60%, artist gate | Aggregates a few file fingerprints by release ID; boolean-ish `acoustid_mismatch` if target has zero hits and another release wins | None directly | Preflight dict: ok/reason/matched/expected/extras/matching_contract |
| `app._candidate_track_build_comparison` / `_candidate_track_comparison_payload` | Import Review candidate release-group selector | Release/RG/track comparison | Local folder candidates, selected release/RG, MB release tracks | `_best_album_track_match` and app album title variants | Greedy-ish per-MB-row candidate selection sorted by score; release candidate ranking by matched/ratio/delta | Match cutoff `0.82`; release-group candidate limit/track count heuristics | Per-row `_album_track_fingerprint_check` may override/update status | None | Side-by-side comparison payload, match counts, failure reason |
| `app._album_track_norm` / `_album_track_score` / `_best_album_track_match` | App preflight, candidate comparison, completeness, cleanup, playlist placement | Track alignment | Local items/files and MB tracks | Richest duplicate normalizer: filename/source-ID suffix stripping, Unicode/casefold, bracket annotation removal, feature variants, path prefixes | `SequenceMatcher` over variants plus position/duration bonuses, exact MB track ID override | Common `0.82`/`0.72`, other callers use `0.62`, `0.90`, etc. | None directly | None | Best track and score |
| `app._album_track_fingerprint_check` / `_acoustid_verify_match` / `_audio_identity_decision` | Candidate comparison, cleanup, AI evidence, playlist matching | Recording/audio identity | Local audio file, expected recording/title | App track normalizer for title fallback | Recording ID equality and title fallback; helper-specific status mapping | AcoustID score often `>=70`; some helpers use stronger cutoffs | Statuses vary: `match/mismatch/none/missing/unclear`, `confirmed/mismatch/unverified`, or review/conflict dictionaries | None | Helper-specific status/evidence |
| `app._validate_wanted_album_items_with_acoustid` | Missing-track import readiness | Album wanted item validation | Local items, target MB release tracks, AcoustID lookup | `_best_album_track_match` and target-pair lookup | MBID/pair/best-score target selection plus fingerprint confirmation/mismatch counters | Score threshold via `_best_album_track_match` plus AcoustID `>=70` | Distinguishes confirmed/mismatched/no_result in counters, but output is bespoke | None | Validation dict with confirmed/mismatched/no_result counts |
| `app._scan_album_track_integrity` | Library cleanup/reconcile workflows | Album track cleanup/reconciliation | Existing album items, MB tracklist, optional fingerprint/AI review | `_best_album_track_match`, `_album_track_fingerprint_check` | Per-item best score, fingerprint rejection, duplicate target collision post-pass, low-album-ratio promotion | Remove `<0.62`, review `<0.90`, fingerprint mismatch removes, album ratio `<0.25` escalation | Fingerprint mismatch may force remove/review; no shared evidence model | Optional AI review can keep/remove/review questionable rows | Cleanup plan with keep/remove/review candidates |
| `app._album_mb_match_plan` | Album MB match/repair route | Album track repair plan | Beets items, MB tracks | `_best_album_track_match` with fingerprint guard | Delegates to `build_album_match_plan`; fingerprint mismatch returns no-match | Threshold `0.82` | `_album_track_fingerprint_check` blocks mismatch | None | Match plan dict |
| `app._album_mb_completeness` | Album completeness/reporting and some result handling | Album track coverage | Album ID, MB release ID, item list | `summarize_mb_track_alignment` via app scorer | Summary coverage percent, extras/missing/duplicates | `0.82` preflight, `0.72` repair | None | None | Completeness summary |
| `app._resolver_title_score` / `_resolver_retag_candidates` | Album duplicate resolver retag candidates | Track slot suggestion | Duplicate/local item and missing MB track slots | Resolver-specific title base normalization | Title similarity and duration/track hints | Retag candidate top score around `0.78` | None | None | Retag candidates for manual duplicate resolver |
| `app._playlist_candidate_match_score` / `_match_playlist_tracks` | Playlist matching | Track/recording library candidate matching | Playlist entry artist/title and library candidates | Playlist-specific title/artist variants and scoring | Title `.78` + artist `.22`, with quality/status bonuses/penalties | Title floor `0.78`, artist floor `0.72`, final match `>=0.82` | Optional `_acoustid_verify_match` status on matched rows | None | Matched/unmatched playlist rows |
| `app._playlist_album_tag_release_placement` | Playlist missing-track import album placement | Album/RG/release placement | Playlist candidate album tags, MB release search result, local track metadata | `_album_track_norm`, playlist artist/title scores | Album score, artist score, best release-track score; confidence = album `.35` + artist `.20` + track `.45` | Auto placement requires confidence `>=0.70` and valid RGID | None directly | None | Placement dict with release/RG/confidence/reason |
| `app._playlist_resolve_album_placement` | Playlist missing-track import placement fallback | Recording/release/RG placement | AcoustID recording candidates, MB recording search, release details | Playlist text scores and `_best_album_track_match` | MB score `.40`, title `.20`, artist `.15`, release-track `.25` | Env floors: title `0.86`, artist `0.68`, MB score `70`; auto placement `>=0.70` | AcoustID provides candidates but does not share album evidence policy | None | Placement dict or review-required reason |
| `backend.beets_control_agent._playlist_import_identity_ok` | Engine-side staged playlist import before mutation | Recording/track identity | Staged file tags, expected item MBID/title/artist, AcoustID lookup | `_norm_import_text` and containment-ish `_import_text_similarity` | Embedded tag conflict checks; AcoustID candidate match by expected MBID or high text similarity | Requires fingerprint candidates; no candidate is review-required; mismatch fails | AcoustID is mandatory identity proof for this workflow | None | `(ok, status, evidence)` |
| `transaction_engine.create_album_mb_track_repair_plan` | Control agent `/transactions/album-mb-track-repair/plan`, web `_match_tracks_from_mb_shared` | Existing album/reimport MB track repair | Beets DB rows, target release/RG, target_tracks, AcoustID lookup | `_mb_track_repair_title_norm`, `backend.mb_alignment` scorer | Greedy alignment plus conflict checks for existing nonblank recording IDs; optional RG establishment/stamping | Greedy threshold `0.72`; title conflict behavior differs by existing MBID; AcoustID mismatch excludes row | `_mb_track_repair_acoustid_check` states `missing/unavailable/none/match/mismatch/unclear`; unavailable/none/unclear fall through | None | Controlled transaction plan/apply payload |
| `transaction_engine.create_confirmed_import_plan` | Fresh reviewed import path | Fresh album import alignment | Immutable source manifest, reviewed release/RG, source files, MB tracks, AcoustID lookup | `_confirmed_import_title_similarity` over filename/title | `track_align.align_tracks`, optional AcoustID rescue, extra-file AcoustID conflict scan | Fuzzy `0.82`; matched `0.96`; conflict AcoustID min score `85` | Distinguishes missing/no candidates enough for import, but not shared five-state per row | None | Controlled confirmed import plan with track alignment/coverage/review flag |
| `helpers_mb._clean_for_mb` / `_mb_recording_search` | Recording candidate generation for AI/manual/import review | Candidate generation for recording | Artist/title query text | Query-cleaning regex and colon restoration | MusicBrainz returned score/order plus helper cleanup | Provider-specific result limits | None | None | Recording candidates |
| `helpers_mb._mb_release_search` / `_resolve_release_group_to_release` | Album/folder suggestion, playlist placement, manual/RG resolution | Candidate generation for release/RG | Artist/album/year/track count/country/RGID | Query cleaning and local ranking fields | MB score plus edition preference: official/non-vinyl/country/year/track count/date; de-dupes by RGID | Provider result limits; no acceptance gate | None | None | Release candidates or representative release |
| `helpers_mb._acoustid_lookup` | Track AI, album/folder AcoustID aggregate, playlist placement | Candidate generation for recordings/releases | File path/fingerprint | None | AcoustID provider score/order | Provider result limit; score carried as raw value | Conflates missing fpcalc, lookup failure, and no-result as `[]` | None | AcoustID candidate records |
| `routes_submissions._compare_release_tracks` | `/api/submissions/musicbrainz-release/validate` | Submission release track validation | Local submission tracks and MB release tracks | Basic casefold trim | Positional mapping; flags title mismatch and duration delta >8s | Exact positional rows only | None | None | Mapping/mismatches requiring confirmation |
| Frontend Import Review evidence display (`ImportReviewPage.tsx`) | Import page | UI for album/recording candidate evidence | Backend suggestion/evidence payloads | None | Renders backend scores/confidence labels and selected match counts | Uses backend confidence strings/levels | Displays AcoustID fields when present but backend schema varies | No scoring; initiates AI suggestion calls | User-visible match buckets, selected match, manual validation state |

## Immediate Contradictions / Risks Found

- Album/RG identity is partly guarded by `matching_contract`, but candidate ranking can still be produced by independent album AI, folder preflight, playlist placement, or release resolver formulas before the shared contract vetoes or explains anything.
- Track matching exists in at least four forms: app `_best_album_track_match`, backend `mb_alignment`, `track_align`, and playlist/submission-specific matchers. Only some are one-to-one; none provides a single shared evidence object for all callers.
- AcoustID status vocabularies differ by workflow (`match/mismatch/none`, `confirmed/mismatch/unverified`, `acoustid_verified`, `review_required/no_acoustid_result`, release-hit aggregates). Missing/unavailable/no-result/conflict are not represented consistently.
- Numeric confidence gates differ: AI batch high/medium gates, album/playlist `0.70`, preflight `0.82/0.72`, cleanup `0.62/0.90`, submission artist `95`, confirmed import `0.82/0.96`.
- Candidate generation and candidate scoring are mixed in several helpers. MusicBrainz search helpers rank editions, app AI formulas rank identity candidates, and preflight independently recalculates coverage.
- Fresh reviewed imports and existing/reimported material correctly use different trust models today, but their alignment/evidence implementations diverge rather than sharing policy primitives.
