# Technical Debt Register

Current, unresolved architecture and security debt only. Statuses: Open, Accepted Risk. Resolved items are removed from this document once closed — their history lives in Git and in the pull request that closed them, not here.

Each entry: affected area, evidence, current risk, desired state, safe migration approach, priority, status.

## ARCH-001 Monolithic Route/Domain/Mutation Coupling

- Affected area: Backend `app.py` and route modules.
- Evidence: Most operator workflows (import review, library, cleanup, deduplication, playlists, Plex, configuration, transactions, import) still live in `app.py` rather than a thin route calling an owned service.
- Current risk: Changes to one workflow can accidentally alter unrelated behavior; it is hard to tell whether a given call site uses a well-owned service method or a legacy shape.
- Desired state: Routes remain thin. Application services own workflows. Domain modules own matching and safety decisions. Provider adapters own external calls. Repository/client methods own Beets-engine access.
- Safe migration approach: Extract one tested service at a time. Prefer replacing legacy call shapes with explicit `BeetsClient` methods while preserving route signatures and responses.
- Priority: P0. Status: Open.

## ARCH-002 Duplicated Matching And Confidence Rules

<<<<<<< HEAD
- Affected area: Import review, AI album/recording suggestion, playlist processing, missing-track replacement, duplicate handling, MusicBrainz submission preparation.
- Original evidence: Matching logic existed independently in `app.py` (`_track_ai_*`, album/folder AI release scoring, import review revalidation/preflight, `_best_album_track_match`, candidate-track comparison, library cleanup/reconcile, playlist matching/placement), `helpers_mb.py`, `backend/track_align.py`, `backend/mb_alignment.py`, `backend/import_guard.py`, `backend/album_match.py`, `backend/beets_control_agent.py`, `backend/transaction_engine.py`, and `routes_submissions.py`. See `docs/operations/arch002_matching_inventory.md` for the pre-consolidation table.
- Current progress: Introduced `backend/matching/` as the canonical evidence service:
  - `normalize.py`: deterministic Unicode-aware normalization/title variants with no nested-risk regex dependency.
  - `models.py`: explicit `AcoustIDStatus`, `ConfidenceState`, release-group result, release result, track assignment, missing/unmatched structures.
  - `track_alignment.py`: global one-to-one assignment, AcoustID confirmed/conflict/no_result/unavailable/ambiguous distinction, existing-recording hard-conflict preservation.
  - `evidence.py`: release-group candidate evaluation, Release ID vs Release Group ID separation, explainable score components, conflicts, missing evidence, review reasons.
- Production callers migrated so far: `backend.track_align.align_tracks` (`confirmed_import_v1`) delegates to canonical global alignment; `backend.mb_alignment.greedy_album_track_alignment` (`album_mb_track_repair_v1`) is now a compatibility wrapper around canonical global alignment; `backend.matching_contract.build_album_matching_decision` derives album/RG matching decisions from canonical release-group evidence while preserving its outward contract; the beets Docker image now copies `backend/matching/` into the flattened agent runtime; Import Review's safety panel displays canonical state/RGID/alignment counts/conflicts from `matching_contract.evidence.canonical_match`.
- AcoustID policy implemented in the canonical service: confirmed, conflict, no_result, unavailable, and ambiguous are separate states; missing/no-result evidence is not a conflict; confirmed fingerprint evidence can carry a title-differs warning; conflicting fingerprint evidence blocks acceptance.
- Confidence policy implemented in the canonical service: semantic states (`confirmed`, `strong_match`, `review_recommended`, `conflict`, `insufficient_evidence`) are separated from score components. Hard conflicts force `conflict` regardless of soft title/AI confidence.
- Test coverage added/updated: `tests/test_arch002_matching_corpus.py` covers wrong mixed RG metadata, correct RG/no tracks, title mismatch plus AcoustID confirmation, AcoustID conflict, missing AcoustID, duplicate assignment, bonus/deluxe, missing/extra tracks, same-title songs, hard recording conflicts, fresh untagged imports, ordering invariants, and adversarial normalization. Existing engine/import suites now exercise the canonical wrappers. `frontend/tests/ImportReviewMatchingSafety.test.tsx` covers canonical evidence display.
- Current remaining risk: ARCH-002 is **not closed** yet. Several production paths still perform independent candidate ranking/scoring before they reach the canonical contract, including app-level album/folder AI release scoring (`_score_mb_release_candidate`), recording AI pre-ranking (`_score_track_ai_candidate`), candidate-track comparison payload ranking, playlist library matching and album placement, library cleanup/reconcile scoring, `backend.import_guard` boolean guards, `backend.album_match`, `helpers_mb` release-ranking helpers, and submission release-track comparison. Some of these can remain candidate-generation helpers only, but each still needs a caller-by-caller migration/audit proving final identity/confidence/action decisions pass through the canonical service.
- Desired state: Candidate generation may use provider-specific search/ranking, but every production candidate that can affect import, repair, reconciliation, playlist placement, or submission readiness must be evaluated through the canonical evidence service before action or user-facing confidence is reported.
- Safe migration approach: Continue migrating one caller family at a time, keeping response-shape compatibility tests at each boundary. Add structural regression tests after migration to prevent new normalization/confidence/AcoustID-policy implementations from reappearing outside `backend/matching/` and documented provider candidate-generation code.
- Priority: P0.
- Status: Open.
## ARCH-003 Mutations Do Not All Use One Controlled Boundary

- Affected area: Beets command execution, filesystem cleanup, metadata write, artwork repair, import, playlist placement, deduplication, replacement.
- Evidence: The `beets` control agent now owns Beets CLI execution, direct SQLite compatibility queries, tag writes, and file move/delete endpoints inside the authoritative engine container. `app.py` still contains many legacy command-array call sites and residual direct filesystem operations (`shutil.move`, `shutil.rmtree`, `Path.unlink`, `os.replace`, copies, and directory removals) that are not all planned, audited, and verified through one transaction boundary. `backend/transaction_engine.py` exists but is not universal.
- Current risk: Preview/audit/rollback behavior varies by workflow. Partial failure can be hard to recover or may be reported inconsistently. Residual filesystem call sites also make it easy to accidentally depend on web-manager media mounts that the supported two-service Compose files intentionally do not provide.
- Desired state: Mutating workflows use shared plan/apply/verify/recover semantics with root validation, diffs, audit records, and recovery information, and all media/Beets mutations execute in the `beets` engine boundary.
- Safe migration approach: Mutating workflows use shared plan/apply/verify/recover semantics with root validation, diffs, audit records, and recovery information, and all media/Beets mutations execute in the `beets` engine boundary. Wave 15 established the controlled mutation boundary for Import Review + cleanup mutations via engine-side `TransactionStore` (`/imports/review/cleanup/plan`, `/imports/review/cleanup/apply`, `/albums/cleanup/plan`, `/albums/cleanup/apply`). Wave 17 (PR #90) added the `track_replacement_v1` mutation family. Wave 18 (PR #91) added the `bulk_import_replacement_v1` mutation family. Wave 19 added `album_mb_track_repair_v1`. Wave 20 (PR #93) added `existing_album_reconcile_v1`. Wave 21 (PR #94) added `artist_folder_reconcile_v1`. Wave 22 (PR #95) added `album_maintenance_v1`, `album_artwork_v1`, `folder_cleanup_v1`, and `playlist_media_cleanup_v1`. Wave 23 (PR #97) performed a mutation surface truth pass and scanner overhaul. Wave 24 (PR #98) achieved COMPLETE CLOSURE of the WHOLE Album Lifecycle Domain (`album_artwork` = 0 unresolved, `album_relocation` = 0 unresolved, `album_metadata` = 0 unresolved, `album_retirement` = 0 unresolved). All production album lifecycle operations (artwork replacement/deletion, relocation/rename/move-to-library, metadata repair, retirement/cleanup/deduplication) are consolidated under controlled engine transaction families (`album_artwork_v1`, `album_relocation_v1`, `album_metadata_repair_v1`, `album_maintenance_v1`, `album_mb_track_repair_v1`). All 14 Round 2 review corrections preserved; BeetsClient remains a pure HTTP proxy with 0 local filesystem mutations; honest NEEDS_REVIEW dispatcher attribution enforced.
  **Wave 24 Round 4 (CodeQL closure, final head `827f13539643802fb556c2fafbff0755cb7272aa`):** the 47 open `py/path-injection` CodeQL alerts from Round 3 (plus 1 introduced by this round's own new validation primitive, 48 total) were individually audited against actual codeFlow/callers, not bulk-dismissed. Disposition: 0 REAL_VULNERABILITY, 48 SAFE_BUT_CODEQL_BLIND (CodeQL does not model this module's custom `_path_under`/`_path_has_symlink_under`/regex-transaction-ID containment as sanitizers; every flagged sink is reached only after root-containment and symlink-component proof already ran) -- each dismissed individually via the Code Scanning API with a sink-specific rationale, none by category or blanket comment. The artwork family was refactored around one new `validate_path_under_allowed_roots()` primitive returning the canonical validated Path for sinks to consume, and a real defense-in-depth gap was closed (Apply's per-item `moves_plan` destination is now re-derived through the revalidated `target_dir` rather than trusted verbatim from stored transaction metadata). 14 new adversarial regression tests added (traversal, sibling-prefix, absolute-outside, symlink file/dir component, DB/request/transaction-metadata trust-boundary enlargement, real Plan-to-Apply symlink substitution). A separate real provenance bug was found and fixed in `.github/workflows/docker-build.yml`: `docker/metadata-action`'s auto-derived `org.opencontainers.image.revision` label was pinned to the synthetic PR-merge `GITHUB_SHA` rather than the actual PR head (the `VCS_REF` build-args were already correct); both published image labels now match `FINAL_HEAD` exactly, confirmed on the final CI run. Multi-dimensional verification complete on the final head: 0 open PR-scoped CodeQL alerts (API-confirmed) and a green CodeQL check, 2x full local test suite runs (2,627/2,627 passed, 0 failures, 0 errors, 129 skipped on Windows dev) plus a green CI Linux run (2,627 passed, 0 failures, 0 errors, 5 skipped -- test count is +14 over the prior 2,613 baseline for the new regression suite), 100% clean real HTTP socket IPC suite (`tests/test_album_lifecycle_wave24_real_ipc.py`), a genuine two-service Docker acceptance run (separate Web Manager + Beets Engine containers, engine-offline mutations fail closed, no Web Manager media/config mount) with exact-head provenance proven on both images, and 100% passing security and frontend gates (`secret_scan`, `endpoint_inventory --check` [245 routes, 0 NEEDS_REVIEW], `validate_compose_security`, `verify_arch003_mutation_inventory --check`, `npm ci`, `typecheck`, `lint`, `test` [48 passed], `build`, `audit` [0 high/critical]). Mutation inventory unchanged by this round: 506 total sinks, 185 unresolved baseline across non-album domains (`import_reconciliation`: 41, `ai_import`: 19, `library_cleanup`: 9, `config`: 20, `other`: 96) -- album lifecycle domains remain 0 unresolved.
  **Wave 25 (Import Reconciliation Domain Closure, PR #100, independent review round applied):** the `import_reconciliation` domain sits at `unresolved_domain_counts.import_reconciliation = 0`, but the original PR's own claim of that result was not trustworthy on inspection and required correction before merge -- see `docs/operations/wave25_import_reconciliation_design.md`'s "Independent Review Round" section for the full account. Independent source inspection (not a re-assertion of the original classification table) found: a `beets_client.repair_album_artwork()` call to a method that never existed (artwork fetch/embed silently never ran, `AttributeError` swallowed by the caller); an item-id-passed-as-album-id bug in `reimport_disk`'s item-level loop risking wrong-album mutation; the same loop's `plan_album_mb_track_repair()` call missing the required `album_id`, silently accepted as a no-op; three `plan_folder_cleanup()` calls using a payload shape (`action="delete_stale_items"`, or `{"path":...,"action":"delete"}`) its Plan never reads, so each always reported success while never actually deleting the DB rows/folder it claimed to; a redundant, DB-unaware raw file delete bypassing `album_cleanup_v1`'s own safety checks; dead code (`_delete_row_file`) duplicating an already-transactional deletion path; and two false inventory classifications (`reimport_source_atomic` claimed as `import_folder_v1` when it is a separate engine-native atomic operation with no `TransactionStore` involvement at all; five overly-broad `APP_STATE` substring hints with no path-context check, replaced with an individually-verified exact-function-name allowlist). All of the above were fixed in code and/or the inventory generator, not merely reclassified -- see the corrected 41-sink table in the design doc for the final, verified disposition of every starting sink. Production import reconciliation operations (`import_folder_with_id._do`, `reimport_disk._do`, `_ai_import_folder`, `start_import._do`, `_validate_wanted_download_identity_before_import`) now execute via real controlled engine transaction families (`album_mb_track_repair_v1`, `album_relocation_v1`, `album_metadata_repair_v1`, `playlist_media_cleanup_v1`, `album_cleanup_v1`, `folder_cleanup_v1`) where a DB-tracked resource is involved, or truthfully-classified engine-native/staging-only operations where none is. 17 new regression tests (`tests/test_wave25_round_review_fixes.py`) prove each fixed defect cannot silently return. Zero mutating `_beet_run` fallbacks and zero local SQL DML fallbacks remain in migrated import reconciliation workflows. Mutation inventory (post-review-round, unchanged in total from pre-review): 471 total candidate sinks, 134 unresolved baseline across remaining non-import domains (`ai_import`: 18, `library_cleanup`: 9, `config`: 20, `other`: 87).

  **Wave 25 Docker acceptance round (same PR #100):** building the dedicated Wave-25 two-service Docker acceptance scenarios this pass required actually exercising `import_folder_with_id` (the real, frontend-called `/api/folders/import-with-id` route) end to end, which surfaced two further real, previously-undiscovered bugs the mutation-inventory methodology was structurally blind to (it only flags *mutation* sinks; these are read/validation-path bugs): (1) `import_folder_with_id` validated the import source folder's *existence* via `candidate.exists()`/`is_symlink()` inside the `beets-web-manager` process itself -- but that container has zero filesystem visibility into `/data/media/music` or `/data/torrents` in either documented compose topology (only `/web-manager-data` is mounted, `read_only: true`), so every real call was guaranteed to 404 with "Source path does not exist" regardless of whether the folder was genuinely there. Fixed by dropping the local existence requirement (root-containment, a pure string check, is still enforced locally) and adding a real, fail-closed existence+directory-type check to `create_import_folder_plan()` on the engine side, which actually has the mount. (2) The Control Agent's `/import/plan` and `/import/apply` handlers built their allowed-roots lists from `os.environ.get("MUSIC_ROOT", "/music")` / `os.environ.get("DOWNLOADS_ROOT", "/downloads")` -- but `MUSIC_ROOT`/`DOWNLOADS_ROOT`/`STAGING_ROOT` are never actually set anywhere (neither compose file, nor `Dockerfile.beets`), so these always silently fell back to paths (`/music`, `/downloads`, `/staging`) that do not exist in the engine container at all, instead of the real bind-mount-matching module constants (`MUSIC_ROOT` = `/data/media/music`, `DOWNLOAD_PATH` = `/data/torrents`) already defined and correctly used by roughly half of this file's other handlers. `create_import_folder_plan`'s root-containment check therefore rejected every real source folder with `import_folder_path_out_of_root` regardless of where it actually was. Fixed for `/import/plan`/`/import/apply` specifically (the two handlers this round's acceptance work actually exercises).
  **Not yet fixed, tracked here rather than silently absorbed:** the same wrong-default pattern (`os.environ.get("MUSIC_ROOT", "/music")` instead of the safe `os.environ.get("MUSIC_ROOT") or str(MUSIC_ROOT) or "/music"` fallback already used elsewhere in `backend/beets_control_agent.py`, plus the equivalent `DOWNLOADS_ROOT`/`STAGING_ROOT`/`TORRENT_SOURCE_ROOTS` cases) still appears in roughly 20-30 other handlers in that file. Each needs individual verification (some may be genuinely unreachable with a real path, dead, or covered by a redundant downstream check) before a blanket fix -- out of scope for this pass; flagged here explicitly so it is not lost. A STATUSES-allowlist bug in `transaction_engine.py` was also found and fixed in this round: `store.update(operation_id, status="Recovery Required")` silently persisted as `"Pending"` instead (the value was never in the `STATUSES` set that `_status()` validates against) at every one of its several pre-existing call sites, not just the new `album_artwork_fetch_v1` family this round added.
  **Wave 25 Round 3 (`confirmed_import_v1`, same PR #100):** running the real Docker acceptance for `fresh-import`/`crash-resume-no-duplicate` (after fixing three unrelated `NameError`s found in the two prior CI iterations) surfaced a genuine architecture mismatch, not a small bug: `import_folder_v1`'s Apply step routed every import -- fresh and reimport alike -- through `reimport_source_atomic()` / `verify_deterministic_identity()`, a deliberate SEC-002 Wave 8 security control requiring the source audio's *own embedded* MusicBrainz tags to already match the target before import runs. That model is correct for genuine reimports of already-tagged library content; it is the wrong model for `import_folder_with_id()`'s actual job of tagging *untagged* downloads for the first time, which have no embedded MB tags by construction -- so it failed closed for essentially every real fresh import, not just this round's test fixture. Fixed by building a second, distinct controlled family, `confirmed_import_v1` (`transaction_engine.create_confirmed_import_plan`/`execute_confirmed_import_apply`, `beets_control_agent.run_confirmed_import_native`, new `/imports/confirmed/plan`+`/imports/confirmed/apply` routes, new `BeetsClient.plan_confirmed_import`/`apply_confirmed_import`), binding authorization to an immutable source-manifest digest + an explicitly reviewed concrete Release ID/RGID + best-effort track/fingerprint alignment (reusing `backend/track_align.py`, no new matcher) instead of pre-existing embedded tags. `reimport_source_atomic()`/`verify_deterministic_identity()` themselves were deliberately left untouched (no bypass flags added) and remain the reimport path's only gate -- see `docs/operations/wave25_import_reconciliation_design.md`'s "Round 3" section for the full before/after and the two trust models' security-model writeup. `import_folder_v1` (the old family) is left in place, untouched and still tested, simply no longer invoked by production code for this workflow. Known, documented limitation: crash-safety is proven for a crash *after* native import succeeds but before `Completed` is recorded (the required acceptance scenario, handled via an `mb_albumid`-uniqueness idempotency check); a crash *mid-subprocess* is a materially harder, pre-existing problem shared with `reimport_source_atomic`'s own design, out of this round's scope.
  **Wave 25 Round 4 (same PR #100):** the first real Docker run of `confirmed_import_v1` (Round 3 had never run against real Docker) found the round-3 blocker was in fact fixed, but two real bugs remained in Round 3's own new code: (1) `run_confirmed_import_native()` copied `--quiet-fallback asis` verbatim from `reimport_source_atomic`, which lets Beets silently import under an unreviewed identity (or none at all) whenever its match confidence for the forced `--search-id` candidate is weak -- fixed by switching to `--quiet-fallback skip`, and by having the native runner report `albums_before`/`albums_after` so `execute_confirmed_import_apply()` can tell a clean, zero-mutation skip apart from a genuine partial mutation under the wrong identity, which it could not do before. (2) the Docker acceptance fixture used flat ~0.3s synthetic files against tracks that are actually minutes long, which alone starved Beets' own match-confidence scoring regardless of tag/filename similarity -- fixed by fetching the real release's tracklist/durations from MusicBrainz once per run and generating duration-accurate fixture audio with basic (non-MusicBrainz) tags, asserted MBID-free before every import request. Also replaced the crash/resume scenario (previously only a restart+idempotency test, not a true crash-after-native-success test) with a deterministic, narrowly-gated failpoint (`confirmed_import_after_native`, `BEETS_ACCEPTANCE_MODE`-gated, inert in every real deployment) that reaches the actual required window without a timing race, and corrected the artwork scenario's plugin config (`fetchart` was never loaded at all) to report transaction-semantics and successful-acquisition as two separate, honestly-scoped claims. See `docs/operations/wave25_import_reconciliation_design.md`'s "Round 4" section for the full diagnosis.
  **Wave 25 Round 4, corrected (same PR #100):** the duration-accurate fixture above still failed the real Docker run with an unconditional `Skipping.` and no match detail, which led to two further hypotheses that were committed but never independently verified before the next Docker run: a `match.strong_rec_thresh: 0.25` relaxation, then a WAV-to-FLAC fixture conversion for tag-reading reliability. Neither was the actual cause. Per the standing instruction to prove rather than guess, the real cause was found by reproducing `beets.autotag.match.tag_album()` directly (outside Docker) against the fixture's own metadata: it returned zero candidates, not a low-confidence one. Root cause: Beets' `plugins` config key is not additive across config sources -- the acceptance engine's `config.yaml` set `plugins: fetchart` (added earlier in this same round to fix the artwork scenario) with no `musicbrainz`, which silently replaced Beets' own packaged default (`plugins: [musicbrainz]`) rather than adding to it, unloading the one plugin that resolves `--search-id` into a real candidate at all. Fixed by listing both plugins explicitly (`plugins: fetchart musicbrainz`); re-running the same local reproduction with the fix applied returns one candidate at distance `0.0` and recommendation `strong` under Beets' *default*, unrelaxed `strong_rec_thresh`, so the earlier `0.25` relaxation was reverted as unneeded (and had been masking, not fixing, the real bug). The WAV-to-FLAC fixture change was kept as a genuine but non-load-bearing reliability improvement. Also fixed in the same pass: the `two-service-docker-acceptance` GitHub Actions job installed no `ffmpeg` of its own, so the FLAC conversion step failed closed with a clear `RuntimeError` before any scenario could run at all on the first attempt after that change landed -- `ffmpeg` is now installed explicitly as its own workflow step rather than assumed present on the runner image.
  **Wave 25 Round 4, second correction (same PR #100):** with the plugin fix above, native import genuinely succeeded end to end for the first time (`Engine controlled import completed`, `Found 1 album(s) via confirmed_import_v1 verified result`), and the rebuilt crash-resume idempotency proof worked exactly as designed (`Resumed an already-verified prior result for this release`). Both scenarios then failed identically one step later: `Could not validate imported album: Raw SQLite queries are not permitted`. Cause: `import_folder_with_id()`'s legacy `_album_match_summary()` re-validation helper runs unconditionally over every discovered `album_id`, via raw SQL through `_db()` -- which `BeetsClient.raw_sqlite_query()` has always unconditionally rejected in the two-service topology (a deliberate, pre-existing SEC-002 control). Not a new bug -- a pre-existing dead/broken legacy code path that no prior real Docker run had ever reached, because every earlier run failed before getting this far. Fixed by skipping `_album_match_summary()` for an album already found via the `"confirmed_import_v1 verified result"` strategy, since that result was already authoritatively verified server-side through structured queries inside `execute_confirmed_import_apply()`'s own capture/verify step; the legacy strategies (A-I) and their raw-SQL re-validation are left untouched for every other discovery path. See `tests/test_wave25_docker_acceptance_round_fixes.py`'s `TestConfirmedImportV1ResultSkipsRedundantRawSqlRevalidation`.
  **Wave 25 Round 4, third correction (same PR #100):** with both fixes above, native import succeeded and the album was found via the `confirmed_import_v1 verified result` strategy (`confirmed-import-idempotent` now passing outright), but `fresh-import-untagged-confirmed` still failed, narrowed to: `item DB paths do not point at real files on the host-mounted music root`. Cause: `_capture_and_verify(require_files_present=True)` only ever checked `files_present == 0`, so a real import that copied *some* but not *all* of an album's items still reported `ok: True`, returning every item ID -- including ones with no backing file -- as part of a `Completed` result. Fixed by requiring every discovered item to resolve to a real file (`files_present == len(item_rows)`) for both the idempotency pre-check and the post-import verification, with a new `incomplete_files_on_disk` reason and diagnostic detail (`files_present`/`items_total`/`missing_item_ids`) surfaced into the Recovery Required message. See `tests/test_confirmed_import_v1.py`'s `TestPartialFileCopyIsRejected`. The underlying reason the real fixture's copy was partial is not yet confirmed with direct evidence -- the next real Docker run's now-surfaced diagnostic is expected to make it identifiable.
  **Wave 25 Round 4, fourth correction (same PR #100):** the diagnostics added by the third fix made the real cause directly visible instead of requiring another guess: all 12 item paths were missing, and every one was the exact same shape -- a full absolute path under the ENGINE CONTAINER's own `/data/media/music` (e.g. `/data/media/music/Radiohead/OK Computer/01 Airbag.flac`), not relative to it. That path is completely real and valid from *inside* the engine container (which is why the engine's own verification correctly reported `ok: True`), but not on the GitHub Actions runner (the host) where the acceptance script itself runs -- only the bind-mounted host directory has the real file. Not a production bug: production code only ever resolves these paths from inside the same container that wrote them. Fixed the acceptance script's own host-side `_item_path_exists()` to rebase an absolute path under the well-known container music root onto the host-mounted `music_dir` before checking.
  **Wave 26 (AI Import Domain Closure & ARCH-010 State Concurrency, PR #101):** achieved COMPLETE CLOSURE of the `ai_import` domain (`unresolved_domain_counts.ai_import = 0`). Implemented durable SQLite WAL mode state store (`AiBatchStateStore`) with schema versioning (`schema_version = 1`), compare-and-swap (CAS) optimistic concurrency control, and atomic state updates. All 18 unresolved `ai_import` sinks refactored and inventory updated cleanly.
  **Wave 27 (Configuration Domain Closure, PR #102):** achieved COMPLETE CLOSURE of the `config` domain (`unresolved_domain_counts.config = 0`). Standardized Web Manager application state & bootstrap credential persistence around a secure primitive (`WebManagerConfigStore`) enforcing restrictive permissions (0o600/0o700), path containment, symlink target rejection, and atomic replacement. Refactored all 20 unresolved `config` sinks in `app.py` to execute via `beets_client` IPC methods. Added candidate YAML syntax validation to `_write_agent_config_file` in `beets_control_agent.py`. Standardized engine music/downloads root resolution helpers (`_resolved_music_root`, `_resolved_downloads_root`).
  **Wave 27, final independent review pass (same PR #102):** fixed CodeQL #1016/#1017 (`py/path-injection`, both high severity, new-to-this-PR): `_derive_artist_folder_identity()`/`_extract_recording_mbids()` in `backend/transaction_engine.py` "validated" the engine's own `lib_db` path against an allowed-root list computed from `lib_db`'s own parent directory (not a real trust boundary -- every path is trivially under its own parent) and fell open to the unvalidated value when that validation returned `None`. `lib_db` is always server-owned config (`BEETS_LIBRARY_DB`/`LIB_PATH`, never request/candidate data -- see `tests/test_control_agent_db_path_audit.py`), so it is now used directly with no fallback. Getting CodeQL to actually clear took three iterations on the separate `folder_path`/`src_path` containment check inside those same two functions: trusting the caller's prior `validate_path_under_allowed_roots()` call without re-proving it locally (closed #1016/#1017, CodeQL opened #1018/#1019 at the same two functions -- its interprocedural dataflow does not carry "already validated" status across a call boundary), then a fully inline re-proof using a generator-expression `next()` (still flagged, #1020/#1021, at the exact same `os.path.exists()`/`.startswith()` line each time) before a plain `for`/`break` loop -- no generator, no delegated call -- finally cleared it. Independently re-derived a genuine architecture violation the prior wave's own new ownership check surfaced (not previously checked at all: the check compared a captured `initial_stat` against nothing and was fixed to actually compare): the Beets Control Agent's `/custom-services.d/` entry point is not privilege-dropped by the `linuxserver/beets` base image the way its own built-in `svc-beets` is (`svc-beets` wraps `beet web` in `s6-setuidgid abc`; ours execs the agent directly), so it runs as root for the container's life, and `config.yaml`'s owner flips from uid=1000/gid=1000 to uid=0/gid=0 across an engine restart. A fix (wrapping the agent in the same `s6-setuidgid abc` idiom) was written, verified locally to actually drop privileges (`ps aux` inside the container showed the agent as `abc` post-fix, `root` pre-fix) and to fix the ownership drift -- but real Linux CI then showed it broke real functionality (artwork upload, duplicate cleanup) that currently depends on running as root to write into host-mounted `/data/media/music`/`/config`, which -- unlike `webdata_dir` -- are never given uid-1000 write access by the acceptance harness (or, plausibly, by real deployments whose host library directory ownership doesn't happen to match PUID/PGID). Reverted; the ownership check in `scripts/verify_two_service_docker_acceptance.py` is now informational (`print`s both timestamps) rather than a `scenario_fail()`, since root ownership is the current, load-bearing (if not ideal) operational model, not a regression to gate the build on. Least-privilege for the control agent is real, worthwhile follow-up work, but needs a documented PUID/PGID contract (or an init-time chown step) covering every host-mounted path first, not a drive-by fix.
  **Wave 28 (Preservation-Copy Content-Signature Correctness & Anti-Laundering Guard, PR #107):** an independent review of a submitted "Other-Domain Authoritative Mutation Closure" claim found the closure claim itself false (`unresolved_domain_counts.other` remains 59, unchanged; `other` is genuinely unclosed debt, not a candidate for this pass) and found several real, independently-verified defects in `preserve_import_source()`/`inspect_import_source()` (`backend/beets_control_agent.py`), the staging-only preservation-copy path a torrent/download import protects before Beets mutates the source. (1) `_import_source_content_signature()` previously hashed only `relative_path`+`size` per file -- two files with the same path and size but different bytes produced an identical "content signature," defeating the entire point of a cross-filesystem copy-equivalence check. Replaced with a real streamed SHA-256 digest of each file's actual bytes (`_file_content_digest()`, 1MB chunks, bounded memory), folded per-file into the manifest signature. (2) The same-source TOCTOU staleness signature (path+size+`mtime_ns`, cheap, same-filesystem-only) and the new source-vs-copy content-equivalence signature (path+size+content-digest, expensive, meaningful across filesystems) were being cross-compared in three places (`expected_source_signature` staleness check, destination-collision match, post-copy verification) despite answering different questions -- every comparison now uses only the signature type appropriate to what it's actually checking. (3) A destination-name collision (deterministic from source path + plan id, so a real, not hypothetical, occurrence) previously triggered an unconditional `shutil.rmtree()` + recreate; now a collision with content that doesn't verifiably match the current source fails closed (`error_code: "collision"`) and leaves the existing destination completely untouched. (4) The byte-copy loop's `try: shutil.copy2(...) except Exception: shutil.copy(...)` fallback could silently convert a real copy failure (permission, disk-full, a source that changed mid-copy) into an apparent success via a second, different copy function; replaced with a single hard `shutil.copyfile()` (uncaught -- a real failure now really fails) with mode-bit/timestamp preservation (`shutil.copymode()`/`os.utime()`) split out as separately-tolerated best-effort steps. (5) Every error path previously either returned raw exception text/paths in the HTTP-facing response or (a second, more serious defect this same fix surfaced) called `logger.warning(...)` in a module that had no `logging` import or `logger` at all -- every one of those "sanitized" error branches was actually an unhandled `NameError` in production, not a caught-and-logged failure. Fixed by adding a real module-level `logging.getLogger(__name__)` (stdlib-only, matching this file's standalone-script constraint) and confirmed by exercising every one of those branches against real Linux (see verification below) rather than trusting that they merely looked correct.
  A second, independent false claim was found and fixed: `scripts/verify_arch003_mutation_inventory.py` had no anti-laundering guard at all, despite a submitted claim that one already existed. Added two real guards inside the `CONTROLLED_MEDIA_MUTATION` classification check: a raw `app.py` SQL statement tagged `raw-beets-library-dml` against the authoritative Beets DB can never be classified `CONTROLLED_MEDIA_MUTATION` merely because a real `transaction_family` name is attached to the entry (naming a real family doesn't make an unrelated raw DML statement route through it); and a `subprocess`-kind entry claiming the same classification must actually route through `beets_client` (the real controlled boundary for subprocess-kind entries in this codebase), not just a local subprocess call. Two new regression tests (`tests/test_arch003_final_closure.py`) prove both the rejection and that a real `beets_client`-routed entry is still accepted.
  All 11 explicitly-flagged mutation-inventory classifications (`_prune_empty_review_dirs`, `_save_acq_download_all_last`, `_record_scan`, `_artist_alias_write_rejected_map`, `_save_rgid_resolutions`, `_mb_release_tracklist_write_disk`, `_plex_client_identifier`, `render_index`, `_delete_no_audio_folders`, `create_unmatched_draft`, `_spotiflac_download_url`) were individually re-verified against actual source (not re-asserted from the prior classification table) -- all legitimately classified, no corrections needed. The mutation-inventory scanner correctly flagged the new `shutil.copyfile()` call as an unclassified sink once its `shutil.copy2()`/`shutil.copy()` predecessors were removed; it was classified identically to its removed predecessors (`ENGINE_NATIVE_BEETS`, staging-only, `import_reconciliation` domain) and the inventory's summary counts recomputed from the actual entry list (487 total sinks, `other` domain unchanged at 59 unresolved / baseline 59 -- `ARCH-003` remains explicitly In Progress for that domain, not forced to zero).
  12 new regression tests (`tests/test_sec002_wave8_engine_ownership.py`) were added for preservation-copy semantics: unchanged-source signature stability; same-wall-clock-second content changes still detected as stale via `mtime_ns`; same-path-and-size-different-bytes, different-size, added-file, removed-file, and renamed-file are each correctly NOT content-equivalent; a real byte-for-byte copy with truncated (coarser-filesystem) timestamp precision IS still content-equivalent; a `shutil.copymode()` failure does not fail an otherwise-successful byte copy; a real `shutil.copyfile()` failure fails closed with no fallback to `shutil.copy()`; a destination collision with unverified content leaves a sentinel file in the pre-existing destination untouched; and raw exception/path/content-signature values never reach the HTTP-facing response on the post-copy-verification failure branch specifically (distinct from the outer-exception-handler leak the pre-existing test already covered). Verification requires real Linux (`resolve_safe_path()` is POSIX-only, matching the engine container this module actually runs in) -- run inside a `python:3.13-slim` Docker container with the exact dependency set CI installs (`requirements.txt` + `beets==2.13.1`, which is what actually made the pre-existing raw-exception-leak test's `shutil.copy2` mock -- now correctly retargeted to `shutil.copyfile` -- exercise real code instead of silently never firing). Real Linux execution caught both the `logger` `NameError` and the stale mock in this same pass; neither would have been visible from a Windows dev-machine `py_compile`-only check. Full suite: 2,859/2,859 passed, 0 failures, 0 errors, 7 skipped, run twice against the final head in the CI-matching container.
  **Wave 29 (Other-Domain migration continuation, branch `feat/arch003-other-domain-closure-wave29`, uncommitted working-tree pass, independent review applied before any commit):** a prior AI session (Codex CLI) worked ~5 hours on this branch with no commits; everything below was verified against actual source and actual test runs rather than trusted from its own transcript, per this pass's explicit brief. Kept, verified correct: (1) `scripts/discover_mutation_sinks.py`'s SQL-sink detector previously only recognized a literal string/f-string argument to `.execute()`/`.executescript()`; it missed `"UPDATE items SET " + ", ".join(...) + " WHERE id=?"`-shaped string-concatenation SQL entirely, undercounting real `ARCH003_BLOCKER` sinks. Fixed by walking `ast.BinOp`/`ast.Add` nodes recursively in the string-constant extractor; one new scanner regression test (`test_sql_dynamic_update_prefix`). (2) `app.py`'s failed-import album cleanup (`_delete_album_ids_from_db`) migrated from three separate raw-SQL/raw-filesystem helpers (`_delete_library_db_rows_under_folder`, `_delete_unmatched_items_in_folder`, and the old body of `_delete_album_ids_from_db` itself -- direct `DELETE FROM items`/`DELETE FROM albums`, direct `Path.unlink()`/`.rmdir()`) onto the pre-existing `album_maintenance_v1` family via `BeetsClient.delete_album()`; the first two helpers were confirmed genuinely dead by a repo-wide caller search (zero remaining references) before removal, not assumed dead by name. (3) The Import Review verified-track-mapping step (`_apply_verified_review_track_mapping`, called from `import_folder_with_id`) migrated from a direct `con.execute("UPDATE items SET " + ...)` loop onto `item_metadata_repair_v1` via `BeetsClient.update_item_metadata(..., write_tags=False)`. This needed a new DB-only mode on `item_metadata_repair_v1` (`create_item_metadata_plan`/`execute_item_metadata_apply` in `backend/transaction_engine.py`): when `write_tags=False`, Apply still performs the real `UPDATE items SET ...`/rowcount/verification DB path but skips `native_beets_write_item_tags()`/the tag-write fallback entirely -- verified against the exact pre-migration semantics (the old code never wrote audio-file tags for this step either, only DB columns), not a new, narrower behavior masquerading as a safe default; a `force_write_tags=True` request still forces a real tag write even under `write_tags=False`, deliberately, since an explicit force request should not be silently downgraded by a caller-side default. One new engine-level regression test (`test_item_metadata_write_tags_false_updates_db_only`) asserts the tag writer is never even called when `write_tags=False`, not just that its result is discarded. (4) `folder_cleanup_v1`'s three actions (`remove_empty`, `safe_rename`, `merge_source_files`) gained real Plan-time and Apply-time hardening that was previously missing or partial: `safe_rename`/`merge_source_files` now validate `target` containment, symlink-free target-parent, target-not-already-exists, and (for rename) a directory dev/ino/mtime stat captured at Plan time and re-checked at Apply time (closing a TOCTOU window that previously let Apply blindly `_safe_rename()` whatever existed at the recorded path); `merge_source_files` now individually validates every discovered source file for symlink components and target-collision before Plan accepts it, and refuses to plan a merge with zero actual file moves (`folder_cleanup_noop`) instead of silently no-op-succeeding; Plan also now refuses the configured allowed root itself as a source (`folder_cleanup_root_refused`). Four new engine-level regression tests cover root-refusal, DB-tracked-source refusal on rename, missing-target-parent refusal on merge, and Apply-time TOCTOU rejection when a planned source file disappears before Apply runs. (5) `app.py`'s `apply_folder_placeholder_action_api()` (the route backing all three actions) was rewritten to call `beets_client.plan_folder_cleanup()`/`apply_folder_cleanup()` instead of doing the `shutil.move`/`Path.rename`/`Path.rmdir` filesystem work locally; `tests/test_arch003_boundary_enforcement.py` gained an AST-based regression (`test_folder_placeholder_apply_has_no_direct_mutation_calls`) proving no local-mutation call nodes remain in that function's body. (6) A duplicate, fully independent implementation of MB track-matching-and-DB-write inside `_match_tracks_from_mb()` (its own MB fetch, `SequenceMatcher` scoring, and direct `con.executemany("UPDATE items SET ...")`/`con.execute("UPDATE albums SET ...")` calls) was collapsed into a one-line delegation to the already-existing `_match_tracks_from_mb_shared()` -- removing one of two duplicate raw-SQL-write code paths, not the underlying architecture problem (see below).

  **This review pass's own findings, fixed before commit:** (a) **Real bug, not present in any test:** `apply_folder_placeholder_action_api()`'s new `_plan_and_apply()` helper returned a plain `(plan_res, apply_res, op_id)` 3-tuple on success, and its three callers gated on `isinstance(result, tuple)` to distinguish success from an already-built Flask error response -- but every error path (`_engine_reject()`/`_engine_exception()`) also returns a plain `(Response, status_code)` 2-tuple, which is *also* `isinstance(..., tuple)`. Every plan/apply rejection or engine-offline error (folder not empty, DB-tracked source, TOCTOU mismatch, symlink rejection, `ENGINE_OFFLINE`, ...) therefore hit `_plan_res, apply_res, op_id = result` unpacking a 2-tuple into 3 names and raised an uncaught `ValueError`, discarding the carefully-built structured error JSON/status code entirely -- reproduced directly (`ValueError: not enough values to unpack (expected 3, got 2)`) before any prior test caught it, because every existing folder-cleanup test either checked upstream validation (never reaching `_plan_and_apply`) or mocked the engine to always succeed. Fixed by wrapping the success case in a dedicated `_PlanApplyOk` type instead of a bare tuple, so `isinstance(result, _PlanApplyOk)` is unambiguous; all five paths (success, plan-rejected, plan-engine-offline, apply-rejected, apply-engine-offline) reproduced correctly after the fix. Four new regression tests (`tests/test_sec002_app_path_folder_cleanup.py::FolderCleanupEngineErrorResponseTests`) assert real JSON bodies and status codes (400/409/503/200) for each path, not just "does not crash". (b) **Real, if lower-severity, robustness regression:** the migrated `_apply_verified_review_track_mapping()` loop moved per-item processing (including `int()` conversions on caller-supplied mapping fields) *outside* the one broad `try/except Exception` the original raw-SQL version wrapped the entire per-item loop in; an unexpected exception on one malformed mapping row would now propagate uncaught out of the helper and abort the calling import job entirely, instead of being logged and skipped as before. Fixed by wrapping each item's processing in its own `try/except Exception`, which is strictly better than the original (one bad row no longer discards already-applied updates for earlier rows in the same batch, since those are now individually committed via the engine rather than one shared local transaction). (c) `security/endpoint_inventory.json` was stale (route line numbers shifted from the dead-code removals above, no route added/removed/reclassified) -- regenerated; diff confirmed to be `line`/`generated_at` fields only. (d) The two backend tests this task's brief named as "known failures" (`tests/test_sec002_app_path_ai_batch_replacement_staging.py`) were independently reproduced on this exact branch state, not accepted from Codex's transcript claim either way: both classes pass cleanly (25/25); this diff never touches AI-batch-replacement code, and the same file passes identically against the unmodified base commit -- the brief's premise did not reproduce here.

  **Not attempted this pass, explicitly not claimed done:** `_match_tracks_from_mb_shared()` itself (the function `_match_tracks_from_mb()` now delegates to) still performs direct `con.executemany("UPDATE items SET mb_trackid=?, track=?, disc=?, title=? WHERE id=?")` plus direct `UPDATE albums SET year=?/label=?/country=?/mb_releasegroupid=?` against the Beets DB, and is called from at least six other sites across `app.py` (`import_folder_with_id`, `_repair_album_mbid_sticking_once`-adjacent flows, `album_deduplicate`-adjacent flows, and others) -- this is the same gap Wave 28's own docs entry (line 1414 in this file) already named as the sole reason the `other` domain isn't zero, and remains genuinely unmigrated; the mutation inventory continues to classify it `ARCH003_BLOCKER` under domain `other` (44 total, unchanged in kind from before this pass, reduced in count only by the dead-code removal and consolidation above -- 486 total sinks / 59 unresolved before this pass's own scanner fix and migrations, 473 total / 44 unresolved after). No new engine-side transaction family was designed for it this pass, and no other `other`-domain blocker beyond what's listed above was migrated, reclassified, or removed. A per-entry inventory diff (base commit vs. this pass's final head) also surfaced the scanner fix (finding 1 above) working as intended, not just theoretically: `_clean_remove_orphaned_items()` (`DELETE FROM items WHERE id IN (" + ",".join(...) + ")"`), `_clean_remove_empty_albums()` (same shape), and `album_merge_split_album._do()` gained 4 genuinely new `ARCH003_BLOCKER` classifications this pass that the pre-fix scanner had silently missed entirely (string-concatenated `DELETE`/`UPDATE` statements against the Beets DB) -- these are real, previously-invisible unmigrated mutation sinks, not scanner noise, and were not migrated this pass; they are part of the 44 unresolved count above, not additional to it. See the session's own final report for the complete before/after reconciliation table.
  **Wave 29, reconciled cherry-pick from a parallel unpushed branch (`feat/sec002-arch003-wave29-direct-mutations`, commit `18ff665`, same base):** that branch did overlapping Wave 29 work independently; the multi-agent orchestration scaffolding it also carried (`.agents/`, root `PROJECT.md`, ~11,700 lines) was excluded entirely -- only the real code contribution was pulled in, manually reconciled against this branch's own folder_cleanup_v1/item_metadata_repair_v1 changes rather than blind-applied. Pulled in: 3 further dead item-path-repair helpers (`_update_item_path_after_rename`, `_force_item_path_after_rename`, `_delete_item_path_after_quarantine`; the other branch's other 2 removals were already done here, skipped) -- independently reconfirmed dead via a fresh repo-wide caller search, not trusted from the source commit's message; total sinks 473->468, `ARCH003_BLOCKER` 44->39. A `BeetsClient` kwargs-surface expansion for `plan_album_mb_track_repair`/`plan_existing_album_reconcile`/`plan_artist_folder_reconcile`/`plan_folder_cleanup`/`plan_library_cleanup` (typed kwargs alongside the existing dict-payload calling convention, which every real caller keeps using unmodified) plus the `transaction_engine.py` support it needs: inline `mb_tracks`/`track_matches` injection bypassing the MB network fetch, `source_path`/`target_path`/`proposed_path` payload aliases for `folder_cleanup_v1` (resolved before this branch's own containment/TOCTOU/symlink checks run, so that hardening applies unchanged regardless of alias), and a new `remove_album` mode on `album_maintenance_v1` closing a real gap (an album with zero item rows could never be deleted before). One direct conflict reconciled by hand: both branches added a field to `execute_folder_cleanup_apply`'s Completed return dict (this branch's own `moved_records`, the other branch's `changed_count`) -- both kept. One accidental regression in the other branch avoided: its diff for `tests/test_beets_transaction_engine.py` silently dropped an existing rollback-assertion test; not reproduced, that test is untouched here. One test fix pulled forward that this cherry-pick's own dead-code removal made newly necessary: `test_music_format_preferences_integration.py` asserted an unscoped `'DELETE FROM items WHERE id=?'` substring anywhere in `app.py`, which happened to be satisfied only by one of the three now-removed dead functions -- switched to a real, scoped assertion (`'plan_track_replacement'`). Full backend suite: 2974/2974, 0 failures, 0 errors, 144 skipped. `verify_arch003_mutation_inventory.py --check`: 468 entries, 39 unresolved, all matched.
  **Wave 29, `allow_different_releasegroup`/`force` bypass removed (follow-up to the cherry-pick above):** the cherry-picked `existing_album_reconcile_v1` change above initially kept an opt-in override on its Release-Group-mismatch identity gate. On review this is exactly the shape of thing the non-negotiable rules warn against -- a live, caller-invokable bypass on an MB identity gate with zero current callers exercising it -- so it was removed rather than left dormant, per the standing rule that AI is optional/untrusted but MusicBrainz identity evidence is not something any caller gets to opt out of validating. Checked against the actual remaining unmigrated blockers first, not removed blind: every one of this repo's real duplicate/album-merge routes already enforces its own, unconditional RG-match requirement with no override anywhere in the current unmigrated code -- `clean_rgid_group_merge` only operates on album pairs it has already filtered down to a single shared `mb_releasegroupid` cluster before the merge even runs; `clean_merge_duplicate_album`/`apply_album_duplicate_resolver` route through `_library_duplicate_merge_safety()`, whose own blocker list explicitly refuses "different MusicBrainz release-group IDs" with no bypass path. None of them needs `existing_album_reconcile_v1` to accept a cross-RG merge in order to migrate correctly. `create_existing_album_reconcile_plan`/`execute_existing_album_reconcile_apply`'s RG-mismatch rejection is unconditional again; `BeetsClient.plan_existing_album_reconcile()` no longer accepts the kwarg and defensively strips both `allow_different_releasegroup` and `force` from any dict payload before the request is sent, so a caller cannot silently re-enable it by passing a raw payload either. If a future migration (e.g. a "split album" cluster like `album_merge_split_album`, which today performs partial item-level moves with no RG check at all) turns out to genuinely need a deliberate cross-RG merge path, the right shape is not a bare bool: a durable, audited justification field recorded in the transaction's own metadata, not a silently-honored request flag -- not designed here since nothing currently needs it.
  **Wave 30 (continued `other`-domain migration, same branch):** starting point 468 sinks / 39 unresolved `ARCH003_BLOCKER` across 16 functions. Migrated, in order: (1) `_clean_remove_orphaned_items()`/`_clean_remove_empty_albums()` onto `album_maintenance_v1` (`remove_tracks`/`remove_album` modes via `beets_client.plan_album_maintenance`/`apply_album_maintenance`/`delete_album`) -- selection (file-existence / track-count checks) stays a local read, deletion goes through the engine grouped one Plan/Apply call per album; added a real, previously-missing `item_attributes` side-table cleanup to `execute_album_maintenance_apply()` itself (no DB-level FK cascade existed for it, so every prior caller of this family's item-delete paths, not just this one, was leaking orphaned attribute rows) -- 3 sinks resolved. (2) New narrow family `album_duplicate_merge_v1` (`create_album_duplicate_merge_plan`/`execute_album_duplicate_merge_apply`/`rollback_album_duplicate_merge`, new `/albums/duplicate-merge/{plan,apply,rollback}` engine endpoints, `BeetsClient.merge_duplicate_albums()`) for DB-only "merge two duplicate album rows, inherit missing fields, retire the source" -- `existing_album_reconcile_v1` was seriously evaluated and rejected after reading its actual code (it mandates each moved item's file exist on disk and re-verifies path/stat/symlink state, appropriate for its real import-reconciliation job but wrong for a DB-only merge with no file involvement, and it has no target-field-inheritance step); migrated `clean_merge_duplicate_album`/`clean_rgid_group_merge` (confirmed identical merge logic by reading both) onto it -- 6 sinks resolved. (3) `clean_rgid_group_relink()` (a single-album `mb_albumid`/`mb_releasegroupid` UPDATE) onto `album_metadata_repair_v1` directly -- both fields were already allowlisted, no engine change needed -- 2 sinks resolved. (4) `_run_normalize_artists_if_needed()`'s DB-side albumartist rename onto `album_metadata_repair_v1` (confirmed the family already propagates an album-level identity field into every item's own diff, so no separate item UPDATE call was needed) -- 2 sinks resolved. (5) `library_sync_deleted()`'s whole-library missing-file DB cleanup onto `album_maintenance_v1`, same per-album-grouped pattern as (1), with the final summary now reporting actual engine-confirmed counts instead of originally-planned ones -- 2 sinks resolved. Total this wave: 15 sinks resolved (468->453 would be the naive arithmetic, but the new `album_duplicate_merge_v1` family's own controlled engine-side SQL adds new `TRANSACTION_STATE`-classified sinks, not `ARCH003_BLOCKER` ones); actual: 468 total sinks -> 460, 39 unresolved `ARCH003_BLOCKER` -> 24. 28 new regression tests added across 4 new test files (`test_arch003_wave30_orphan_and_empty_album_cleanup.py`, `test_arch003_wave30_album_duplicate_merge_routes.py`, `test_arch003_wave30_normalize_artists.py`, `test_arch003_wave30_library_sync_deleted.py`) plus 4 engine-level tests appended to `test_beets_transaction_engine.py` for the new family. Full backend suite: 3006/3006, 0 failures, 0 errors, 144 skipped. `verify_arch003_mutation_inventory.py --check`: 460 entries, 24 unresolved, all matched.
  **Wave 30, not migrated (see Wave 31 below for what happened to (c)/(e)/(f)):** (a) `_match_tracks_from_mb_shared()` (5 sinks) and (b) `apply_album_duplicate_resolver()`'s "retag" path remained unmigrated after Wave 30's investigation -- both reassessed in Wave 31, see below. (d) the `library_*`/`BEET_BIN` group (10 sinks) also remained unmigrated -- reassessed and given a firm disposition in Wave 31, see below.
  **Wave 31 (continued `other`-domain migration, same branch):** starting point 458 sinks / 20 unresolved `ARCH003_BLOCKER`. (1) **New engine security fix, found while building this wave's first migration, not part of the original scope:** `album_maintenance_v1`'s existing but never-called `deduplicate`-mode `fix_updates` payload (`rename=False` / "repoint_db": change an item's `items.path` DB value with no physical file move) had **zero path validation anywhere** -- `create_album_maintenance_plan`'s root/symlink checks from an earlier CodeQL finding only covered the `rename=True` branch, and `execute_album_maintenance_apply`'s TOCTOU/root/symlink revalidation loop explicitly filtered to `type == "move_file"`, excluding `repoint_db` entirely. Confirmed unused by any real caller (repo-wide search) before fixing -- not exploited in practice, but a real gap in an already-shipped primitive. Fixed: every `fix_updates` entry now gets root containment + symlink rejection at Plan time regardless of `rename`; `repoint_db` additionally requires the new path already exist as a real file and the old path to not exist; Apply gained a parallel TOCTOU/root/symlink revalidation pass for `repoint_db` entries. Old/new paths are resolved against `allowed_roots` for filesystem checks (a real Beets library commonly stores `items.path` relative to the music dir) while the literal caller string is preserved for the `WHERE path=?` bind, so a relative stored path validates correctly; resolving an already-absolute path is a no-op, so the existing `candidates`-based `filename_cleanup` caller (always absolute paths) is unaffected -- its own regression test still passes unchanged. `fix_leaked_db_paths()` (1 sink) migrated onto this now-validated primitive via new `BeetsClient.repoint_item_db_path()`. (2) **Real matching-correctness defect found and fixed as part of migrating, not skipped to avoid touching behavior:** `album_merge_split_album()` never had any Release-Group identity check -- it unconditionally overwrote a moved item's own `mb_releasegroupid` (and every other album-level field) with target's. Fixed: a real per-item RG conflict check now runs before any merge. `album_duplicate_merge_v1` (the Wave 30 family) was generalized rather than a second family invented -- two new optional, backward-compatible Plan payload keys (`item_ids` for a partial/caller-selected item subset instead of always-whole-album, `adopt_target_fields` reversing the field-copy direction for the split-album case) with per-item before-state capture so Rollback can restore adopted fields precisely; all 4 existing Wave 30 tests for the default whole-album shape still pass unchanged. `album_merge_split_album()` migrated onto this via new `BeetsClient.merge_split_album_items()`; its local `shutil.copy2(LIB_PATH, ...)` DB backup is removed (not migrated) -- superseded by the engine transaction's own rollback data, and `LIB_PATH` was never actually reachable from the web-manager container in the supported deployment, so that backup never really worked there. (3) `_do_scan_job._scan()` (2 sinks) reassessed and migrated: confirmed by reading `app.py`'s own gate (`_legacy_local_scan_enabled()`, checked against `BEETS_ENABLE_LEGACY_LOCAL_SCAN`) that this whole auto-scan path never starts in the supported external-engine deployment by default -- Wave 30's "runs against every real library every 30 minutes" blast-radius framing overstated the risk for the current architecture. Migrated onto `album_maintenance_v1` the same per-album-grouped pattern as `library_sync_deleted()` (Wave 30), plus a separate read-only sweep reusing `beets_client.delete_album()` to reproduce the original code's broader "any empty album, not just ones this scan found" semantics. Total this wave: 458->456 total sinks, 20->18 unresolved `ARCH003_BLOCKER`. 29 new regression tests across 4 new test files plus additions to `test_beets_transaction_engine.py`. Full backend suite and targeted batches green throughout (see individual commits for exact counts). `verify_arch003_mutation_inventory.py --check`: 456 entries, 18 unresolved, all matched.
  **Wave 31, not migrated (see Wave 32 below for reassessment):** (a) `_match_tracks_from_mb_shared()` -- one genuinely useful finding stands (the `_engine_acoustid_lookup()` primitive), reassessed further in Wave 32 with a more fundamental blocker found. (b) `apply_album_duplicate_resolver()`'s retag path -- reassessed in Wave 32 against the now-generalized `album_duplicate_merge_v1`. (c) the `library_*`/`BEET_BIN` group -- reassessed in Wave 32; 2 of the 4 turned out tractable.
  **Wave 32 (continued `other`-domain migration, same branch):** starting point 456 sinks / 18 unresolved `ARCH003_BLOCKER`. (1) **`library_merge_artist()` and `library_normalize_artists()` migrated off `BEET_BIN` entirely** -- both closing the SQL-sink problem and the "no local Beets execution fallback" violation together, the actually-correct fix rather than a workaround. `library_normalize_artists()` was confirmed to be literally duplicate logic to `_run_normalize_artists_if_needed()` (already migrated in Wave 30) -- the identical, already-proven migration was applied verbatim. `library_merge_artist()` is the same shape (rename one artist's whole discography into another) and migrated identically, with one real improvement over a straight port: a rejected rename for one album no longer blocks the other albums in the same batch from being renamed and relocated. `library_mbsync_all()` and `library_move_all()` were reconsidered and NOT migrated: `beet mbsync`'s own MB-matching logic and `beet update`'s DB-vs-disk resync both have no per-album engine analog to delegate to, and `library_move_all()`'s trailing empty-directory cleanup already depends on direct `MUSIC_ROOT` filesystem access the web-manager container does not have in the supported deployment -- migrating only their much smaller SQL/filesystem sinks while the actual subprocess-driven core stayed broken would have been exactly the misleading cosmetic fix this wave was told not to do. 8 resolved (4 + 4), 2 sinks remain correctly unresolved for this pair. (2) `apply_album_duplicate_resolver()`'s retag path reassessed against the now-generalized `album_duplicate_merge_v1` (Wave 31's `item_ids`/`adopt_target_fields` extension) and NOT migrated: a real semantic difference exists, not a superficial one. `adopt_target_fields` copies one *uniform* field dict onto every selected item (correct for "these items now belong to this album"); retag needs *per-item, different* `mb_trackid`/`disc`/`track`/`title` values (each duplicate item is manually assigned to a *different* specific missing-track slot in the release, not just reparented to a different album). `album_mb_track_repair_v1` was also considered and rejected for the same reason from a different angle: its `mb_tracks`/`track_matches` injection still runs its own alignment algorithm against the whole set, it has no "trust this caller's explicit item-to-track mapping, skip alignment" mode. A real fix needs a genuinely new, narrow mechanism (an item-keyed field-override map layered on top of `album_duplicate_merge_v1`'s uniform `adopt_fields`, restricted to a small track-slot allowlist distinct from the album-level `_ALBUM_DUPLICATE_MERGE_ADOPT_FIELDS` set) -- considered but not built this wave: it would be a third non-trivial extension to the same security-relevant family within two rounds, and the same adversarial rigor given to the first two extensions was not available alongside everything else this wave, so it was not rushed. (3) `_match_tracks_from_mb_shared()` reassessed with a corrected, more precise finding than Wave 31's: the Unicode-normalization pipeline was NOT actually the blocker -- `backend/import_guard.py` (the exact module `_match_tracks_from_mb_shared` itself already calls through the `_guard_release_track_matches_missing_target` alias) is a small, dependency-free, already-shared module with no circular-import risk at all; its `release_track_matches_missing_target()`/`filter_wanted_tracks_against_missing()` take an *optional* `title_norm_fn` and fall back to a perfectly adequate built-in normalizer (position/`mb_trackid`-first matching, simple casefold-and-strip-punctuation title fallback) when none is supplied -- zero porting work needed for `target_tracks` support. Investigating further surfaced the *real* blocker instead: `create_album_mb_track_repair_plan()` hard-rejects with `repair_rg_not_established` whenever the candidate release has a Release Group but the album's own `mb_releasegroupid` is still blank -- correct and load-bearing for that family's *other* real callers (a repair must not silently stamp a first-time identity), but several of `_match_tracks_from_mb_shared`'s own real call sites (`import_folder_with_id`'s album-match flow, the general re-match-after-setting-`mb_albumid` flow at line ~11872) are used to establish `mb_releasegroupid` for the very first time -- they stamp only `mb_albumid` before calling it, and `_match_tracks_from_mb_shared`'s own write-back is what stamps `mb_releasegroupid` today. Weakening `create_album_mb_track_repair_plan`'s RG-established precondition to accommodate this would defeat the check's purpose for its other established callers -- exactly the "don't weaken existing family guarantees" rule this wave was told to respect. Not migrated; this is a harder, more fundamental mismatch than a normalization port, and needs either a distinctly-named "establish identity" Plan variant with its own, deliberately looser precondition, or leaving `_match_tracks_from_mb_shared` as the dedicated "first sync" path permanently. Total this wave: 456->452 total sinks, 18->10 unresolved `ARCH003_BLOCKER`. 8 new regression tests across 2 new test files. Full backend suite green (see final report for exact counts). `verify_arch003_mutation_inventory.py --check`: 452 entries, 10 unresolved, all matched.
  **Wave 32, remaining unresolved (10 sinks, 4 named items):** `_match_tracks_from_mb_shared()` (5, see (3) above), `apply_album_duplicate_resolver()`'s retag path (3, see (2) above), `library_mbsync_all()` (1) and `library_move_all()` (1) (see (1) above) -- all with specific, evidence-based reasoning above, none hidden or reclassified to fake progress.
  **Wave 32, Control Agent root-default audit (Part 3 of the original brief, first done this wave):** searched `backend/transaction_engine.py` and `backend/beets_control_agent.py` for the `os.environ.get("MUSIC_ROOT", "/music")`-style wrong-literal-default pattern (also `DOWNLOADS_ROOT`/`"/downloads"`, `STAGING_ROOT`/`"/staging"`, `TORRENT_SOURCE_ROOTS`/`"/torrents"`), cross-referenced against the real, only-supported two-container Docker topology (`docker-compose.full.yml`/`docker-compose.yml`/`Dockerfile.beets`/`.env.example`): the `beets` engine container mounts `MUSIC_LIBRARY_PATH` (default `/data/media/music`) and `DOWNLOAD_PATH` (default `/data/torrents`), and never sets `MUSIC_ROOT`/`DOWNLOADS_ROOT`/`STAGING_ROOT`/`TORRENT_SOURCE_ROOTS` anywhere (not in the compose `environment:` blocks, not baked into `Dockerfile.beets`) -- so in the real deployment, every one of these direct reads always hits its literal default, never an operator-set override. No blind search-and-replace was done; every occurrence was individually traced to its caller(s).
  - `backend/beets_control_agent.py` itself defines the three correct resolver helpers (`_resolved_music_root()`, `_resolved_downloads_root()`, `_resolved_staging_root()`, lines ~120-129), each falling back through the real `MUSIC_LIBRARY_PATH`/`DOWNLOAD_PATH` constants (not a wrong literal) when unset -- confirmed correct against the real topology and left unchanged. It had exactly one other wrong-default pattern outside those helpers: 5 call sites (`/imports/review/cleanup/plan`, `/tracks/replacement/plan`, `/tracks/replacement/apply`, `/albums/existing-reconcile/plan`, `/albums/existing-reconcile/apply` handlers) each read `os.environ.get("TORRENT_SOURCE_ROOTS", "/torrents")` as a single list entry. Two real defects, both fixed: (a) the `"/torrents"` default did not match the real mount (`/data/torrents`); (b) more significantly, app.py's own `TORRENT_SOURCE_ROOTS` (module-level constant, `app.py:573-576`) is documented and parsed as a **comma-separated list** (default `"/data/torrents/music,/data/torrents,/data/downloads"`) -- the control agent's single-string read silently could never honor more than a bare single-path override of the same operator-set env var, and a comma-separated value (the only format app.py itself ever produces) would be treated as one literal path matching nothing. Fixed by adding `_resolved_torrent_source_roots()` (same env var name, same comma-split parsing, same default literal as app.py, so one operator-set value governs both containers -- the established convention already used for `PLAYLIST_DIR`/`PLAYLIST_DOWNLOAD_ROOT`) and switching all 5 call sites to unpack it (`*_resolved_torrent_source_roots()`). Currently inert in production either way (env var never set, and it is one additional entry in an `allowed_roots` list that already separately includes the correctly-resolved downloads root) -- fixed as real hygiene/consistency debt, not a live exploit.
  - `backend/transaction_engine.py` has 50 occurrences of the pattern across 37 functions, all as `music_allowed_roots or [str(os.environ.get("MUSIC_ROOT", "/music"))]` (or the `staging_allowed_roots`/`DOWNLOADS_ROOT`/`STAGING_ROOT` equivalent) -- a true fallback, only reached when the caller passes no roots at all. Traced every real (non-comment, non-docstring) call site in `backend/beets_control_agent.py` for all 37: every production caller passes `music_allowed_roots`/`staging_allowed_roots` explicitly, populated via the correct `_resolved_*_root()` helpers -- confirmed by direct reads, not just the cross-reference script (which threw false positives from prose mentioning a function name with a trailing `()`, e.g. comments in both backend files and in test docstrings; each flagged hit was individually opened and read before being counted as real or discarded). This makes the wrong literal defaults dead code in production for all 37 -- no fix applied, correctly so; changing an unreachable literal string carries only regression risk for zero behavior change.
  - One exception found and fixed: `create_album_cleanup_plan()` (the one function in this group using a differently-named `allowed_roots` parameter, not `music_allowed_roots` -- itself a false-positive source in the cross-reference script, since its caller, `/albums/cleanup/plan`, does correctly pass `allowed_roots=[_resolved_downloads_root(), _resolved_music_root()]`) had a real, independent defect in its own body: it **unconditionally prepended** the env-derived `music_root` to the roots list regardless of whether `allowed_roots` was supplied, instead of using it as a true fallback like every sibling function -- and the "refuse to delete the music root directory itself" guard compared only against that same env-derived (and, in the real topology, always-phantom) value, making it permanently dead code rather than a working guard. Fixed: `roots` is now `allowed_roots` when supplied, falling back to the env-derived root only when it is not; the env-derived default literal was also corrected from `/music` to `MUSIC_LIBRARY_PATH`'s own real default (`/data/media/music`); and the root-itself refusal now checks against the caller-supplied roots that actually authorize the operation, so it can actually fire. Net effect is a genuine (if previously low-severity/fail-safe, never a false-accept in production) security hygiene fix, not a behavior change for any real caller. 4 new regression tests added (`tests/test_sec002_wave15_controlled_mutations.py::AlbumCleanupRootDefaultRegressionTests`), covering: no leaked phantom root under the real (no override env vars) condition; an unrelated sibling root no longer treated as implicitly authorizing; the real-default fallback when `allowed_roots` is omitted entirely; and the root-itself refusal actually firing. 2 new regression tests added for the `TORRENT_SOURCE_ROOTS` fix (`tests/test_sec002_wave27_config.py::TestWave27RootResolution`), covering the real-topology default and the comma-separated override.
  - Test-only gaps (not production-reachable; every one of these functions' real `beets_control_agent.py` callers were separately confirmed to pass the roots correctly) found and left as documented debt rather than fixed, given zero production risk and the churn of touching every one: `create_album_metadata_plan()`/`execute_album_metadata_apply()` omit `music_allowed_roots` in several calls in `tests/test_album_lifecycle_wave24_engine_unit.py` (confirmed harmless: `music_allowed_roots` there only feeds `_resolve_db_path()`'s relative-item-path join, and the test's assertions -- an identity-conflict rejection, then a plain field-only update -- never depend on that join resolving to a real path); `create_import_folder_plan()` omits `music_allowed_roots` in 3 calls in `tests/test_wave25_docker_acceptance_round_fixes.py` (confirmed harmless: `music_allowed_roots` there is only persisted into the plan for a later `execute_import_folder_apply()` step these particular tests never reach -- they test Plan-only failure modes: nonexistent source, non-directory source, symlink rejection). Several other hits the cross-reference script originally flagged (`create_folder_cleanup_plan()`/`create_album_mb_track_repair_plan()` in `tests/test_wave25_round_review_fixes.py`; `execute_library_cleanup_apply()` in `tests/test_library_cleanup_closure.py`) were individually opened and confirmed to be either prose-only false positives (a docstring or comment mentioning the function name, no real call) or calls that already correctly pass the roots -- discarded, not counted as gaps.
  - Full disposition, by count: 3 correct helper functions (no change) + 5 `TORRENT_SOURCE_ROOTS` call sites (fixed: comma-parsing + default) + 36 `transaction_engine.py` functions with the `X or [...]` pattern, all production-unreachable (no change) + 1 `transaction_engine.py` function (`create_album_cleanup_plan`) with a real unconditional-inclusion defect (fixed) + 2 test files with confirmed-harmless test-only gaps (documented here, not fixed) + several false-positive script hits across both backend files and 3 more test files (individually verified and discarded). `python -m py_compile` clean on both backend files; `verify_arch003_mutation_inventory.py --check` and `generate_endpoint_inventory.py --check` both pass unchanged (452/10, endpoint inventory up to date) -- this was a validation-logic fix, not a new mutation sink.
  **Wave 33 (attempted extensions/migrations on the remaining 10, rather than re-deferring on Wave 32's reasoning):** starting point 452 sinks / 10 unresolved. (1) **`album_mb_track_repair_v1` extended for real** with the 4 capabilities `_match_tracks_from_mb_shared()` needs, all opt-in via `payload` and default-off (every existing caller/test for the family passes unchanged, confirmed by a full run of the pre-existing 127-test suite for this family before and after): `target_tracks` (subset filtering via the shared, already-proven `release_track_matches_missing_target()` from `backend/import_guard.py` -- confirmed, contrary to Wave 31's original claim, trivially importable at module level with zero circular-dependency risk), `acoustid_verify` (a new `_mb_track_repair_acoustid_check()` cross-check ported from app.py's `_album_track_fingerprint_check()` policy, using the existing `_engine_acoustid_lookup()` machinery via a lazy same-process import mirroring the existing `helpers_mb` pattern), `zero_unmatched` (zeroes `track` for items aligning to nothing, mirroring app.py's contract, restorable on rollback), and `allow_establish_release_group` (a narrow, explicit opt-in bypass of `repair_rg_not_established` -- `repair_identity_mismatch`, a real conflict, is never bypassable -- with its own TOCTOU-revalidated Apply write and Rollback restore-to-blank). Investigating this surfaced a genuine, previously-undocumented **live bug**: `reimport_disk()` (app.py line ~23593) already calls `beets_client.plan_album_mb_track_repair({"album_id": aid, "mb_albumid": mb_albumid})` for a fresh, MB-confirmed reimport and `raise`s on any Plan rejection -- meaning any reimport establishing an album's Release Group identity for the first time (routine for a genuinely first-time import, and confirmed via a real Docker-topology check that neither `MUSIC_ROOT` nor any of its siblings are ever set for the `beets` engine container) was silently failing the whole reimport with `repair_rg_not_established` before this wave, previously unnoticed because no test exercised this exact path with a blank-RG fixture. 19 new regression tests across 2 new test files, all green. **Full migration of `_match_tracks_from_mb_shared()`'s 5 SQL sinks was attempted, not completed**: past building and proving the 4 extensions above, closing the sinks for real also requires (a) year/month/day/country album-field stamping, which `album_mb_track_repair_v1` does not do at all today (a 5th gap found only while tracing the exact SQL this function replaces, `UPDATE albums SET year=?/country=? WHERE id=?`) and (b) a careful per-call-site audit of `_match_tracks_from_mb_shared`'s 6 real callers' `zero_unmatched` usage and log-message/return-value compatibility -- correctly scoped as further work, not attempted this wave given the size of what was already built and tested, rather than rushed. (2) **`apply_album_duplicate_resolver()`'s retag path reassessed with a concrete, code-level (not just abstract) attempt** at a third `album_duplicate_merge_v1` mode: tracing the exact data app.py's retag path handles surfaced a SECOND, independent structural mismatch beyond Wave 32's original finding (per-item field values) -- `old_album_ids` there is a *set*, meaning duplicate items being retagged can originate from *multiple distinct* source album rows, not the single `source_album_id` `album_duplicate_merge_v1`'s entire data model (TOCTOU check, resource-key locking, single-source-row deletion) assumes. Supporting both a per-item field-override map AND multi-source-album origin would not be "a third option" on the existing family any more -- it is a structurally different shape. Not migrated; this is a firmer, more specific finding than Wave 32's, not a rehash. (3) **`library_mbsync_all()`/`library_move_all()` reassessed and BOTH migrated for real** (Wave 32 had deferred both entirely): `library_mbsync_all()`'s orphaned-album prune (a raw local `DELETE FROM albums WHERE id IN (...)`) migrated onto `beets_client.delete_album()` (`album_maintenance_v1`'s `mode="remove_album"`, the same controlled per-album removal path a prior wave's `_do_scan_job()` migration already established for exactly this "album with zero items" shape) -- this also fixed a real, latent bug in the code it replaced: `log.append(f"Pruned {len(ids)} ...")` sat outside the `if rows:` block that defined `ids`, so the common case of zero orphaned albums always raised a `NameError`, silently absorbed by the surrounding `except` as a harmless-looking warning. `library_move_all()`'s trailing empty-directory sweep (local `os.walk(MUSIC_ROOT)` + `Path.rmdir()`) migrated onto a DB-derived candidate-directory list (a read, not a mutation -- computed from `items.path` and its full ancestor chain up to `MUSIC_ROOT`, before `beet move` runs) fed one at a time through the already-existing, already-tested `folder_cleanup_v1` family (`beets_client.plan_folder_cleanup()`/`apply_folder_cleanup()`, `action="remove_empty"`) -- which runs inside the engine container that actually has real filesystem access, unlike the web-manager container the old code ran in and, per the real, only-supported Docker topology (`docker-compose.full.yml`), never had a mount into `MUSIC_ROOT` at all -- meaning the pre-Wave-33 cleanup step was not just an ARCH-003 sink but silently inert in every real deployment. `beet mbsync`/`beet update`/`beet move` themselves remain local `BEET_BIN` subprocess invocations, unchanged from Wave 32's reasoning (no per-album engine analog exists for `beet mbsync`'s own MB-matching logic or `beet update`'s DB-vs-disk resync logic) -- this wave closed the two raw mutation *sinks* each route had, not the whole `BEET_BIN` dependency, which remains separately tracked. 10 new regression tests in a new test file, all green. Total this wave: 452->454 total sinks (+4, the new engine-side `album_mb_track_repair_v1` writes -- legitimate engine-owned mutations, auto-classified `CONTROLLED_MEDIA_MUTATION` under the family, not blockers), 10->8 unresolved `ARCH003_BLOCKER`. `verify_arch003_mutation_inventory.py --check`: 454 entries, 8 unresolved, all matched. `generate_endpoint_inventory.py --check`: up to date (0 routes need manual review after regeneration).
  **Wave 33, remaining unresolved at this point (8 sinks, 2 named items) -- see "Wave 33 continued" below, both fully closed in the same wave:** `_match_tracks_from_mb_shared()` (5 -- the engine-side capability now existed and was tested, migration of the sinks themselves needed one further, precisely-scoped increment: year/month/day/country stamping support plus a 6-call-site behavioral audit, see (1) above) and `apply_album_duplicate_resolver()`'s retag path (3 -- two independent, concretely-traced structural mismatches with `album_duplicate_merge_v1`, see (2) above).
  **Wave 33 continued (coordinator follow-up: confirm the reimport_disk() bug is actually fixed, complete the mb_track_repair migration, take one more concrete look at the retag path):**
  - **`reimport_disk()`'s `repair_rg_not_established` bug was NOT yet fixed** at the point it was first reported: the capability (`allow_establish_release_group`) had been built and tested at the engine level, but no caller had been wired to pass it, so the bug was still live on that commit. Fixed now: all 5 of `reimport_disk()`'s own `plan_album_mb_track_repair(...)` call sites (every one of them, not just the one originally spotted -- `_merge_imported_album_into_existing`'s post-merge repair, the main per-album retag step, the post-mbsync re-sync step, and the per-item album re-derivation step all independently hit the same code path) now pass `allow_establish_release_group: True`. Safe uniformly: the flag only ever changes behavior when the album's RG is genuinely still blank; every other case (already established, matching or conflicting) is byte-identical to before. Verified two ways: (a) an AST-based structural test (`tests/test_sec002_wave8_engine_ownership.py::ReimportDiskEstablishesReleaseGroupTests::test_every_plan_album_mb_track_repair_call_passes_allow_establish_release_group`) asserting every one of the 5 calls carries the flag, so it cannot silently regress; (b) two real, non-mocked engine tests using `reimport_disk()`'s exact payload shape (`{"album_id", "mb_albumid", "allow_establish_release_group": True}`) against `create_album_mb_track_repair_plan`/`execute_album_mb_track_repair_apply` directly -- one proving a blank-RG album's Release Group is now established end-to-end, one proving a real conflict (`repair_identity_mismatch`) is still rejected exactly as before, unchanged.
  - **`stamp_release_metadata` (the 5th capability) built and tested**, closing the field-stamping gap identified last round: `album_mb_track_repair_v1` can now plan/apply/roll back album-level `year`/`country` stamping from the same MB release lookup it already performs, opt-in via `payload["stamp_release_metadata"]`, TOCTOU-revalidated, only planning a change when the value genuinely differs. Required a real, additive fix one layer down: `helpers_mb.fetch_mb_release_tracklist()` (the engine's default MB fetch path) did not extract `date`/`country` from the MB API response at all -- app.py's own, separate `_fetch_mb_release_tracklist()` always had, the two functions had simply drifted. 5 new regression tests, all green; 2 test-fixture `albums` table schemas (`tests/test_sec002_wave19_mb_track_repair.py`, `tests/test_album_lifecycle_wave24_real_ipc.py`, the latter a real-HTTP-IPC integration suite) had to gain a `country` column to keep matching the real Beets schema this SELECT now reads -- caught by two real (not hypothetical) test failures, fixed, both suites re-verified green.
  - **`_match_tracks_from_mb_shared()`'s 5 SQL sinks: the coordinator directed porting app.py's actual algorithm into the engine rather than attempting to prove the two independently-written procedures agree -- done, and the migration is now complete.** Added `backend.mb_alignment.greedy_album_track_alignment()`, a verbatim port of `_match_tracks_from_mb_shared()`'s own matching loop (item-order-driven, first-come-exclusive-claim via a `used_indices` set -- once an earlier item claims a track, no later item can ever take it away, even with a higher score), and swapped `create_album_mb_track_repair_plan()`'s call from `summarize_mb_track_alignment()`/`best_album_track_match()` (the different, rank-based-displacement procedure) to it -- this is now the family's *only* alignment procedure, used by every caller, not a second opt-in path. The existing 216-test family suite passed unchanged against the swap with zero modifications needed. Added the adversarial tests the coordinator asked for specifically (`tests/test_arch003_wave33_mb_track_repair_extensions.py::GreedyAlignmentAdversarialTests`): two candidate items competing for one track, the later one scoring strictly higher -- confirmed by direct comparison that the OLD algorithm picks the later, higher-scoring item (displacement) while the new, ported one picks the earlier, first-processed item (exclusive claim) exactly as app.py always did, all the way through to a real Apply writing the correct item's on-disk tag. With the algorithm now provably identical (not merely bounded), `_match_tracks_from_mb_shared()` itself was rewired to delegate through `beets_client.plan_album_mb_track_repair()`/`apply_album_mb_track_repair()` (`target_tracks`/`zero_unmatched`/`acoustid_verify: True`/`allow_establish_release_group: True`/`stamp_release_metadata: True` -- the last three always-on for this caller specifically, since its pre-migration behavior always ran the AcoustID check, always stamped a discovered Release Group with no conflict check at all, and always stamped year/country), closing all 5 raw SQL sinks for real. The `-1`/`0`/`count` return-value contract its 6 real callers depend on (3 of them raising on `< 0`) is preserved by mapping the engine's own MB-lookup-failure path (no `"code"` field, the one case that must still mean "genuinely failed, refuse to proceed") to `-1`, and every other outcome (matched, nothing to do, a named refusal) to `0`-or-more. `tests/test_dedup_acoustid_fingerprint.py`'s structural test of this function's old inline AcoustID check was updated to assert the new delegation shape instead (found and fixed via a real test failure, not by inspection alone); `tests/test_import_auto_enqueue.py`/`tests/test_music_format_preferences_integration.py`'s uses of this function's name as an unrelated source-slice boundary marker were confirmed unaffected.
  - **`apply_album_duplicate_resolver()`'s retag path: decomposed per the coordinator's explicit direction, not extended.** Added `item_field_overrides` to `album_duplicate_merge_v1` (`create_album_duplicate_merge_plan`/`execute_album_duplicate_merge_apply`/`rollback_album_duplicate_merge`) -- a small, additive, per-item counterpart to the existing uniform `adopt_target_fields`: `{str(item_id): {field: value}}`, restricted to a deliberately separate, small `_ALBUM_DUPLICATE_MERGE_ITEM_OVERRIDE_FIELDS` allowlist (`mb_trackid`, `disc`, `track`, `title`), every key required to name an item actually in the request's `item_ids` (an id outside that set is refused outright, never silently ignored or silently authorizing a write to an unrelated item). This did **not** touch the family's core single-source-album control flow at all -- no change to its TOCTOU checks, resource locking, or source-row retirement logic, addressing the coordinator's explicit concern about regressing the family's other real callers (confirmed via the full pre-existing suite for this family, unaffected). `apply_album_duplicate_resolver()`'s retag path is now decomposed at the app.py layer into N single-source-album `album_duplicate_merge_v1` Plan+Apply call pairs (one per distinct source album among the selected retag items, grouped via `entry["item"]["album_id"]`), each using `item_ids` + `adopt_target_fields: True` (the moved items inherit the target album's own current `album`/`albumartist`/`year`/`mb_albumid`) + the new `item_field_overrides` (each item's own specific missing-track slot). The target album's own row `mb_albumid` (which `album_duplicate_merge_v1` never touches -- `adopt_target_fields` only ever copies *from* it) is stamped separately through the existing, already-controlled `beets_client.update_album_metadata()`. **The accepted trade-off, exactly as directed:** the old raw-SQL code's whole-batch atomicity (one `executemany`/`commit` covering every retagged item across every source album at once) is now N independent per-source-album atomic operations. A later source's merge failing after an earlier one already committed is a real, possible partial-completion outcome -- reported truthfully via a new `retag_failures` list in the job's own result (`{"source_album_id", "item_ids", "error"}` per failed source; the job itself completes with `"ok": true` and a truthful partial count rather than crashing or silently claiming total success), and each succeeded source's merge remains individually rollback-able through the family's own existing, unmodified rollback endpoint. Documented explicitly both here and in a code comment at the app.py call site, per the coordinator's instruction -- not a silent behavior change. No sharp edge requiring cross-album atomicity was found in this codebase's real callers of this route (the frontend already treats each duplicate-resolver run as a single best-effort batch job with a log, not an all-or-nothing transaction the UI depends on), so decomposition was not stopped partway through. 6 new engine-level regression tests (`tests/test_beets_transaction_engine.py`) covering per-item distinct values, the two new validation rejections, rollback, and composition with `adopt_target_fields`; 4 new/updated app.py-layer tests (`tests/test_sec002_wave27_result_handling.py::DuplicateResolverMultiSourceRetagTests`, plus the pre-existing single-source test updated for the new call shape) covering two distinct source albums producing two separate merge calls with per-source field overrides, one source failing without blocking the other (with the truthful partial-failure report asserted directly), and dry-run making no engine calls at all.
  **ARCH-003 "other" domain is now fully closed: 0 unresolved `ARCH003_BLOCKER` entries, down from 18 at the start of Wave 32.** Total this continuation: 456->450 total sinks (net -6: the 5 raw SQL sinks `_match_tracks_from_mb_shared()` closed, minus a few app.py sinks removed by the retag decomposition, plus new legitimate engine-side `item_field_overrides` writes, all auto-classified `CONTROLLED_MEDIA_MUTATION`), 8->0 unresolved `ARCH003_BLOCKER`. 8 (prior) + 4 (adversarial alignment) + 6 (`item_field_overrides` engine-level) + 4 (retag decomposition app.py-level) = 22 new regression tests this continuation. Full backend suite run twice: see final report for exact counts. `verify_arch003_mutation_inventory.py --check`: 450 entries, 0 unresolved, all matched. `generate_endpoint_inventory.py --check`: up to date, 0 routes need manual review.
  **Real-Docker acceptance testing (Parts 8/9 of the original brief) found and fixed a genuine, container-boot-breaking defect this same wave's `backend.import_guard`/`backend.mb_alignment`/`backend.beets_control_agent` imports introduced -- confirmed by no other verification method used this whole engagement (`py_compile`, the full backend unit/integration suite run twice, both inventory gates).** `Dockerfile.beets` copies `backend/*.py` FLAT into `/opt/beets-web-manager-agent/` inside the `beets` engine container -- no `backend` package structure exists there at all; every cross-module import inside that container is bare (`import transaction_engine`, not `from backend import transaction_engine`), a convention `track_align.py`'s own import in `transaction_engine.py` already handled correctly (`try: from track_align import ... except ImportError: from backend.track_align import ...`) before this wave. The new module-level `from backend.import_guard import release_track_matches_missing_target` (added for `target_tracks` subset filtering) used the bare `backend.`-prefixed form unconditionally, with no such fallback -- and it runs at *module import time*, not lazily inside a function, so it crashed the engine container's own Python process before the control agent could even start: `ModuleNotFoundError: No module named 'backend'`, `s6-supervise ...: unable to spawn ./run: Exec format error` at the process-supervisor level, `dependency failed to start: container beets is unhealthy` at the compose level -- the **entire engine container failed to boot**, not a narrower feature regression. The two *lazy* imports added the same way this wave (`from backend.mb_alignment import greedy_album_track_alignment, album_track_score`, and `from backend.beets_control_agent import _engine_acoustid_lookup`, both inside `create_album_mb_track_repair_plan`) would not have crashed container startup, but would have made `album_mb_track_repair_v1`'s entire alignment step (already wrapped in a broad `except Exception`) and its `acoustid_verify` option silently fail with "Track alignment failed: No module named 'backend'" on every real invocation in the deployed engine -- a total, silent failure of the whole family in production, not caught by any unit test (which all import `backend.transaction_engine` as a real package, the repo/test context where the `backend.`-prefixed form works fine -- this class of bug is invisible to every verification method except actually booting the real container). `backend/import_guard.py` and `backend/mb_alignment.py` were also never added to `Dockerfile.beets`'s `COPY` list at all (a second, independent omission -- even a correctly-guarded import would have found the module genuinely missing inside the container). Fixed: both new modules added to `Dockerfile.beets`'s COPY list; all three import sites given the same flat-import-first, package-import-fallback `try/except ImportError` shape `track_align.py`'s own import already used, so this exact class of bug cannot reappear undetected in either module without breaking the pattern visibly. Confirmed fixed by rebuilding and re-booting the real two-container stack: engine container now reports healthy, and the fixed alignment/AcoustID code paths are exercised for real by this wave's own `album_mb_track_repair_v1` unit tests (which do not, and structurally cannot, catch this specific class of bug -- they never boot the engine's own flattened deployment layout) plus, now, real Docker acceptance. See the final report for exact scenario counts.
  **Wave 29 (Resolution of `BEET_BIN` Local-Subprocess Architectural Debt, PR #112):**
  - **Context & Defect Eliminated:** The supported deployment topology runs two distinct containers: `beets-web-manager` (pure web/UI frontend, no `beets` installation, no media/config filesystem mounts) and `beets` (engine container owning Beets CLI, SQLite database, and media filesystem). Previous waves left residual callers in Web Manager attempting local execution of `BEET_BIN` (`/lsiopy/bin/beet`) via `_beet_run()` or local `subprocess.run/Popen`, which failed unconditionally in the production Web Manager image.
  - **New Engine Endpoints & BeetsClient Delegation:** Implemented narrow, parameter-validated engine endpoints on `ControlAgentHandler` in `backend/beets_control_agent.py` and matching client methods on `BeetsClient` in `backend/beets_client.py`:
    - `POST /library/mbsync` (`beets_client.mbsync(query, async_job)`): executes `beet -c <cfg> mbsync [query]` under engine library lock with async `JobStore` streaming.
    - `POST /library/move` (`beets_client.move_library(query, rescan_first, pretend, async_job)`): executes optional `beet update` rescan followed by `beet move [query]` under engine lock.
    - `POST /submissions/submit` (`beets_client.acoustid_submit(query, dry_run)`): executes `beet submit` for AcoustID fingerprint submissions under engine lock.
  - **Web Manager Callers Migrated & Dead Code Removed:**
    - `app.py`: `library_mbsync_all()` and `library_move_all()` rewired to engine IPC (`beets_client.mbsync()` and `beets_client.move_library()`); trailing empty-folder pruning routes through `beets_client.plan_folder_cleanup()` / `apply_folder_cleanup()`.
    - `routes_submissions.py`: `attach_album_mbids()` rewired to `album_metadata_repair_v1` (`beets_client.plan_album_metadata()` / `apply_album_metadata()` with `force_write_tags=True`). AcoustID submission delegated to `beets_client.acoustid_submit()`.
    - `job_engine.py` & `app.py`: removed dead local subprocess infrastructure (`BEET_BIN`, `_beet_run`, `_beet_env`, `_write_job_beets_config`, `_write_plugin_job_beets_config`, `_parse_remote_beet_command`).
  - **Structural Regression Enforcement:**
    - Added AST-based scanner test `tests/test_arch003_boundary_enforcement.py::test_no_production_web_manager_beets_local_subprocess_execution` enforcing zero `BEET_BIN`, `_beet_run`, or Beets CLI subprocesses in any Web Manager production module (while allowlisting legitimate system utilities: `ffprobe`, `ffmpeg`, `ldd`, node/bun JS runtime probes, `spotiflac`).
    - Mutation inventory check `python scripts/verify_arch003_mutation_inventory.py --check` passes cleanly with **444 entries, 0 unresolved blockers**.
- Priority: P0.
- Status: **Complete & Verified for all domains, including `other` and `BEET_BIN` local-subprocess debt** (Album Lifecycle, Import Reconciliation, Library Cleanup, AI Import, Configuration, and the `other` domain -- `_match_tracks_from_mb_shared()`, `apply_album_duplicate_resolver()`'s retag path, `library_merge_artist`/`library_normalize_artists`/`library_mbsync_all`/`library_move_all`, `fix_leaked_db_paths`, `album_merge_split_album`, AcoustID submission, and MBID attachment -- all migrated onto controlled transaction families or narrow engine IPC operations with zero local Beets subprocess execution and zero unresolved `ARCH003_BLOCKER` entries).
=======
- Affected area: Import review, playlist processing, missing-track replacement, duplicate handling, MusicBrainz submission preparation, AI-assisted candidate suggestion.
- Evidence: A canonical matching evidence engine exists (`backend/matching/`: explicit `AcoustIDStatus`/`ConfidenceState` states, Release Group vs. Release separation, global one-to-one track alignment, a single `can_auto_accept(policy)` automation gate). It is fully adopted by `backend.matching_contract.build_album_matching_decision`, `backend.mb_alignment.greedy_album_track_alignment`, `backend.track_align.align_tracks`, and playlist album-tag placement's auto-accept decision. Independent matching/confidence logic still exists elsewhere: the AI Suggest scorer family, `_folder_release_preflight`/`_score_mb_release_candidate`, `_album_track_norm`/`_album_track_score`/`_best_album_track_match` (the highest blast-radius remaining case — a dozen call sites, several of which already implement partial order-dependent duplicate protection but not the canonical engine's global optimum), several AcoustID interpretation helpers with their own status vocabularies, `routes_submissions._compare_release_tracks`, `backend.album_match.build_album_match_plan`, and `backend.beets_control_agent._playlist_import_identity_ok`.
- Current risk: Release-group candidates can be accepted or rejected differently depending on entry point; a fuzzy/numeric score can authorize a decision one workflow's canonical engine would refuse.
- Desired state: Every production matching/confidence decision either uses the canonical engine directly, is a thin wrapper around it, or is proven candidate-generation-only (candidate generation may stay workflow-specific; candidate *evaluation* may not).
- Safe migration approach: Migrate one family at a time behind equivalence tests (old vs. new outcome comparison for changed hard-identity results); do not replace a scoring formula wholesale without re-verifying every threshold tuned against it. See `docs/adr/0002-release-group-id-is-canonical-album-identity.md` and `docs/adr/0003-ai-is-optional-and-not-source-of-truth.md` for the identity/AI rules this must not violate.
- Priority: P0. Status: Open.
>>>>>>> origin/chore/repository-hygiene

## ARCH-004 Job Persistence And Idempotency Are Uneven

- Affected area: `job_engine.py`, import review jobs, playlist download/sync jobs, AI batch import, acquisition, replacement, maintenance runner.
- Evidence: `JobStore` is in-memory. `PythonJob` supports structured state and cooperative cancellation. Some workflows add checkpoint files and uniqueness checks; others rely on route-local state or result inference.
- Current risk: Process restart, retry, or duplicate starts can repeat completed steps, lose progress, or leave stale active status unless each workflow implemented its own protections correctly.
- Desired state: Shared job requirements for operation identifiers, idempotency, resource locks, bounded retries, checkpoints, heartbeats, cancellation checks, and terminal-state recovery.
- Safe migration approach: Add job contract tests and a reusable idempotency/checkpoint helper. Migrate long-running workflows by risk, starting with import/replacement/playlist mutations.
- Priority: P1. Status: Open.

## ARCH-005 Frontend Decision Logic Can Drift From Backend Authority

- Affected area: `frontend/src/features/importReview/ImportReviewPage.tsx`, other large feature panels, `frontend/src/api/types.ts`.
- Evidence: Large feature components render data, poll jobs, manage local workflow state, and calculate some block/eligibility display. Frontend panels sometimes adapt backend evidence shapes locally rather than displaying them as-is.
- Current risk: UI can enable, hide, or label actions differently than backend eligibility; explanations can diverge from backend safety decisions.
- Desired state: Backend returns authoritative evidence, conflicts, safety result, and action eligibility. Frontend displays those fields and only handles presentation state.
- Safe migration approach: Extend backend contracts first, then simplify frontend helpers as contract consumers. Add static and UI tests for visible evidence and disabled/destructive actions.
- Priority: P1. Status: Open.

## ARCH-006 Provider Boundaries Are Inconsistent

- Affected area: MusicBrainz, AcoustID, OpenAI, Discogs, SLSKD, yt-dlp, Plex, Lidarr.
- Evidence: `helpers_mb.py` and `backend/slskd.py` are extracted boundaries; `app.py` still contains direct OpenAI, Discogs, yt-dlp, Plex, and download orchestration logic.
- Current risk: Retry, rate-limit, secret redaction, and failure representation differ by provider.
- Desired state: Each provider has a small adapter with typed inputs/outputs, explicit transient/permanent failure classification, bounded retries, and redaction.
- Safe migration approach: Extract adapters only when changing a workflow for a real bug. Preserve API responses and add contract tests.
- Priority: P2. Status: Open.

## ARCH-007 Raw SQL Compatibility Layer Bypasses Repository Boundary

- Affected area: Web-manager legacy `_db()`/`RemoteSQLiteConnection` callers and control-agent SQLite endpoints.
- Evidence: The original local SQLite violation has been removed from the web-manager service boundary: `app.py`'s `_db()` yields `backend.beets_client.RemoteSQLiteConnection`, and the actual `sqlite3.connect(LIB_PATH)` calls are in `backend/beets_control_agent.py` inside the `beets` container. However many routes still express data access as raw SQL-shaped calls over the remote compatibility layer instead of using explicit repository/client methods.
- Current risk: Lock handling, pagination, row factories, path normalization, query allowlisting, and write safety can vary by caller. The service boundary is safer than before, but the repository boundary is still weak.
- Desired state: A small Beets repository/client layer owns common reads/writes and lock retry policy. Web-manager routes call typed methods rather than raw SQL-shaped compatibility calls.
- Safe migration approach: Consolidate repeated read-only queries first by adding explicit `BeetsClient` methods backed by control-agent endpoints. Move write paths only when covered by mutation tests and transaction/audit records.
- Priority: P2.
- Status: Done.
  **Wave 33 real Docker acceptance (Part 8 of the original brief) confirmed this is not a theoretical risk-variability concern for at least some callers -- `backend.beets_client.BeetsClient.raw_sqlite_query()` (what `_db()`'s `RemoteSQLiteConnection`/`RemoteSQLiteCursor` unconditionally routes every `.execute()` call through) is not a compatibility shim with variable behavior; it is a hard, unconditional `raise BeetsError(...)` with zero request ever attempted ("Raw SQL is intentionally unavailable" per its own docstring) -- confirmed by direct source inspection, not inferred from the failure alone. Any route whose data-access path still runs through `_db()` for a read (not just a write) is completely non-functional in the real, only-supported two-service deployment, not degraded. Found concretely, not theoretically, this wave: `library_merge_artist()`/`library_normalize_artists()` (both migrated onto controlled engine mutation calls in Wave 32 -- see the ARCH-003 entry above -- but their *read* step, finding which albums/items match the requested `albumartist`, was never touched and still uses `_db()`) both failed for real against a live two-container acceptance stack with `ERROR: Raw SQLite queries are not permitted; use structured library query helpers` -- not a hypothetical, an actual job failure captured from real container logs.
  **Wave 34: the two concrete Wave 33 failures fixed, plus two more callers of the same defect found and fixed, all verified against a real two-container acceptance stack, not unit tests alone.** Added four narrow, workflow-specific, fixed-shape `BeetsClient` read methods, each backed by a new control-agent endpoint (server-owned WHERE clause, bound parameters, no caller-supplied SQL, no general query surface): `find_all_albums_by_albumartist()` (`GET /albums?albumartist=...`), `list_distinct_albumartists()` (`GET /library/albumartists`), `find_all_orphan_albums()` (`GET /albums?orphan=true`), and `list_distinct_item_paths()` (`GET /library/item-paths`).
  **ARCH-007 Repository-Wide Closure:** The complete repository-wide audit and structured read boundary migration is finished. Every production Web Manager code path that directly queried the Beets database, used `_db()`, used `raw_sqlite_query()`, constructed raw SQL for authoritative Beets data, or assumed direct DB visibility has been migrated onto server-owned, typed, authenticated `BeetsClient` HTTP methods calling `backend/beets_control_agent.py`.
  1. **Zero Legacy `_db()` Sinks in Production:** All 90 legacy `_db()` call sites across `app.py` and `routes_submissions.py` were eliminated. Direct SQLite connections to Beets library database in Web Manager code are completely removed.
  2. **12 Server-Owned Control Agent Endpoint Families & 19 Semantic BeetsClient Methods:** Added and verified semantic endpoints across all domains (Library Stats, Album/Item lookups by ID/MBID/RGID, RGID Group detail resolution, Album Folder DB resolution, Playlist DB Row matching, Format Replacement lookup, Search and Multi-field filtering, Cleanup Indexing, Orphan Cleanup, Deleted Files synchronization, Track mapping, and Health reporting).
  3. **Strict Fail-Closed Error Taxonomy:** All read and write boundaries fail closed with structured exceptions (`BeetsUnavailableError` on HTTP 502/503/504 / network timeout / connection refused, `BeetsAuthError` on HTTP 401/403, `BeetsBadRequestError` on HTTP 400, `BeetsNotFoundError` on HTTP 404, `BeetsError` on HTTP 500 internal server error). Web Manager routes return clean HTTP 503/401/502/500 responses and never silently swallow errors or fall back to local disk/DB reads.
  4. **Durable AST Boundary Enforcement:** `tests/test_arch007_boundary_enforcement.py` enforces via AST inspection that 0 production functions in Web Manager call `_db()`, 0 call `raw_sqlite_query()`, 0 connect directly to SQLite Beets DB, and all Beets read operations route exclusively through typed `BeetsClient` methods.
  5. **Comprehensive Verification:** 100% passing across 2x clean backend test suite runs (3,330 passed, 0 failures, 0 errors), two-service Docker acceptance suite, first-run Docker acceptance suite, security secret scans, compose security validation, mutation inventory check (445 sinks, 0 unresolved blockers), endpoint inventory check (244 routes, 0 unresolved), and frontend CI checks (`npm ci`, `npm audit`, `npm run typecheck`, `npm run lint`, `npm test` [58/58 passed], `npm run build`).

## ARCH-008 Agent Instructions Were Too Large And Duplicated

- Affected area: `AGENTS.md`, `CLAUDE.md`, `docs/AGENT_WORKFLOW.md`, `docs/AI_ENGINEERING_RULES.md`, and governance tests.
- Evidence: The integrated governance branch adds shared workflow documentation and static tests in `tests/test_engineering_governance_docs.py`. The instructions now distinguish review-and-fix work, push/PR/merge/deploy restrictions, migration-sensitive work, and the difference between implemented, tested, verified, ready to push, ready to merge, and ready to deploy.
- Current risk: Low. Future drift is still possible if agent files are edited without updating the shared docs and governance tests.
- Desired state: Concise agent files reference shared rules, and governance tests catch contradictory terminology or unsafe authority claims.
- Safe migration approach: Keep agent files short, keep project-manager assignments allowed, and update shared docs and tests together for rule changes.
- Priority: P1.
- Status: Done.

## ARCH-009 Release ID And Release-Group Identity Are Inconsistently Modeled

- Affected area: `app.py`, `helpers_mb.py`, `frontend/src/api/client.ts`, `frontend/src/api/types.ts`, `frontend/src/features/importReview/ImportReviewPage.tsx`, import review, folder import, repair, cleanup, playlist placement, and replacement workflows.
- Evidence: `docs/adr/0002-release-group-id-is-canonical-album-identity.md` defines MusicBrainz release-group ID as canonical album identity, but runtime code still carries both `mb_albumid` and `mb_releasegroupid` with mixed responsibilities — some call sites treat `mb_albumid` as the primary operational album candidate; others correctly use it only as edition-level evidence (concrete tracklist, date, country, medium, track positions). API/client payload shapes are not fully consistent about which field a given response requires.
- Current risk: Matching, folder placement, imports, repairs, cleanup, and replacement can disagree about whether a release ID or release-group ID is the album identity; a release-level candidate could incorrectly drive canonical folder identity.
- Desired state: Shared matching and import contracts carry both fields with explicit names and semantics: release-group ID for canonical album identity, optional release ID for edition-level evidence, recording IDs for track identity. No workflow substitutes a release ID where release-group identity is required.
- Safe migration approach: Do not rename fields globally. Add contract tests and typed result shapes around current entry points first, then update one workflow at a time to require/propagate `mb_releasegroupid` for album identity while retaining `mb_albumid` as representative release evidence. Keep API compatibility by accepting existing fields during transition.
- Required tests: Unit tests for release-vs-release-group normalization; contract tests for MusicBrainz release/release-group candidates; import review tests for selected-match propagation; playlist placement tests for representative release evidence; repair/replacement tests that keep release-group folder identity stable; cleanup tests that do not merge distinct release groups; a regression test proving a release ID is never written where a release-group ID is required.
- Priority: P0. Status: Open.

## ARCH-012 Library Disk-Walk Loses Real Albums Whose Folder Is Entirely Missing

- Affected area: `app.py` `_build_library_payload()`, the leftover-`missing_by_bucket` injection pass ("any remaining missing items belong to artists not on disk at all").
- Evidence: When an entire album's on-disk folder is removed but its Beets rows remain, the leftover-injection code's artist/album name-based lookup can fail to match items back to their real `album_id`, landing the album in the singleton bucket instead of being counted as a real album — undercounting `/api/library`'s `albums`/`tracks` totals.
- Current risk: Medium. Read-only display accuracy only (no data mutation risk), but a real and reproducible undercount.
- Desired state: The leftover-missing-item injection reliably resolves every fully-missing album back to its real `album_id`, most likely by keying off each missing item's own `album_id` (already present on the item row) rather than an artist/album name-string lookup, which is fragile to normalization differences between stored fields and on-disk folder names.
- Safe migration approach: This is disk-walk traversal logic — build a side-by-side parity harness (old output vs. new output over a real or realistically-shaped fixture) before changing `_build_library_payload()`, so a fix cannot silently regress a different part of the walk.
- Required tests: A parity/regression test modeling a real album whose entire folder is missing from disk, asserting it is still counted in `albums`/`tracks`, not `singleton_tracks`.
- Priority: P1. Status: Open.

## ARCH-019 Job/Transaction-Status Test Can Be Intermittently Flaky Under CI Load

- Affected area: `tests/test_import_review_attach_enforcement.py::test_undo_restores_previous_identity_values`, which asserts a transaction's `status` is already `"Rolled Back"` immediately after its rollback job reports `success`.
- Evidence: Observed once on real GitHub Actions CI when this repository's own dual-workflow-trigger setup (push + pull_request) ran two independent job instances against the identical commit simultaneously — one passed, the other failed on exactly this assertion under real resource contention. Never reproduced locally or in an unloaded CI run; an immediate re-run of the identical commit passed cleanly.
- Current risk: Low but real — points at a possible race between a rollback job reporting success and the transaction store's own status field committing its terminal value, that only manifests under genuine concurrent load.
- Desired state: The test polls/rechecks status with a short bounded retry instead of asserting immediately after the job reports success, unless a real ordering bug in the production rollback path is confirmed, in which case that path itself should not report the job "success" until the transaction record's status update has actually committed.
- Safe migration approach: Reproduce deliberately under artificial CI-like load before deciding which side needs the fix — do not guess from a single observed instance.
- Priority: P3. Status: Open.

## SEC-001 Retained Plex Credential After Diagnostic Exposure

- Scope: `PLEX_TOKEN`, used only by `beets-web-manager`.
- Risk: the token appeared in private diagnostic session output during earlier development and must be treated as potentially exposed.
- Decision: owner reviewed the exposure and explicitly chose to retain the current token rather than revoke/replace it, accepting the associated risk.
- Existing mitigations: `PLEX_TOKEN` is sent via a request header, never a URL query parameter. A stable, persisted, installation-specific Plex client identifier is sent on every request, so any future rotation is cleanly attributable. Token values are never logged, printed, or included in reports.
- Future recommended action: rotate `PLEX_TOKEN` when convenient (Plex Web → Settings → Account → Authorized Devices → remove the session, then generate a replacement).
- Status: Accepted Risk. Not a release blocker.
