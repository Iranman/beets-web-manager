# Technical Debt Register

Statuses: Open, In Progress, Blocked, Done.

## ARCH-001 Monolithic Route/Domain/Mutation Coupling

- Affected area: Backend `app.py` and route modules.
- Evidence: Route scan still finds most operator workflows in `app.py`, including import review, library, cleanup, deduplication, playlists, Plex, configuration, transactions, and import endpoints. The external-engine branch changed the service boundary: Beets command arrays now flow through `job_engine._beet_run()`/`BeetsClient`, and `_db()` returns `RemoteSQLiteConnection` instead of a local file handle. The monolith still mixes routing, matching, orchestration, legacy SQL-shaped calls, and residual filesystem mutation call sites.
- Current risk: Changes to one workflow can accidentally alter unrelated behavior. Compatibility shims reduce migration blast radius, but they also hide whether a route is using a well-owned service method or a legacy remote-SQL/remote-command shape.
- Desired state: Routes remain thin. Application services own workflows. Domain modules own matching and safety decisions. Provider adapters own external calls. Repository/client methods own Beets-engine access.
- Safe migration approach: Extract one tested service at a time. Prefer replacing legacy SQL-shaped and command-array callers with explicit `BeetsClient` methods while preserving route signatures and responses.
- Priority: P0.
- Status: Open.

## ARCH-002 Duplicated Matching And Confidence Rules

- Affected area: Import review, playlist processing, missing-track replacement, duplicate handling, MusicBrainz submission preparation.
- Evidence: Matching logic exists in `app.py` (`_track_ai_*`, import review revalidation, `_best_album_track_match`, playlist matching), `helpers_mb.py`, `backend/track_align.py`, `backend/mb_alignment.py`, and `backend/import_guard.py`.
- Current risk: Release-group candidates can be accepted or rejected differently depending on entry point. AI, AcoustID, tracklist, duration, and title evidence can be weighted inconsistently.
- Desired state: One shared matching result contract and one authoritative backend implementation for confidence, conflicts, warnings, and action eligibility.
- Safe migration approach: Define contract around existing strongest structures, add adapter tests for each entry point, then migrate callers one workflow at a time.
- Priority: P0.
- Status: Open.

## ARCH-003 Mutations Do Not All Use One Controlled Boundary

- Affected area: Beets command execution, filesystem cleanup, metadata write, artwork repair, import, playlist placement, deduplication, replacement.
- Evidence: The `beets` control agent now owns Beets CLI execution, direct SQLite compatibility queries, tag writes, and file move/delete endpoints inside the authoritative engine container. `app.py` still contains many legacy command-array call sites and residual direct filesystem operations (`shutil.move`, `shutil.rmtree`, `Path.unlink`, `os.replace`, copies, and directory removals) that are not all planned, audited, and verified through one transaction boundary. `backend/transaction_engine.py` exists but is not universal.
- Current risk: Preview/audit/rollback behavior varies by workflow. Partial failure can be hard to recover or may be reported inconsistently. Residual filesystem call sites also make it easy to accidentally depend on web-manager media mounts that the supported two-service Compose files intentionally do not provide.
- Desired state: Mutating workflows use shared plan/apply/verify/recover semantics with root validation, diffs, audit records, and recovery information, and all media/Beets mutations execute in the `beets` engine boundary.
- Safe migration approach: Wrap one high-risk mutation family at a time using `TransactionStore` and explicit control-agent/BeetsClient APIs rather than replacing all callers. Start with deletes/moves from Import Review and cleanup paths.
- Priority: P0.
- Status: Open.

## ARCH-004 Job Persistence And Idempotency Are Uneven

- Affected area: `job_engine.py`, import review jobs, playlist download/sync jobs, AI batch import, acquisition, replacement, maintenance runner.
- Evidence: `JobStore` is in-memory. `PythonJob` supports structured state and cooperative cancellation. Some workflows add checkpoint files and uniqueness checks; others rely on route-local state or result inference.
- Current risk: Process restart, retry, or duplicate starts can repeat completed steps, lose progress, or leave stale active status unless each workflow implemented its own protections correctly.
- Desired state: Shared job requirements for operation identifiers, idempotency, resource locks, bounded retries, checkpoints, heartbeats, cancellation checks, and terminal-state recovery.
- Safe migration approach: Add job contract tests and a reusable idempotency/checkpoint helper. Migrate long-running workflows by risk, starting with import/replacement/playlist mutations.
- Priority: P1.
- Status: Open.

## ARCH-005 Frontend Decision Logic Can Drift From Backend Authority

- Affected area: `frontend/src/features/importReview/ImportReviewPage.tsx`, other large feature panels, `frontend/src/api/types.ts`.
- Evidence: Large feature components render data, poll jobs, manage local workflow state, and calculate some block/eligibility display. API types are extensive and frontend panels sometimes adapt backend evidence shapes locally.
- Current risk: UI can enable, hide, or label actions differently than backend eligibility. User-facing explanations can diverge from backend safety decisions.
- Desired state: Backend returns authoritative evidence, conflicts, safety result, and action eligibility. Frontend displays those fields and only handles presentation state.
- Safe migration approach: Extend backend contracts first, then simplify frontend helpers as contract consumers. Add static and UI tests for visible evidence and disabled/destructive actions.
- Priority: P1.
- Status: Open.

## ARCH-006 Provider Boundaries Are Inconsistent

- Affected area: MusicBrainz, AcoustID, OpenAI, Discogs, SLSKD, yt-dlp, Plex, Lidarr.
- Evidence: `helpers_mb.py` and `backend/slskd.py` are extracted boundaries, while `app.py` still contains direct OpenAI, Discogs, yt-dlp, Plex, and download orchestration logic.
- Current risk: Retry, rate-limit, secret redaction, and failure representation differ by provider.
- Desired state: Each provider has a small adapter with typed inputs/outputs, explicit transient/permanent failure classification, bounded retries, and redaction.
- Safe migration approach: Extract adapters only when changing a workflow for a real bug. Preserve API responses and add contract tests.
- Priority: P2.
- Status: Open.

## ARCH-007 Raw SQL Compatibility Layer Bypasses Repository Boundary

- Affected area: Web-manager legacy `_db()`/`RemoteSQLiteConnection` callers and control-agent SQLite endpoints.
- Evidence: The original local SQLite violation has been removed from the web-manager service boundary: `app.py`'s `_db()` yields `backend.beets_client.RemoteSQLiteConnection`, and the actual `sqlite3.connect(LIB_PATH)` calls are in `backend/beets_control_agent.py` inside the `beets` container. However many routes still express data access as raw SQL-shaped calls over the remote compatibility layer instead of using explicit repository/client methods.
- Current risk: Lock handling, pagination, row factories, path normalization, query allowlisting, and write safety can vary by caller. The service boundary is safer than before, but the repository boundary is still weak.
- Desired state: A small Beets repository/client layer owns common reads/writes and lock retry policy. Web-manager routes call typed methods rather than raw SQL-shaped compatibility calls.
- Safe migration approach: Consolidate repeated read-only queries first by adding explicit `BeetsClient` methods backed by control-agent endpoints. Move write paths only when covered by mutation tests and transaction/audit records.
- Priority: P2.
- Status: Open.

## ARCH-008 Agent Instructions Were Too Large And Duplicated

- Affected area: `AGENTS.md`, `CLAUDE.md`, `docs/AGENT_WORKFLOW.md`, `docs/AI_ENGINEERING_RULES.md`, and governance tests.
- Evidence: The integrated governance branch adds shared workflow documentation and static tests in `tests/test_engineering_governance_docs.py`. The instructions now distinguish review-and-fix work, push/PR/merge/deploy restrictions, migration-sensitive work, and the difference between implemented, tested, verified, ready to push, ready to merge, and ready to deploy.
- Current risk: Low. Future drift is still possible if agent files are edited without updating the shared docs and governance tests.
- Desired state: Concise agent files reference shared rules, and governance tests catch contradictory terminology or unsafe authority claims.
- Safe migration approach: Keep agent files short, keep project-manager assignments allowed, and update shared docs and tests together for rule changes.
- Priority: P1.
- Status: Done.

## ARCH-009 Release ID And Release-Group Identity Are Inconsistently Modeled

- Affected area: `app.py`, `helpers_mb.py`, `frontend/src/api/client.ts`, `frontend/src/api/types.ts`, `frontend/src/features/importReview/ImportReviewPage.tsx`, import review, folder import, repair, cleanup, playlist placement, and replacement workflows.
- Evidence: ADR-0002 defines MusicBrainz release-group ID as canonical album identity, while runtime code still carries both `mb_albumid` and `mb_releasegroupid` with mixed responsibilities. Examples where `mb_albumid` is treated as the primary operational album candidate include `app.py` functions `_resolve_album_release_for_import`, `_folder_release_preflight`, `_start_reimport_disk_job_internal`, `_album_mb_completeness`, and frontend fallback logic such as `initialMbid`/candidate matching in `ImportReviewPage.tsx`. Examples where release ID is legitimately edition-level evidence include `_fetch_mb_release_tracklist(mb_albumid)`, `_resolve_release_group_to_release`, `_playlist_album_tag_release_placement`, and `helpers_mb.py` release/recording candidate payloads that need concrete release tracklists, dates, country, medium, and track positions. Missing or inconsistent `mb_releasegroupid` propagation remains visible across API types and client payloads where some responses require only `mb_albumid`, while newer import-target preview and selected-match paths expose `release_group_id` or `mb_releasegroupid`.
- Diagnostic snapshot: On 2026-07-20, `git grep -I -o` at commit `1e065a6772bc6eed83e2d7be1c71dc3128285907` found 968 `mb_albumid` and 350 `mb_releasegroupid` occurrences across all tracked files in this branch. A source-focused scan over `app.py`, route modules, `helpers_mb.py`, `backend/`, and `frontend/src/` found 853 and 312 respectively. This is a dated diagnostic snapshot; it is not proof that every occurrence is incorrect and it is not a permanent invariant. The reviewer's approximate 572/172 count likely used a narrower or different search scope.
- Current risk: Matching, folder placement, imports, repairs, cleanup, and replacement can disagree about whether a MusicBrainz release ID or release-group ID is the album identity. A release-level candidate may incorrectly drive canonical folder identity, while valid edition-level release evidence is still required for tracklist and date comparison.
- Desired state: Shared matching and import contracts carry both fields with explicit names and semantics: release-group ID for canonical album identity, optional release ID for edition-level evidence, and recording IDs for track identity. No workflow substitutes a release ID where release-group identity is required.
- Safe migration approach: Do not rename fields globally. First add contract tests and typed result shapes around current entry points. Then update one workflow at a time to require/propagate `mb_releasegroupid` for album identity while retaining `mb_albumid` as representative release evidence. Keep API compatibility by accepting existing fields during transition and emitting warnings when only release ID is present for album-level decisions.
- Required tests: Unit tests for release-vs-release-group normalization, contract tests for MusicBrainz release and release-group candidates, import review tests for selected match propagation, playlist placement tests for representative release evidence, repair/replacement tests that keep release-group folder identity stable, cleanup tests that do not merge distinct release groups, and regression tests proving a release ID is never written where a release-group ID is required.
- Priority: P0.
- Status: Open.

## ARCH-010 AI Batch Import State Writes Have No Versioning Or Compare-And-Swap

- Affected area: `app.py` AI Batch Import durable state (`_ai_batch_write_state`, `_ai_batch_commit`, `_AI_BATCH_STATE_DIR/<batch_job_id>.json`) and every route that reads-modifies-writes it outside the active worker thread. Direct writers, confirmed by inspection to call `_ai_batch_commit`/`_ai_batch_write_state` themselves from the route: `ai_batch_pause`, `ai_batch_stop` (also directly kills the JobStore job before its own whole-state write), `ai_batch_import_status`'s reconcile-and-commit path, `ai_batch_import_recover`'s reconcile-and-commit path, and `start_ai_batch_import`'s same-source-path reconnect commit. `ai_batch_skip` is *not* a direct writer -- confirmed by inspection it only mutates the in-memory `_ai_batch_controls` dict and sets `_ai_batch_skip_event`; it never itself calls `_ai_batch_commit`/`_ai_batch_write_state`. It is an indirect trigger: the active worker thread observes the control/event change on its own next iteration and is the one that actually persists the resulting state. An earlier revision of this entry overstated `ai_batch_skip` as a direct writer; corrected here.
- Evidence: `PR #12` fixed the retry/recover *start* path specifically, across three iterations. The first iteration (`_start_ai_batch_job`, `_run_ai_batch_import`, `_ai_batch_recalculate_batch_state` self-heal) added `_ai_batch_try_reserve_start`/`_ai_batch_release_start`, a process-local guard held only while `_start_ai_batch_job` itself was starting a worker. A second, independent review reproduced a remaining duplicate-worker race: that reservation was released as soon as `_start_ai_batch_job()` returned, before the worker it started had made its first commit. A second recover request landing in that post-start/pre-heartbeat window read the still-pre-reconciliation, all-terminal-looking `folder_states` snapshot; `_ai_batch_recalculate_batch_state`'s finalize branch stamped `worker_alive=False` over the correct just-computed value, so the second request didn't recognize the first worker as active and started a second one. Fixed by replacing the startup-only reservation with `_ai_batch_active_workers`, a registry authoritative for the worker's *entire* lifetime (reserve -> promote to job_id -> ownership-safe release in the worker's own `finally`, only after `_run_ai_batch_import` exits), plus making `_ai_batch_reconcile_state` defer entirely to that registry (skip recalculation/finalization) whenever a worker is registered for the batch, instead of trusting a possibly-stale folder snapshot. A third independent review then found the worker-lifetime registry itself had a post-spawn startup-abort gap: the spawned worker thread's `job_id_ready.wait(timeout=10)` ignored its return value, so a failure in `_ai_batch_persist_job_association`/`_ai_batch_promote_worker` *after* the worker thread was already spawned released the registry from the outer function but left the worker blocked, which then proceeded into real batch work unregistered and unvalidated once the 10s wait elapsed. Fixed with an explicit handoff protocol (`handoff_ready`/`startup_aborted` events plus a registry re-check, `_ai_batch_active_worker_job_id(batch_job_id) == owned_job_id`, as the actual proof of a valid handoff) that the worker must positively pass before calling `_run_ai_batch_import`; cleanup responsibility transfers to the worker thread as soon as it is spawned rather than being retained by the outer function. The underlying mechanism all three bugs came from -- `_ai_batch_write_state` does a whole-file JSON replace with no version field, no compare-and-swap, and no check that the writer's in-memory snapshot is still current -- is unchanged and still used by every route listed above. Any of those routes committing a stale snapshot while the worker thread is concurrently committing newer progress can still silently lose data, by the same shape as the bugs PR #12 fixed for the start path.
- Current risk: A pause/stop/status-poll/recover request landing at the wrong moment relative to worker progress can revert folder state, drop a requeue, or misreport batch status -- same failure mode as the original retry-race bug, just via a different route. Risk is currently mitigated only by these routes' commits generally being fast and infrequent relative to worker commits, not by any structural guarantee. `ai_batch_skip` carries a narrower version of this risk indirectly, through the state the worker itself later persists in response to the control/event change, not through a write of its own. The active-worker registry added in this update mitigates the *duplicate-start* instance of this risk (routes now consult `_ai_batch_worker_registered`/`_ai_batch_active_worker_job_id` rather than trusting a raw state-file read for that specific decision), but does not add versioning to the underlying writes themselves.
- Desired state: A single compare-and-swap or optimistic-concurrency primitive for `_AI_BATCH_STATE_DIR` writes (e.g. a monotonic `state["version"]` checked-and-incremented under `_ai_batch_state_lock`, rejecting a stale-based write) that all commit call sites go through, replacing the current unconditional whole-file overwrite.
- Safe migration approach: Add the version field and CAS check to `_ai_batch_write_state`/`_ai_batch_commit` first, in a mode that only logs a warning on a detected stale write (no behavior change), to confirm how often it actually fires against real traffic before making it authoritative. Then migrate call sites incrementally, starting with the routes identified above. Required regression tests for that migration: route-versus-worker stale-write detection (a route's commit using a snapshot older than the worker's latest must be rejected or merged, not silently overwrite), and confirmation that legitimate sequential commits from the same writer are never rejected.
- Note on runtime model: this app's supported deployment is one Waitress process with a thread pool, not multiple worker processes (see `_ai_batch_active_workers`' comment in `app.py`). A version/CAS field is still worth adding for correctness and clarity even in a single-process model, and becomes necessary (not just nice-to-have) if the deployment model ever changes to multiple processes or containers sharing one state directory. The active-worker registry is explicitly process-local only -- it provides no guarantee across multiple Python processes, multiple containers, or multiple hosts sharing the JSON state directory; durable/file-based locking would be required for that, tracked here as future work, not implemented in PR #12.
- Priority: P2.
- Status: Open.

## ARCH-011 Remote Beets Plugin Diagnostics For `/api/setup/status`

- Affected area: `routes_setup.py`, `backend/beets_client.py`, `backend/beets_control_agent.py`, and setup-status tests.
- Evidence: The integrated branch removes setup-status dependency on `_beet_binary()`/`_run_beet_diagnostic()` and normalizes one authenticated Beets control-agent `/status` snapshot into the existing public fields (`available`, `version`, `plugin_loader_ok`, `plugin_loader_error`, `configured_plugins`, `plugin_failures`) plus `loaded_plugins`, `installed_plugins`, `capabilities`, `commands`, and remote path/tool health. The control agent reports Beets version, loaded plugins, plugin failures, `submit` and `mbsubmit` command readiness, Chroma/fingerprinting, AcoustID lookup/submission, MusicBrainz submission, FetchArt, ReplayGain, LastGenre, Discogs, ListenBrainz, and Discpath state. Unit tests cover connection failure, authentication failure, timeout, malformed shapes, FetchArt loaded/failed, Chroma present/missing, independent submit/mbsubmit readiness, ReplayGain backend readiness, Discpath readiness, redaction, no local subprocess lookup, and one-snapshot reuse. Disposable runtime validation passed for the patched engine, Chroma disabled, invalid control-agent credentials, and unreachable control-agent cases.
- Current risk: Low. Future risk is mostly contract drift between `backend/beets_control_agent.py`, `backend/beets_client.py`, and `routes_setup.py` if one side changes without tests.
- Desired state: `/api/setup/status` reports authoritative remote Beets readiness, fails closed on remote errors, never falls back to a local Beets executable or local `find_spec()` as operational proof, and does not leak the internal API token or raw exceptions.
- Safe migration approach: Keep the current compatibility response shape for frontend callers. Extend the control-agent status contract only with additive structured fields. Do not treat setup-status display as a security boundary; command endpoints must continue enforcing capability checks themselves.
- Required tests: Covered by `tests/test_routes_setup.py`, `tests/test_external_beets_architecture.py`, image verification, and disposable Compose runtime probes.
- Priority: P1.
- Status: Done.

## ARCH-012 Library Disk-Walk Loses Real Albums Whose Folder Is Entirely Missing

- Affected area: `app.py` `_build_library_payload()`, specifically the leftover-`missing_by_bucket` injection pass that runs after the per-artist-folder walk (the "Any remaining missing items belong to artists not on disk at all" block).
- Evidence: Live verification against the real production database (3,144 items / 413 albums, confirmed by direct query) found `/api/library` reporting only 368 unique `album_id`s after the `_library_stats_for_artists()` aggregation fix (see the library-summary-aggregation fix on `integration/external-beets-engine-release-v2`) -- 45 real albums are not represented by any disk-walk card at all. Their items are all "missing" (file gone from disk), and the leftover-injection code's artist/album name-based lookup (`beets_album_lk.get((mart.lower(), malb.lower()), {})`) fails to match them back to their real `album_id`, so `ba_info2` stays `{}` and the card is built with `album_id=0` -- landing in the singleton bucket instead of being counted as a real album.
- Current risk: Medium. Read-only display accuracy only (no data mutation risk -- the underlying Beets database is unaffected), but `/api/library`'s `albums`/`tracks` totals undercount by roughly this margin whenever an entire album folder has been removed from disk while the Beets rows remain.
- Desired state: The leftover-missing-item injection should reliably resolve every fully-missing album back to its real `album_id`, most likely by keying off each missing item's own `album_id` (already present on the item row) instead of, or in addition to, the artist/album name-string lookup, which is fragile to normalization differences between the stored `albumartist`/`album` fields and the on-disk folder-derived name.
- Safe migration approach: This is disk-walk traversal logic -- CLAUDE.md's "`/api/library` is high risk, needs side-by-side parity checks before any traversal-logic change" applies. Do not modify `_build_library_payload()` without first building a side-by-side parity harness (old output vs. new output over a real or realistically-shaped fixture) so a fix here can't silently regress a different part of the walk.
- Required tests: A new parity/regression test modeling a real album whose entire folder is missing from disk, asserting it is still counted in `albums`/`tracks`, not `singleton_tracks`.
- Priority: P1.
- Status: Open.

## SEC-001 Retained Plex Credential After Diagnostic Exposure

- Date: 2026-07-29.
- Scope: `PLEX_TOKEN`, used only by `beets-web-manager`.
- Risk: the token appeared in private diagnostic session output during earlier work on this branch and must be treated as potentially exposed.
- Decision: owner reviewed the exposure and explicitly chose to retain the current token rather than revoke/replace it, accepting the associated risk.
- Existing mitigations: `PLEX_TOKEN` is sent via a request header, never a URL query parameter (query-string tokens are far more likely to end up in access logs/proxies). A stable, persisted, installation-specific Plex client identifier (`X-Plex-Client-Identifier`/`X-Plex-Product`/`X-Plex-Device-Name`) is now sent on every request, so any future rotation is cleanly attributable instead of facing the ambiguity that blocked attribution this time (the token has visibility into 35 authorized devices with no way to isolate which one originally produced it). Token values are never logged, printed, or included in reports.
- Future recommended action: rotate `PLEX_TOKEN` when convenient, using Plex Web -> Settings -> Account -> Authorized Devices to remove the specific session, then generate a replacement.
- Status: Owner accepted. Not a release blocker.

## SEC-002 Main-Branch CodeQL Backlog

- Discovery date: 2026-07-30, during final independent review of PR #49. A first query without `--paginate` under-reported the backlog as 30 open alerts; the authoritative paginated query against `main` at `e694a40e597428614b0be888b05ea5cc58ec7db9` found 320.
- Initial exact alert count: 320 open, `state=open`, `ref=refs/heads/main`. By rule: `py/path-injection` 195, `py/stack-trace-exposure` 81, `py/polynomial-redos` 18, `js/incomplete-url-substring-sanitization` 14, `py/incomplete-url-substring-sanitization` 4, `js/xss-through-dom` 2, and one each of `py/full-ssrf` (critical), `py/clear-text-storage-sensitive-data`, `py/bad-tag-filter`, `py/regex-injection`, `py/weak-sensitive-data-hashing`, `js/insecure-randomness`. By severity: 1 critical, 238 high, 81 medium. By file: `app.py` 277, `index.html` 12, `routes_submissions.py` 10, `routes_setup.py` 8, `backend/transaction_engine.py` 5, `frontend/src/views/Playlists.tsx` 5, `routes_lidarr.py` 3. By age: 313 of 320 (98%) were created in a single baseline scan on 2026-07-20; only 7 postdate that scan.
- Affected files: overwhelmingly `app.py` (86% of the backlog), a single ~49,500-line legacy module. See `docs/ARCHITECTURE.md`/CLAUDE.md for why `app.py` remains the primary route module.
- Root-cause groups (full backlog, not yet individually triaged beyond this batch): the 195 `py/path-injection` and 81 `py/stack-trace-exposure` alerts are almost certainly dominated by a small number of repeated code shapes across `app.py`'s many route handlers (unsanitized filesystem helper parameters; `except Exception as ex: ... f"{ex}"` returned directly in a JSON response), based on the pattern found in this batch, but this has not been confirmed alert-by-alert for the remaining ~312.
- This batch (critical + 7 non-baseline alerts, owner-selected scope -- see below): 8 alerts individually traced and closed out:
  - `#18` `py/full-ssrf` (critical, `routes_submissions.py:1276`, `_fetch_open_graph_metadata`): **proven false positive**. Triple-layered: route-level `_validate_reference_url()` calls `validate_outbound_url()`; the sink itself re-validates immediately before request; `urllib.request.urlopen` is globally monkey-patched to `backend.security.secure_urlopen`, which re-validates and installs `SafeRedirectHandler` so every redirect hop is independently re-checked (closing the classic SSRF-guard-via-redirect bypass) and strips cross-origin auth headers. New tests: `tests/test_outbound_security.py` (`test_install_secure_urllib_patches_module_level_urlopen`, `test_fetch_open_graph_metadata_blocks_ssrf_before_any_socket_use`, `test_fetch_open_graph_metadata_uses_the_secured_opener_for_redirects`). No code change. Dismissed as `false positive`.
  - `#327`/`#328` `py/path-injection` (`app.py`, `_ai_batch_write_state`'s `os.replace` sink): **proven false positive**. `_ai_batch_state_file()` reduces `batch_job_id` to `[A-Za-z0-9_.-]` before it is ever joined onto `_AI_BATCH_STATE_DIR`; no input can introduce a path separator. New test: `tests/test_sec002_codeql_backlog.py::AiBatchStateFileContainmentTests`. No code change. Dismissed as `false positive`.
  - `#331` `py/path-injection` (`app.py:1480`, `_write_plugin_job_beets_config`'s `write_text` sink): **proven false positive**. `temp_path`'s only variable component (`cmd`) is checked with exact-membership against the fixed 4-item `_PLUGIN_CMDS` allowlist (`ytimport`, `ytupdate`, `wlg`, `aisauce` -- none contain path metacharacters) before the call; CodeQL does not model the allowlist check as a taint barrier. New test: `tests/test_sec002_codeql_backlog.py::PluginJobConfigCommandAllowlistTests`. No code change. Dismissed as `false positive`.
  - `#332` `py/clear-text-storage-sensitive-data` (`app.py:1514`, `_write_job_beets_config`'s `write_text` sink; CodeQL's flagged source is `routes_submissions.py:60`, `_acoustid_key()` reading `ACOUSTID_API_KEY`/`ACOUSTID_KEY`): **genuine defense-in-depth weakness, fixed**. The AcoustID key can flow through `_acoustid_submit_config_extra()` into the generated temp beets config, which was written with the process default (world-readable) permissions and never cleaned up. Fixed by `os.chmod(temp_path, 0o600)` immediately after every `_write_job_beets_config()` write, protecting this and any future secret embedded via its `extra` parameter. New test: `tests/test_sec002_codeql_backlog.py::JobBeetsConfigSecretPermissionTests`.
  - `#334`/`#335` `py/path-injection` (`app.py`, `_folder_import_track_count`'s `Path.is_dir()`/`Path.rglob()` sinks): **genuine defense-in-depth weakness, fixed**. `source_folder` reaches this function through several layers of import-matching helpers; at least one traced call path is DB-derived (an existing album's on-disk folder, looked up by ID) but the function itself performed no containment check, so a future or undiscovered caller passing raw request text would have had an unbounded read-only directory walk. Fixed by constraining `source_folder` to `MUSIC_ROOT` or `DOWNLOADS_ROOT` via the existing `_path_is_under()` helper (no new resolver introduced), returning `0` (the function's existing not-available sentinel) for anything outside those roots. New test: `tests/test_sec002_codeql_backlog.py::FolderImportTrackCountRootContainmentTests`.
  - `#401` `py/stack-trace-exposure` (`app.py:10611`, `album_delete_art`): **genuine vulnerability, fixed**. The art-file `p.unlink()` failure path returned the raw exception text (`f"Could not delete {p.name}: {ex}"`) directly in the client-facing JSON error, while the sibling `clear_album_artpath()` failure path a few lines below it in the same function already logged server-side and returned a sanitized message. Made the unlink-failure path match that existing pattern. New test: `tests/test_sec002_codeql_backlog.py::AlbumArtDeleteExceptionSanitizationTests`.
- Alerts dismissed: `#18`, `#327`, `#328`, `#331` at merge time, all `false positive`, individually justified above. **Correction (added during Wave 2 review):** post-merge rescanning of `main` at the PR #52 squash commit found `#332`, `#334`, and `#335` still `open` despite the code fix already being live -- `os.chmod(0o600)` restricts access but does not change what CodeQL's `py/clear-text-storage-sensitive-data` query checks for (the value is still stored unencrypted, which is unavoidable here since beets requires a plaintext YAML value to authenticate with AcoustID), and CodeQL's taint model does not recognize the codebase's `_path_is_under()` helper as a sanitizer for `py/path-injection`. All three were dismissed individually after the fix was verified: `#332` as `won't fix` (architecture decision -- plaintext storage is required by beets, 0600 permissions is the applied mitigation, encryption is not viable since the app must present the credential back to beets in cleartext), `#334`/`#335` as `false positive` (fixed and proven safe by `FolderImportTrackCountRootContainmentTests`; CodeQL simply does not trace through the existing containment helper). This distinction -- code-fixed-and-then-dismissed vs. dismissed-without-any-code-change -- was not previously called out; all 7 of PR #52's dismissed/fixed alerts (`#18`, `#327`, `#328`, `#331`, `#332`, `#334`, `#335`) plus `#401` (auto-transitioned to `fixed` by CodeQL's own rescan, no dismissal needed) are the complete accounting for that PR.
- Security impact of that batch: closed the one critical alert and all 7 non-baseline alerts with either a proven-safe dismissal or a real fix; no user-facing behavior change for the false-positive/won't-fix group, and the three fixes were narrow (file permission, one added containment check reusing an existing helper, one error-message change).

### Wave 2: non-app backend files (routes_submissions.py, routes_setup.py, routes_lidarr.py, backend/transaction_engine.py)

- Starting count (live, paginated, re-verified independently of PR #52's report): 312 open on `main` at `332e12fbda00cedc0d95f9cebcf86baece0fb473` (320 initial minus the 8 alerts PR #52 closed). Confirmed unchanged from the reported figure.
- In-scope count: 25 (`routes_submissions.py` 9, `routes_setup.py` 8, `backend/transaction_engine.py` 5, `routes_lidarr.py` 3) -- one less than the originally estimated maximum of 26, since `#18` (already closed by PR #52) was itself in `routes_submissions.py`.
- Alert numbers: `#128`, `#28`, `#323`, `#30`, `#31`, `#32`, `#33`, `#34`, `#36` (`routes_submissions.py`); `#23`, `#24`, `#25`, `#26`, `#27`, `#102`, `#321`, `#322` (`routes_setup.py`); `#19`, `#20`, `#21` (`routes_lidarr.py`); `#129`, `#130`, `#131`, `#132`, `#133` (`backend/transaction_engine.py`).
- Root-cause groups:
  1. **Unguarded path resolution helper** (`py/path-injection`, `#128`): `routes_submissions._abs_resolved()` performed `Path(path_str).expanduser().resolve(strict=False)` with **no containment check at all**, and its caller chain (`_resolve_folder_submission_target` <- `_resolve_submission_target` <- `GET /api/submissions/target?path=...`) is reachable from an authenticated request query parameter with no other validation in between. This is the most significant finding of this wave -- a genuine arbitrary-directory-read/audio-tag-parse primitive anywhere in the container filesystem. Fixed by requiring the resolved path fall under `MUSIC_ROOT` or `DOWNLOADS_ROOT`, reusing `_path_is_under()` and the `_SUBMISSION_ALLOWED_ROOTS` tuple -- both already defined in the same file and already used for a *different* check (cover-art path, line ~509) but never applied to this function.
  2. **Deliberately-raised, safe-message exceptions** (`py/stack-trace-exposure`, `#23`, `#28`, `#30`, `#31`, `#32`, `#33`, `#321`, `#20`, `#21`, plus all 5 `#129`-`#133` `py/path-injection` alerts): proven false positives across all four files. Each traced exception type (`ValueError` from `_write_env_file`/`_draft_target_ref`/`_validate_reference_url`, `KeyError` from `_resolve_submission_target`, `RuntimeError` from `_lidarr_config_error()` via `_http_error_message()`) is raised only with fixed, deliberately-crafted, non-sensitive text (env-var key names, numeric IDs the caller already supplied, or one of two hardcoded config-status strings) -- never a wrapped raw system/library exception. `TransactionStore._path()` requires a `txn_` prefix and rejects `/`, `\`, and NUL before any of its 5 flagged sink lines, and it is the *only* place in the file that builds a transaction path.
  3. **Genuinely broad `except Exception` leaking to a public response** (`py/stack-trace-exposure`, `#24`/`#322` (`_write_env_file` broad except, two call sites), `#25`/`#26`/`#27` (AI/MusicBrainz/Plex connectivity-test broad excepts), `#323`/`#34`/`#36` (submission-target/reference-url/musicbrainz-validate broad excepts)): fixed by logging the real exception server-side (`app.logger.warning`, type name only) and returning a fixed safe message, while preserving the existing `ValueError`/`KeyError` deliberate-message paths unchanged. Also fixed two **unreported** instances of the identical pattern discovered while reviewing the same functions (fpcalc connectivity check, AcoustID lookup check in `routes_setup.setup_test_acoustid`) plus one dormant/currently-unreachable instance (`routes_setup._check_path`, not wired to any route yet but tested directly and left for future use).
  4. **Incomplete URL substring check** (`py/incomplete-url-substring-sanitization`, `#102`): `"openai.com" in base_url` in `setup_test_ai` matched any URL merely *containing* that substring (e.g. `https://evil.example/openai.com`), which would select the wrong request path shape. Fixed with a real parsed-hostname check (`urlsplit(base_url).hostname == "api.openai.com"` or a proper subdomain suffix match). Low severity in practice -- this only picks a URL path shape, not an access-control decision -- but fixed to a correct pattern regardless.
  5. **Out-of-scope root cause surfaced in an in-scope sink** (`#19`): the actual unbounded `str(exc)` originates in `app.py`'s `_acq_fetch_lidarr_wanted()` broad `except Exception as exc: return [], str(exc)`, which is out of scope for this wave (`app.py` is explicitly excluded). Fixed at the in-scope boundary instead: `routes_lidarr.wanted_lidarr()` now only echoes the string when it is the known-safe `"not configured"` message, otherwise logs it server-side and returns a fixed message. `app.py`'s `_acq_fetch_lidarr_wanted` itself remains untouched and is presumably already counted among that file's 270 remaining `py/stack-trace-exposure`/`py/path-injection` alerts for a future `app.py` wave.
- Fixes: `routes_submissions.py` (`_abs_resolved` containment, `submission_target`/`submission_reference_url`/`validate_musicbrainz_release` exception sanitization); `routes_setup.py` (6 exception-sanitization sites + 1 URL-hostname fix, all in `setup_save_env`/`setup_test_ai`/`setup_test_musicbrainz`/`setup_test_acoustid`/`setup_test_plex`/`setup_regenerate_auth_token`/`_check_path`); `routes_lidarr.py` (`wanted_lidarr` boundary sanitization). `backend/transaction_engine.py`: no code change (all 5 alerts proven false positive).
- Dismissals: 14 total, all `false positive`, each individually justified and backed by a passing behavioral test in `tests/test_sec002_nonapp_backend.py`: `#23`, `#28`, `#30`, `#31`, `#32`, `#33`, `#321` (`routes_setup.py`/`routes_submissions.py` deliberate-message exceptions), `#20`, `#21` (`routes_lidarr._http_error_message`), `#129`, `#130`, `#131`, `#132`, `#133` (`backend/transaction_engine.py` `TransactionStore._path()`).
- Tests added: `tests/test_sec002_nonapp_backend.py`, 19 tests across `TransactionStoreIdContainmentTests`, `SubmissionFolderPathContainmentTests`, `SubmissionExceptionSanitizationTests`, `SetupExceptionSanitizationTests`, `LidarrErrorSanitizationTests`.
- Remaining backlog after this wave: 287 (312 - 25). All remaining alerts are in `app.py` (270), `index.html` (12), and `frontend/src/views/Playlists.tsx` (5).
- Next recommended wave: `app.py` itself, the largest remaining concentration (270 of 287, ~94%). Given this wave confirmed the two dominant repeated shapes (deliberate-safe-message exceptions that are false positives vs. genuinely broad excepts that need sanitizing; unguarded path-resolution helpers vs. already-sanitized ones CodeQL doesn't recognize), a per-rule sweep across `app.py`'s `py/path-injection` and `py/stack-trace-exposure` alerts (the two largest rule groups, ~195 and ~81 respectively before this wave) is likely more efficient than alert-by-alert triage, but `app.py`'s size (~49,500 lines) means this should be split into multiple scoped waves rather than attempted at once. `index.html`/`Playlists.tsx` (frontend, 17 alerts) need a separate frontend-focused wave.
- Priority: P1 (unchanged -- the `_abs_resolved` fix in this wave closed a genuine arbitrary-directory-read primitive; remaining backlog is still high-severity-heavy and concentrated in the customer-facing legacy `app.py`).

### Wave 3: app.py py/stack-trace-exposure (all 62 open alerts)

- Starting main SHA: `c92150a5c2f0275353aba28c5e0c77bcae00cb40` (PR #55 squash).
- Starting total open-alert count (live, paginated, independently re-verified): 287, matching the reported figure exactly. `app.py` `py/stack-trace-exposure`: 62 (also matching exactly).
- Alert numbers: `#37`, `#38`, `#39`, `#324`, `#42` through `#100` (non-contiguous; the full list is `37,38,39,42,43,44,45,46,47,48,49,50,52,53,54,56,57,58,59,60,61,62,63,64,65,66,67,68,69,70,72,73,74,75,76,77,78,79,80,81,82,83,84,85,86,87,88,89,90,91,92,93,94,95,96,97,98,99,100,102(app.py-scoped stack-trace group only)` plus `#324,#325,#326`).
- Root-cause groups:
  1. **Genuinely broad `except Exception`/specific-exception leaking raw text to a public response field** (majority of the 62; ~40 alerts across dozens of routes and helpers): fixed with the established pattern -- log the real exception server-side (`app.logger.warning`, type name only, never the message), return a fixed safe message. Includes several call sites managing the beets `config.yaml` file itself (`/api/config` GET/POST, `/api/config/revert`) -- the single most sensitive file in the container, since it can hold provider credentials, yet previously leaked raw filesystem-operation exception text on any read/write/backup failure.
  2. **Shared helper functions feeding many downstream sinks at once** (`#37` 4 sources / `#43,#44,#45,#46,#48,#56` overlapping sources / `#72-76,78,80` sharing one source): fixing the helper once closed every alert that traced through it. Notable: `_classify_openai_error()`'s fallback branch (`f"AI provider error ({exc})"`, feeding `#324`/`#39` plus 2 already-safe call sites) now returns only the exception type name; `_folder_cleanup_path()`'s except-branch (`f"Invalid path: {exc}"`, feeding 7 alerts across the folder-move/rename/merge maintenance route) now returns a fixed message; `_SpotifyFetchError`'s two raise sites (`#85`/`#86`) used to wrap the underlying urllib/json exception in violation of the "never wrap another exception" rule and now raise fixed messages.
  3. **Deliberately-raised, safe-message exceptions** (`#38`, `#44`, `#61`, `#62`, `#100`): proven false positives. `_start_metadata_apply_transaction()` (`#38`/`#100`, two call sites of the same function), `delete_import_review_folder()`'s `ValueError` branch (`#44`, verified against `_delete_review_source_folder()`'s raise sites), and `_folder_clean_root()` (`#61`/`#62`, after tightening its one path-embedding raise to a fixed string) all raise only fixed, deliberate `ValueError`/`RuntimeError` messages.
  4. **Job-log-only taint, no top-level response field affected** (`#43`, `#46`, `#68`, `#83`; the last with 5 separate `log.append()` sources): **won't fix**, per CLAUDE.md's explicit non-negotiable requirement that jobs expose "raw debug detail." Every one of these confirmed to feed *only* a `"log": log` list in an operation's response, never a top-level `"error"`/similar field, and none are provider/credential-adjacent (all are internal filesystem/DB cleanup operations).
  5. **Existing redaction helper not yet applied at one call site** (source line 1002, part of `#37`): the codebase already has `_redact_security_text()` (used elsewhere for yt-dlp auth-rejection reasons) but a yt-dlp cookie "smoke test" probe error wasn't routed through it. Fixed by applying the existing helper rather than adding a new one.
- Fixes: `app.py` only, ~48 call sites/helper functions across the file (path-cleanup, AI suggestion, Spotify/yt-dlp playlist import, Plex status/playlist-delete, config.yaml management, qBittorrent hardlink repair, transaction settings, library-health/duplicate-resolver/mb-completeness routes, maintenance-report readers, and more). No other files changed except one existing test harness (`tests/test_ai_auth_setup_hardening.py`, updated below).
- Test regression found and fixed mid-wave: `tests/test_ai_auth_setup_hardening.py::ClassifyOpenAiErrorBehaviorTests` used an AST-extraction harness that didn't stub `app` in its exec namespace (the fix added an `app.logger.warning()` call inside `_classify_openai_error`) and explicitly asserted the *old, unsafe* fallback behavior (`assertIn("weird provider response", result)`). Fixed by adding a minimal `app` stub (a `logging.getLogger` shim) to the harness namespace and rewriting the assertion to require the exception type name while explicitly asserting the raw message and an embedded secret are both absent.
- Dismissals: 5 total, all `false positive`, each individually justified and backed by a passing behavioral test in `tests/test_sec002_app_stacktrace.py`: `#38`, `#44`, `#61`, `#62`, `#100`.
- Won't-fix (job-log carve-out, dismissed individually on GitHub as `won't fix` with the CLAUDE.md raw-debug-detail justification -- these are not false positives, they are accepted-by-design and the hard "zero open PR alerts" merge gate requires a real disposition, not just documentation): `#43`, `#46`, `#68`, `#83`. A 5th instance of the identical pattern (`#413`) surfaced after the `delete_import_review_folder` fix itself, once CodeQL re-scanned: fixing that route's "error" field to a static string left its sibling "log" field (same `_delete_review_source_folder` job-log sources) as the sole remaining taint at that exact sink, so CodeQL assigned it a new alert number. Dismissed the same way.
- Tests added: `tests/test_sec002_app_stacktrace.py`, 16 tests across `ClassifyOpenAiErrorSharedHelperTests`, `FolderCleanupPathContainmentTests`, `FolderCleanRootFalsePositiveTests`, `SpotifyFetchErrorSafeMessageTests`, `ConfigYamlExceptionSanitizationTests`, `PlexStatusExceptionSanitizationTests`, `StartMetadataApplyTransactionFalsePositiveTests`, `PlaylistDeleteExceptionSanitizationTests`.
- Remaining backlog after this wave: 225 (287 - 62; all 62 individually accounted for as fixed, false-positive-dismissed, or won't-fix-dismissed -- zero open alerts remain scoped to this wave's PR head). By file: `app.py` `py/path-injection` 184, `py/polynomial-redos` 18, `py/incomplete-url-substring-sanitization` 2 (1 fixed this wave), `py/bad-tag-filter` 1, `py/regex-injection` 1, `py/weak-sensitive-data-hashing` 1, plus `index.html` 12 and `frontend/src/views/Playlists.tsx` 5.
- Next recommended wave: `app.py` `py/path-injection` (184 alerts, the largest remaining single group by far) -- given this wave's finding that CodeQL does not recognize this codebase's established containment helpers (`_path_is_under`, `_folder_cleanup_path`-style resolve-then-validate) as sanitizers, expect a similar high proportion of already-safe-but-unrecognized findings requiring per-alert trace verification rather than code changes, with a smaller number of genuine unguarded-path-helper fixes like `_abs_resolved` in wave 2. `py/polynomial-redos` (18) is a good second target -- narrow, mechanical, and independent of the path-injection triage. Frontend (`index.html`/`Playlists.tsx`, 17 alerts) needs a separate frontend-focused wave.
- Priority: P1 (unchanged).
- Status: Open. See the "Post-merge correction" note above for wave 3's actual final disposition (225 remaining, not 220 -- this line was a stale draft superseded after post-merge cleanup; corrected here to avoid two conflicting counts in the same document).

### Wave 4 (path-injection wave 1): app.py folder-cleanup/rename/merge cluster

- Starting main SHA: `92a7ea62274eb9de3e3a4aafd4e18152e7235109` (PR #56 squash).
- Starting total open-alert count (live, paginated, independently re-verified): 225, matching the reported figure exactly. `app.py` `py/path-injection`: 184 (also matching exactly, all high severity; 176 from the 2026-07-20 baseline, 8 newly numbered on 2026-07-30 from the post-wave-3 rescan).
- Cluster map (summary; full alert-number breakdown kept as local review evidence, not committed): the 184 path-injection alerts cluster around roughly a dozen functional areas by line-number proximity -- import review/folder import (~9100-12500), library duplicate/cleanup scanning (~15000-19800), AI batch/replacement acquisition (~21500-30250), qBittorrent hardlink repair (~32600), **folder-cleanup/rename/merge maintenance (~35800-36280, 32 alerts)**, artist-folder/root-folder repair (~38300-40650), submission/Plex/playlist paths (~41400-47020). Full per-alert line numbers recorded in the PR description.
- Selected cluster: **folder-cleanup/rename/merge maintenance** (`/api/clean/folder-placeholder/*`), alert numbers `256,257,258,259,260,261,262,263,264,265,266,267,268,269,270,271,272,274,275,276,277,280,281,282,283,284,285,417,418,419,420,421` (32 total).
- Selection rationale: matches cluster-selection priority #1 (request-controlled destructive paths: rmdir, rename, shutil.move) exactly -- `remove_empty_source` (rmdir), `safe_rename` (rename), and `merge_source_files` (shutil.move + rmdir) are all directly reachable from a single authenticated POST endpoint with attacker-supplied `source_path`/`target_path`. Internally coherent: every alert in the cluster traces through the same `_folder_cleanup_path()`/`_path_under()`/`MUSIC_ROOT` containment primitives, already deeply understood from wave 3's stack-trace-exposure review of the same functions. Deferred clusters (import review, library cleanup, AI batch, qBittorrent, artist/root-folder repair, submission/Plex/playlist) are read-heavy or already behind narrower request surfaces; none combine destructive mutation with as direct a request-to-sink path.
- Genuine vulnerability found and fixed: `_path_under()`/`Path.relative_to()` treat `MUSIC_ROOT` as "under" itself (`relative_to` returns `.`), so none of the three destructive actions refused the music library root itself as a source. Each is incidentally slowed by a "DB items tracked under source" or "folder is empty" check, but none of those is a reliable, by-design barrier -- a fresh/out-of-sync beets DB, or an emptied root after a merge, could let any of the three delete, rename, or dissolve the entire music library root folder. Fixed with a new `_folder_cleanup_is_approved_root()` helper (reuses `_path_under()`/`MUSIC_ROOT`, no new resolver), checked once at action dispatch in `apply_folder_placeholder_action_api()` for all three destructive actions.
- Second instance of the same gap, plus a narrower one of its own: `apply_safe_folder_placeholder_renames_job()`'s request-supplied `source_paths` branch computed a destination as `src.parent / clean_name` with no containment re-check -- safe for an ordinary subfolder (whose parent stays under `MUSIC_ROOT`), but with no guard against `src` being the root itself (whose parent falls entirely outside `MUSIC_ROOT`). Fixed with the same root refusal plus an explicit source+destination containment check immediately before every `rename()`, applied uniformly to both the request-supplied and background-scan code paths.
- Proven false positives: the remaining 30 of the 32 original alerts. Full review of `_folder_cleanup_path()`, `_folder_cleanup_file_inventory()`, `_folder_cleanup_db_items()`, and `_folder_cleanup_compare_files()` confirms every path reaching a filesystem sink in this cluster is either already resolved-and-validated via `_folder_cleanup_path()`/`_path_under()` before use, or derived from real filesystem enumeration (`rglob()` + `relative_to()`) that cannot itself reintroduce a traversal segment (a real filesystem walk can never yield a discoverable child literally named `..`). CodeQL does not model this codebase's containment helpers as sanitizers (the same non-recognition pattern confirmed across every prior remediation wave), so it continues to flag the `resolve()`/`rglob()`/`rename()`/`shutil.move()` call sites themselves regardless of the validation that precedes them.
- Architecture boundary decisions: none required. This entire cluster operates on `web-manager-owned state` under the app's own `MUSIC_ROOT` configuration value pointing at the shared Beets media volume -- it does not call into `BeetsClient`/the control agent, and no new engine-side operation was needed or added.
- Equivalent unreported flows: none found. The `rg` sweep across this line range (`open(|Path(|...|rename|...`) surfaced no filesystem sink outside the functions already reviewed above.
- Tests added: `tests/test_sec002_app_path_folder_cleanup.py`, 12 tests across `FolderCleanupPathContainmentTests`, `ApprovedRootSelfRefusalTests`, `SafeRenamesJobRootAndDestinationContainmentTests`. 7 of the 12 tests independently verified to fail against the pre-fix commit (4 real assertion failures, 3 `AttributeError` since the new helper didn't exist), confirming they exercise the actual vulnerability rather than merely the new helper's existence.
- Remaining backlog after this wave: 193 total (225 - 32; all 32 accounted for as fixed or proven false positive, zero dismissals needed since no alert met the strict false-positive dismissal bar independently of the code fix -- the root-cause fix is what makes the remaining 30 alerts' non-recognition classification provably correct, not a standalone dismissal). By file: `app.py` `py/path-injection` 152, `py/polynomial-redos` 18, `py/incomplete-url-substring-sanitization` 3, `py/bad-tag-filter` 1, `py/regex-injection` 1, `py/weak-sensitive-data-hashing` 1, plus `index.html` 12 and `frontend/src/views/Playlists.tsx` 5.
- Recommended Wave 2 (path-injection) cluster: the import-review/folder-import area (~9100-12500), the next-largest destructive-adjacent cluster (delete/move operations on review-queue source folders), followed by library duplicate/cleanup scanning and AI-batch/acquisition areas.
- Priority: P1 (unchanged -- this wave closed a genuine root-folder-destruction vulnerability).
- Status: Open. PR #52, wave 2, wave 3, and wave 4 (this folder-cleanup cluster) are closed; 193 alerts remain, requiring further dedicated waves.

### Wave 5 (path-injection wave 2): app.py import-review source deletion and per-file cleanup

- Starting main SHA: `cfc95ede8ebabebac0241897bdaaf29c786a779a` (PR #57 squash). Live paginated `main` inventory on this SHA returned 225 total open alerts and 184 `app.py` `py/path-injection` alerts, all high severity. The 32 Wave 1 alert numbers (`256,257,258,259,260,261,262,263,264,265,266,267,268,269,270,271,272,274,275,276,277,280,281,282,283,284,285,417,418,419,420,421`) are still listed open on `main`; excluding that completed cluster leaves the previously reported effective backlog of 193 total and 152 `app.py` path-injection alerts for wave selection.
- Remaining cluster-map summary (excluding Wave 1): folder import/preflight reads (`135-138`); metadata/art cache writes/deletes (`139-149`); shared browse/import path helpers and disk-art serving (`150,152-155`); import-review delete, stats, and per-file cleanup (`156-181`); album-art save/upload (`182-184`); library duplicate/import target scanning (`185-217`); AI batch, acquisition, and replacement staging (`219-252,414-416`); qBittorrent hardlink repair (`253-254`); artist/root-folder repair (`287-296`); playlist, Plex, and staging manifests (`297-320`).
- Selected cluster: **import-review source deletion and per-file cleanup**, alert numbers `156,157,158,159,160,161,162,163,164,165,166,167,168,169,170,171,172,173,174,175,176,177,178,179,180,181` (26 total), spanning `_pending_review_path_key`, `_library_no_mb_album_matches_folder`, `_delete_library_db_rows_under_folder`, `_delete_review_source_folder`, `/api/import/folder-stats`, `_remaining_audio_files`, `_import_review_cleanup_destination`, `_unique_import_review_cleanup_path`, `_prune_empty_review_dirs`, and `/api/import/review-files/cleanup`.
- Selection rationale: highest coherent remaining risk after Wave 1. This route family combines authenticated request-controlled and stored Pending Review paths with recursive scans and destructive sinks (`unlink`, `shutil.move`, `rmdir`, `shutil.rmtree`). The download/temp folder delete path previously accepted any configured cleanup-root subfolder without proving it was the Pending Review source, while per-file cleanup trusted the stored Pending Review folder itself at the filesystem boundary.
- Genuine fixes expected to close after rescan: download/temp source-folder deletion now requires Pending Review membership before `shutil.rmtree`; per-file cleanup now refuses music-library folders, approved roots themselves, outside-root stored paths, symlink folder escapes, symlink parent components, and symlink file escapes before `unlink` or `shutil.move`; folder-stats recursive scanning is now fenced to the configured review roots and music root and rejects root-self scans.
- Defense-in-depth fixes: import-review path text is validated through bounded URL-decoding while preserving literal percent filenames; traversal, dot segments, encoded separators, encoded NUL, decoded absolute paths, Windows drive/UNC/backslash attacks in container-style inputs, and double slash forms are rejected before path construction reaches the sink; quarantine destinations are canonicalized under `IMPORT_REVIEW_QUARANTINE_DIR`, checked for symlink components, and rechecked after uniqueness selection and directory creation so symlinked or replaced destination parents cannot redirect a move outside the quarantine root.
- Equivalent unreported flows fixed: the unreported `/api/import/folder-stats` arbitrary recursive scan and the symlinked quarantine destination-parent escape and destination-parent replacement race in `/api/import/review-files/cleanup` were fixed with the same import-review path contract. No Beets engine-side operation was added; this wave keeps direct destructive cleanup limited to provider download/temp staging and blocks per-file cleanup from mutating Beets/media-owned library paths.
- Proposed false-positive dismissals for Claude: final PR-head CodeQL after the code fixes reported 12 open `py/path-injection` alerts in the new import-review helper chain (`#424`, `#425`, `#426`, `#428`, `#429`, `#430`, `#431`, `#432`, `#433`, `#434`, `#435`, `#436`). Codex trace review classifies these as probable sanitizer-model false positives, not dismissed: CodeQL does not model the bounded path-text validator, fixed-root lexical containment, symlink-component rejection, canonical containment, trusted return values, or destination-parent revalidation as taint barriers. Behavioral coverage now proves literal-percent compatibility, outside/root-self rejection, symlink file and directory escape rejection, symlinked destination-parent rejection, destination-parent replacement after initial validation, and outside sentinels unchanged. Claude must independently decide whether to dismiss them.
- Tests added: `tests/test_sec002_app_path_import_review_cleanup.py` covers real Flask route requests, real temp roots, real files, literal percent filenames, stored hostile Pending Review paths, root-self refusal, encoded traversal, encoded NUL, raw-relative decoded-absolute input, symlink file and directory escapes, symlinked quarantine destination parents, destination-parent replacement after initial validation, and outside-root sentinel preservation.
- Remaining backlog estimate after rescan: if CodeQL recognizes all 26 selected alerts as fixed, effective backlog becomes 167 total and 126 `app.py` path-injection alerts, while the live count may still include Wave 1 until those already-completed alerts are reconciled. Status remains open; no alerts were dismissed by Codex.
- Recommended Wave 3 (path-injection) cluster: artist-folder/root-folder repair (`287-296`) because it is the next destructive media-owned mutation cluster and likely needs an architecture-boundary decision around Beets/control-agent ownership. qBittorrent hardlink repair (`253-254`) is a smaller high-risk candidate if Claude wants a narrower pre-wave.
- Priority: P1 (unchanged).
- Status: Open. This wave is code-fixed in the draft PR; ordinary CI is passing, aggregate CodeQL is blocked on the 12 proposed sanitizer-model false positives above, and Claude must make the dismissal/final-review decision.

#### Claude final independent review (PR #58)

- Independently re-verified, live and paginated, on `main` at `cfc95ede8ebabebac0241897bdaaf29c786a779a`: 225 total open alerts, 184 `app.py` `py/path-injection`, matching Codex's figures exactly. Also independently confirmed the Wave 1 (32-alert) discrepancy Codex flagged: those 32 alert numbers (`256-285` non-contiguous, `417-421`) are still individually `state=open, dismissed_reason=null` on `main` despite the Wave 1 fix having been live since PR #57's merge -- CodeQL's non-recognition of this codebase's containment helpers persists across rescans, it does not self-resolve. Per Section 30/Section 4 of this review's instructions, the "effective backlog" framing in the Wave 5 entry above is explicitly **not** used as a final count anywhere in this correction; all counts below are the live paginated figures.
- Reviewed the complete diff independently (app.py, the new test file, package-lock.json, TECHNICAL_DEBT.md) against the surrounding helper chain, request transport, and every one of the 12 PR-head alert traces (`#424-#436` minus `#427`). Concurred with Codex's classification that all 12 are CodeQL sanitizer-model non-recognition, not genuine vulnerabilities -- every flagged sink (`is_symlink()`, `resolve()`, `exists()`/`is_dir()`) operates on a value that already passed lexical containment against a trusted, non-request-controlled root (`DOWNLOADS_ROOT`/`_DOWNLOADS_ROOTS`/`MUSIC_ROOT`/`IMPORT_REVIEW_QUARANTINE_DIR`, the last read once from `os.environ` at request time, never from request data) and is canonically re-validated via `_path_is_under()` immediately after.
- Independent adversarial testing (not merely trace review) found one genuine gap Codex's review had not proven safe: the TOCTOU window between the final source revalidation (`resolved.resolve()` + `_path_is_under()` recheck) and the destructive `shutil.move()` call was wider than necessary, since destination-side work (parent resolution, symlink-component checks, `mkdir`) happened in between with no final recheck on the source. A constructed race test (source swapped for a symlink to an outside sentinel file at the exact instant `shutil.move()`/`os.rename()` would fire) additionally proved that `shutil.move()`'s destination-is-a-directory auto-descend behavior (`os.path.isdir(dst)` before deciding rename-vs-copy) would follow an attacker-planted destination-leaf directory symlink and write the quarantined file *inside* the attacker's target directory -- a real, previously-untested escape, independent of any of the 12 CodeQL alerts.
- Fixed both: (1) added a final `resolved.is_symlink()` check immediately before both the `unlink()` and the (now) `os.rename()` call, closing the bulk of the source-side race window; (2) replaced `shutil.move()` with `os.rename()` (falling back to `shutil.copyfile()`/`copystat()`+`unlink()` only on cross-device `errno.EXDEV`), neither of which auto-descends into an existing directory target the way `shutil.move()`/`shutil.copy2()` do, plus an explicit `dest.exists() or dest.is_symlink()` pre-check and a post-move `dest.is_symlink()` verification that undoes (`unlink()`s) and reports `skipped` rather than falsely reporting success if the source was swapped for a symlink at the exact rename instant. The residual race (an attacker who already has concurrent local write access to the downloads volume, winning a kernel-syscall-width window) is accepted and documented, consistent with the TOCTOU doctrine established in prior waves -- no portable application-level code can close a true kernel-level race, and an attacker with that capability already has direct filesystem access equal to or greater than what this endpoint would grant.
- Verified the `/api/import/folder-stats` response-contract change (200 OK with `exists:false` -> 400 with an error for any invalid/nonexistent/outside-root path) against the single frontend caller (`ImportReviewPage.tsx::requestDeleteFolder`): it already treats a thrown/non-2xx `getFolderStats()` response as best-effort (`catch { /* show dialog without stats */ }`), so the change is compatible with no frontend edit required.
- Verified the `frontend/package-lock.json` change: `npm ci` succeeds cleanly with 0 vulnerabilities and a clean working tree afterward; `npm ls brace-expansion` resolves to `5.0.9` (via `eslint -> minimatch`), matching the advisory fix; no unrelated version drift.
- Added 6 new tests to `tests/test_sec002_app_path_import_review_cleanup.py` (19 total, up from 13): a query-string encoded-traversal test against `/api/import/folder-stats` itself (not just the cleanup route), a deep-encoding test (4/5/6 percent-encoding layers, proving payloads beyond the 3-additional-decode budget fail closed because no downstream consumer decodes further), a source-replaced-by-symlink-before-`unlink()` race test, a source-replaced-by-symlink-before-move race test, and a destination-leaf-replaced-by-directory-symlink race test (the one that caught the `shutil.move()` escape above). All 19 pass; the two race tests that initially failed against the pre-fix code are what drove the two fixes in this correction.
- Live Linux-container adversarial validation (disposable, no production mounts) independently confirmed on the actual runtime platform: drive-letter/UNC/double-slash/relative-path rejection, single/double-encoded traversal rejection, real symlink-file escape rejected while a literal-percent filename (`100%25 legit.flac`) and a normal file were correctly quarantined with names preserved verbatim, the outside sentinel file's content was untouched and no symlink ever appeared inside the quarantine directory, and all three `delete-folder` authorization gates (not-pending, approved-root-itself, music-library-root-itself) held.
- Alert disposition (12 of 12, individually justified, see GitHub dismissal comments): dismissed as `false positive` -- `#424`, `#425`, `#426`, `#428`, `#429`, `#430`, `#431`, `#432`, `#433`, `#434`, `#435`, `#436`. Each dismissal comment names the specific runtime barrier at that exact sink (prior lexical containment against a trusted root, or canonical `_path_is_under()` revalidation immediately following).
- Wave 1's 32 still-open alerts (`256-285` non-contiguous, `417-421`) are **not** dismissed by this review -- they are out of scope for PR #58 (a different code cluster, already merged and independently re-verified safe in Wave 4's own review). They are noted here only to keep the live-count reconciliation accurate; a future maintenance pass should dismiss them individually against the Wave 4 fix, the same way this review dismissed Wave 5/PR #58's 12.
- Remaining backlog (live, paginated, post-merge -- see Section 41/42 of the final report for the exact post-merge figures): unchanged in shape from Wave 5's estimate once Wave 1's 32 and this wave's 12 are accounted for; the true `app.py` `py/path-injection` figure will not drop until CodeQL's own rescan re-numbers or closes the original `#156-#181` alerts, which -- per the established pattern -- may require a further individual dismissal pass after the post-merge rescan rather than an automatic closure.
- Priority: P1 (unchanged).
- Status: Open (SEC-002 overall). This wave (PR #58) is merged; see the PR's final report for exact merge SHA and post-merge verification.

### Wave 6 (path-injection wave 3): app.py artist-folder/root-folder repair -- Codex implementation, pending Claude final review

- Starting main SHA: `170650c8432dd22b62dfb8d20f8d17c50e1db123` (PR #58 squash). Live paginated `main` inventory on this SHA: 216 total open alerts, 175 `app.py` `py/path-injection`, all high severity, matching the reported figures exactly. Latest Python CodeQL analysis is on this exact SHA.
- Known fixed-but-open prior-wave alerts (not subtracted from the live totals above): Wave 1's 32 (`256-285` non-contiguous, `417-421`) remain open; of Wave 2/5's 26 original (`#156-#181`), 9 auto-closed as `fixed` on the post-merge rescan and 17 remain open. Both sets are pre-existing debt, out of scope for this wave.
- Wave 3 original alert inventory (confirmed live, unchanged, one coherent cluster): `#287` (app.py:38582), `#288`/`#289` (38769), `#290`/`#291` (38811), `#292` (38984), `#293` (39179), `#294` (39195), `#295`/`#296` (39335) -- 10 alerts total, all `py/path-injection`, all still open, all tracing to the same root cause.
- Functions/routes: `_apply_artist_folder_groups()`, `_scan_artist_folder_groups()`, `_stamp_artist_folder_scan()`, `_stamp_artist_folder_album_mbid_counts()`, `_auto_merge_case_duplicate_artist_folder()`, `POST /api/clean/artist-folders/scan`, `POST /api/clean/artist-folders/merge`, `POST /api/clean/artist-folders/stamp-mbid`.
- Genuine vulnerability found and fixed: all three routes accepted a `root` request value (defaulting to `MUSIC_ROOT` but freely overridable -- the frontend exposes a free-text root field) with only an `exists()`/`is_dir()` check, no containment against `MUSIC_ROOT` at all. `_apply_artist_folder_groups()` and the `_stamp_artist_folder_*` chain then perform destructive filesystem operations (`shutil.move`, `rmdir`-equivalent removal, `Path.rename`) and direct Beets-DB path rewrites (via the already-architecture-compliant `RemoteSQLiteConnection`) driven entirely by that unrestricted root. An authenticated caller could point `root` at any directory on the container filesystem reachable from the web-manager process and have "duplicate artist folder" merge logic move, merge, or remove files there. Fixed with a new `_artist_folder_repair_root()` helper (reuses the wave 2 `_import_review_path_text_error`/`_path_lexically_under`/`_path_has_symlink_component_under`/`_path_is_under` helpers -- no new resolver), applied at all three route entry points and defensively inside `_apply_artist_folder_groups()` itself (the function's own alert-#287 sink). `root` must resolve to `MUSIC_ROOT` itself or a real subdirectory of it; internal trusted callers (`_auto_merge_case_duplicate_artist_folder`, always invoked with `MUSIC_ROOT` directly) are unaffected since they already satisfy the new constraint.
- Defense-in-depth / matching-policy fix (found during review, not itself a CodeQL alert): `_apply_artist_folder_groups()`'s core "duplicate artist folder" grouping (`_scan_artist_folder_groups()`) groups folders by normalized-name equality alone -- the `use_musicbrainz` flag only affects which name/casing is chosen as canonical, never whether folders are considered duplicates. This meant the merge path could commingle two different artists' catalogs purely because their folder names happened to normalize the same way (case/punctuation variants), violating the project's non-negotiable "do not merge on fuzzy name matching alone" rule -- and inconsistent with the sibling `stamp-mbid` feature's plain-name-duplicate path, which already AcoustID-fingerprint-verifies before merging. Fixed by requiring the same `_artist_folder_fingerprint_confirms()` check (reused, not duplicated) before merging any group with no MusicBrainz artist ID evidence; a confirmed mismatch skips that source folder instead of merging it. Groups with real MB Artist ID evidence are unaffected and skip the (network-dependent) fingerprint check entirely.
- Architecture boundary decision: none required, consistent with Wave 1's precedent for the folder-cleanup cluster. The direct filesystem mutation and Beets-DB writes in this cluster are the same class of residual `app.py` direct-mutation debt already tracked as ARCH-003 in `docs/ARCHITECTURE.md`; the DB access itself already goes through `backend.beets_client.RemoteSQLiteConnection` (confirmed by reading `_db()`'s definition), not a local SQLite handle, so no architecture-compliance regression exists. No new Beets/control-agent engine capability was added or judged necessary for this wave.
- Equivalent unreported flows: none found beyond the two fixes above. Every filesystem sink in `_merge_artist_dir_contents()`, `_replace_stamp_db_path_prefixes()`/`_replace_stamp_db_exact_paths()`, and the candidate-construction code in `_stamp_artist_folder_scan()` operates on values structurally guaranteed to be direct children of (or renamed variants within) the now-validated `root_path`, by construction of `_scan_artist_folder_groups()`'s and `_stamp_artist_folder_scan()`'s own iteration logic (`root_path.iterdir()`), so no additional containment gap was found. `/api/clean/album-folders/apply-issue` (a neighboring but functionally distinct route) was reviewed only enough to confirm it is a separate cluster, not touched here.
- Proposed false-positive dismissals: superseded by Claude's final review below (the root contract change renumbered these to `#454`-`#456`; all three dismissed after independent verification -- see the Claude final review subsection for the exact justification).
- Tests added: `tests/test_sec002_app_path_artist_root_repair.py`, 18 tests across `ArtistFolderRepairRootBoundaryTests` (outside-root/prefix-collision/symlink/encoded-traversal/Windows-UNC rejection and music-root/subfolder acceptance, via real Flask requests and a synchronous job-body run proving the background job captures the validated root, not raw request text), `ArtistFolderRepairRootHelperTests` (literal-percent preservation, deep-encoding, NUL rejection), and `ArtistFolderMergeIdentityTests` (fingerprint-mismatch blocks a name-only merge, fingerprint-confirm allows it, MB-ID-evidenced groups skip the fingerprint check entirely). 11 of 18 tests independently verified to fail (8 failures + 3 errors) against the pre-fix commit via `git stash`/`git stash pop`, confirming they exercise the actual vulnerabilities.
- Remaining live-count estimate: not claimed here -- see the draft PR's CodeQL results for the exact live count on the pushed head; per the task's own live-inventory discipline, no reduced "effective" total is reported.
- Recommended Wave 4 cluster: album-folder relocation/duplicate-cleanup (`/api/clean/album-folders/*`) and the remaining library duplicate/import-target scanning area, both still-open path-injection concentrations noted in Wave 5's cluster map.
- Priority: P1 (unchanged -- this wave closes a genuine unrestricted-root arbitrary-directory read/write/delete primitive reachable from three authenticated destructive routes).
- Status: Open (SEC-002 overall). This wave is implemented and pushed as a draft PR by Codex; Claude has not yet performed final independent review, alert disposition, or merge decision.

#### Claude final independent review (PR #59)

- Independently re-verified, live and paginated, on `main` at `170650c8432dd22b62dfb8d20f8d17c50e1db123`: 216 total open, 175 `app.py` `py/path-injection`, matching Codex's figures exactly. Independently re-traced `#287`/`#292`/`#293` via the alert-instances API against this exact SHA and confirmed the same root cause and line numbers Codex reported.
- **Genuine defect found and fixed -- accepted-root contract was too permissive.** Codex's `_artist_folder_repair_root()` accepted `MUSIC_ROOT` *or any real descendant of it*. The frontend labels this field "Library root" (`ArtistFoldersPanel.tsx`) with no supported concept of scanning an individual artist or album folder -- there is only one `MUSIC_ROOT`, no multi-section library concept anywhere in the codebase. Accepting a descendant (e.g. an artist folder) as `root` would make `_scan_artist_folder_groups()` misinterpret that folder's own children (album folders) as "duplicate artist folders," destructively merging two unrelated albums together and rewriting the Beets DB's `albumartist` column with an album folder's name. Fixed by requiring `root` to resolve to *exactly* `MUSIC_ROOT`, not a descendant. Also made `MUSIC_ROOT.resolve()` failure fail closed (reject) instead of silently falling back to an unresolved comparison basis, per the review's explicit instruction.
- **Genuine defect found and fixed -- fingerprint tri-state fail-open bug.** `_artist_folder_fingerprint_confirms()` returns a real tri-state `Optional[bool]`: `True` (confirmed match), `False` (confirmed mismatch), or `None` (no usable evidence -- empty folder, fpcalc/AcoustID unreachable, provider error, or no candidate had a usable artist field). Both Codex's new check in `_apply_artist_folder_groups()` and the **pre-existing** (not introduced this wave) plain-name-duplicate check in `_stamp_artist_folder_scan()` only blocked on `fp_result is False`, silently letting `None` fall through and authorize the merge -- i.e. whenever the fingerprint service simply failed to produce a result, the safety gate did nothing. Fixed both call sites to require `fp_result is True` explicitly; both `False` and `None` now block and log a distinct reason. This is the single most safety-critical finding in this review: the previous code's *entire point* was to prevent silently commingling two different artists' catalogs, and an unavailable AcoustID/fpcalc service (a very plausible real-world condition) defeated it completely.
- **Genuine defect found and fixed -- weak MB Artist ID evidence bypassed fingerprint verification.** `_apply_artist_folder_groups()` skipped the fingerprint check entirely whenever the group had any non-empty `mb_artistid`. That ID is derived from `_mb_canonical_for_artist_entries()`, which performs a MusicBrainz *text search on each folder's name* -- not per-folder audio-content evidence. Two different real artists who happen to share a folder name would produce the same (or a similarly ambiguous) name-search result, so this "evidence" is vulnerable to exactly the collision scenario the fingerprint check exists to catch. Fixed by requiring fingerprint confirmation (`is True`) for every source unconditionally, regardless of `mb_artistid` presence; the ID is still used for canonical-name selection and DB tagging (a separate, lower-severity, pre-existing concern noted below), just no longer trusted to authorize the file merge itself.
- **Pre-existing bug found and fixed while reviewing the above (not itself a CodeQL alert or a Wave 3 regression):** `_stamp_artist_folder_scan()`'s plain-name-duplicate skip branch added the skipped folder's own path to `candidate_paths`, a set whose sole purpose elsewhere in the function is tracking paths that became *real candidates* (used to drop now-stale `skipped` entries from earlier loops). Adding a skip-only path there caused the function's own end-of-scan filter to erroneously discard that exact skip entry, silently hiding the fingerprint-mismatch/unavailable reason from the operator -- directly undermining the visibility the fix above depends on for "leave for review" to mean anything. Fixed by removing that erroneous line; confirmed via a direct unit test that the skip reason now survives to the response.
- MB Artist ID evidence quality (task's "album identity"/"MB Artist ID Evidence Decision" sections): given fingerprint verification is now unconditional for every merge, `mb_artistid` consistency across sources is no longer the operative safety gate for the file-merge decision. It remains used only for canonical-name choice and DB `mb_albumartistid` tagging on the canonical entry's *existing* rows (a pre-existing behavior, not part of Wave 3's diff) -- writing a name-search-derived MBID onto already-imported catalog metadata independent of whether any merge actually occurred. This is a real, narrower, lower-severity data-quality concern (mistagging, not destructive file mutation) that was consciously **not** fixed in this PR to avoid scope creep into `_mb_canonical_for_artist_entries()`'s broader design; recorded here as a specific, actionable follow-up rather than left implicit.
- Architecture boundary decision (independently re-examined, not accepted from Codex's "Wave 1 precedent" framing alone): **Option A, narrowly secured legacy flow**, for the reviewed cluster's actual filesystem/DB operations -- DB writes already go through `RemoteSQLiteConnection` (verified by reading `_db()`), and direct filesystem mutation is the same already-tracked ARCH-003 debt Wave 1 accepted for an equivalent cluster. However, independent review found a **more specific, previously-undocumented** architecture violation in the same function: `_apply_artist_folder_groups()`'s post-merge tag-write step calls `subprocess.run([BEET_BIN, "-c", cfg, "write", ...])` -- a **local subprocess shell-out**, not `backend.beets_client.BeetsClient.run_command()` (an existing remote-execution method already used elsewhere for exactly this purpose). Per `docs/ARCHITECTURE.md`'s own text, Beets command arrays "must not shell out in the web-manager container." Since the web-manager image ships zero Beets binaries, this call fails every time in the actual deployed topology (caught by a broad `except Exception`, logged as a `WARN`), meaning the "write tags for N albums" step is a silent no-op in production today. `BEET_BIN`/local `subprocess.run` appears at ~47 sites across `app.py`, not unique to this cluster -- fixing it here would mean auditing and testing dozens of unrelated call sites, far outside a path-injection wave's narrow scope. **Not fixed in this PR; documented instead** as a new, more specific, actionable architecture item (broken functionality, not just style debt) recommended for its own dedicated wave -- see Recommended Wave 4 below. This does not block the path-injection/identity fixes in this PR, which are independent of whether the tag-write step itself functions.
- Test quality: added 5 new tests (23 total in the wave-3 file, up from 18) covering the corrected root contract (`test_scan_rejects_subfolder_of_music_root`, `test_subfolder_of_music_root_is_rejected`, literal-percent re-targeted at `MUSIC_ROOT` itself), the fingerprint fail-open fix (`test_name_only_group_blocked_when_fingerprint_unavailable`, `test_group_with_musicbrainz_artist_id_blocked_on_fingerprint_mismatch`, `test_group_with_musicbrainz_artist_id_still_requires_fingerprint_confirmation`), and a new `StampMbidPlainNameDuplicateFingerprintTests` class covering the pre-existing `_stamp_artist_folder_scan()` path's identical tri-state contract and the `candidate_paths` visibility bug. 6 of the (now) 23 tests independently verified to fail against Codex's pre-review head via `git stash`/`git stash pop`. Also corrected a pre-existing, unrelated source-text test (`tests/test_dedup_acoustid_fingerprint.py::test_artist_folder_name_merge_fingerprint_verified`) whose fixed-offset extraction window no longer reached the relevant code after this review's edits added an explanatory comment; widened the window and strengthened its assertion to check for `if fp_result is not True:` explicitly.
- Final reviewed head: `f2442f4d5ab115aea4d4ee0e024d021a162a35b4`. PR-head CodeQL after the review's corrections reported 3 open `py/path-injection` alerts (renumbered from the original `#452`-`#454` to `#454`-`#456` by the root-contract fix): `#454` (app.py:38609, `candidate.resolve(strict=False)`), `#455`/`#456` (app.py:38606, `candidate.exists()`/`candidate.is_dir()`). All three sit after an exact-equality proof (`candidate != music_root -> reject`, two-to-three lines above each sink) that `candidate` literally equals the trusted, canonically-resolved `MUSIC_ROOT` constant -- a strictly stronger barrier than the original wave's lexical-containment check, and still not recognized by CodeQL's taint model. All three individually dismissed as `false positive` with sink-specific justifications. 0 open PR alerts; aggregate CodeQL check green.
- Priority: P1 (unchanged).
- Status: Open (SEC-002 overall). Reviewed, corrected, and merged by Claude -- see the final report for exact squash SHA and post-merge verification.

### Path-Injection Wave 4: album-folder relocation, duplicate cleanup, and import-target boundaries -- Codex implementation, pending Claude final review

- Starting main SHA: `15e1068a1fc1770684205f3c72fffe2f928572ea` (PR #59 squash). Live paginated `main` inventory on this SHA: 209 total open alerts, 192 `app.py` alerts, 168 `app.py` `py/path-injection`, all high severity. Latest Python CodeQL analysis: `1567867616` on commit `15e1068a1fc1770684205f3c72fffe2f928572ea`.
- Fixed-but-open prior-wave accounting, not subtracted from the live totals: Wave 1's 32 (`#256`-`#272`, `#274`-`#277`, `#280`-`#285`, `#417`-`#421`); Wave 2's 17 still-open original alerts (`#156`-`#163`, `#166`-`#170`, `#174`-`#176`, `#178`); Wave 3's 3 still-open reviewed false positives (`#292`-`#294`).
- Current cluster map summary: album art save/upload (`#182`-`#184`, deferred); Import Review album target/candidate scanning and apply selection (`#192`-`#217`, selected); AI/library suggestion scans (`#185`-`#191`, deferred); album-folder cleanup under `/api/clean/album-folders/*` (no direct live CodeQL alert on the route, but equivalent stored-path destructive album relocation/duplicate-cleanup flow, selected as connected hardening); remaining unrelated acquisition, playlist, and maintenance path clusters deferred.
- Selected original alerts: `#192`-`#217` (26 live `app.py` `py/path-injection` alerts). Routes/helpers include `/api/candidates/<mb_albumid>/tracks`, `/api/folders/import-target-preview`, `/api/folders/import-with-id`, `/api/import/review-files/cleanup`, `/api/import-review/auto-enqueue`, `/api/import-review/auto-enqueue/reconcile`, `/api/import/reconcile-job`, `_candidate_track_local_candidates()`, `_target_preview_source_files()`, `_import_review_selected_source_files()`, `_remaining_audio_files()`, `_review_paths_equal()`, and `_normalised_submission_files()`.
- Root causes fixed: request and stored source paths reached `Path()`, `stat()`, `rglob()`, `exists()`, selected-file staging, and path-equality sinks without a single trusted returned path contract; album cleanup apply trusted stored scan issue paths when moving/quarantining files and pruning folders. Added a shared Import Review source resolver that preserves literal-percent POSIX filenames while rejecting traversal, encoded traversal, encoded NUL/slash/backslash, backslash/UNC/drive-style container paths, approved roots, outside roots, symlink components, symlink leaves, missing sources, and wrong expected types. Stored DB paths are re-rooted under `MUSIC_ROOT` only after validation and are treated as untrusted at every filesystem boundary.
- Release Group identity policy: album cleanup apply now rechecks that the selected issue carries a valid MusicBrainz Release Group ID immediately before mutation. Missing RGID, blocked status, or conflict-tagged issue types block instead of moving files. Confirmed `Needs review` issues with a valid RGID remain supported for the existing manual apply contract; name-only, Release ID-only, AI, and fuzzy evidence are not sufficient.
- Track/file collision policy: import-target preview and selected-file import only operate on canonical source files under the approved source root; nested symlink files are ignored. Album cleanup never silently overwrites a target file; an introduced destination collision or unverifiable/different same-name file blocks. Verified duplicate quarantine rechecks the source/target file identity immediately before moving to trash.
- Path/TOCTOU corrections: album cleanup validates canonical source/target paths in planning and again in apply, rejects root-self, outside-root, symlink source/destination/parent components, symlinked trash parents, unsafe destination parents, and destination paths outside the approved canonical album folder. Source files, target files, and duplicate targets are revalidated at sink time; DB update failure is now reported as failure instead of being counted as success.
- Architecture decision: added one narrow engine-side operation, `POST /library/rewrite-path`, exposed through `BeetsClient.rewrite_library_path()`, so album cleanup updates Beets-owned item/art paths through the control-agent lock instead of raw SQL or local SQLite. The operation accepts only old/new music-library paths, rejects root-self/outside/symlink-component inputs, revalidates under the engine lock, stores the canonical relative destination path, and returns structured row counts. The legacy direct filesystem move portion remains ARCH-003 debt but is now bounded and failure-truthful. The selected cluster does not introduce a new local `BEET_BIN` call; the broader approximately 47-site `BEET_BIN`/local-subprocess architecture issue remains separate debt and was not migrated here.
- Tests added/updated: new `tests/test_sec002_app_path_album_folder_repair.py` covers real temp roots, actual Flask requests, synchronous album-cleanup job execution, real files, symlinks where available, hostile stored paths, outside-root sentinels, literal-percent filenames, and SQLite path update assertions, exact BeetsClient path-rewrite calls, and direct control-agent path-rewrite coverage. `tests/test_album_folder_cleanup.py` loader was updated so its AST-isolated helper tests include the new trusted-path and identity helper chain.
- Expected CodeQL disposition: selected original alerts are fixed in code or expected to become sanitizer-model false positives on the PR head; Codex did not dismiss any alerts. Any remaining PR-head alerts require Claude's independent trace review and individual dismissal decision where justified.
- Remaining live-count estimate before PR-head CodeQL: live `main` remains 209 total open and 168 `app.py` path-injection alerts. This wave is expected to address 26 selected original alerts plus equivalent unreported album-cleanup stored-path flows; exact remaining count is authoritative only after PR-head CodeQL and/or a post-merge `main` rescan.
- Recommended next wave: album-art save/upload (`#182`-`#184`) or the deferred AI/library duplicate scanning region (`#185`-`#191`), unless PR-head CodeQL shows a missed genuine issue in this Wave 4 cluster.
- Priority: P1 (unchanged).
- Status: Open (SEC-002 overall). Codex implementation is in progress; Claude has not performed final independent review, alert disposition, or merge decision.

#### Claude final independent review (PR #60)

- Independently re-verified live paginated `main` at `15e1068a1fc1770684205f3c72fffe2f928572ea`: 209 total open, 168 `app.py` `py/path-injection`, matching exactly. Independently re-traced all 26 original alerts (`#192`-`#217`) to the exact reported functions/lines.
- Issue provenance (task's central concern): independently confirmed `/api/clean/album-folders/apply-issue` does **not** trust a client-supplied issue payload -- it takes only `issue_id`, runs a fresh server-side rescan (`_album_folder_cleanup_plan`), and looks the issue up by ID from that fresh scan's own results. The ID (`_album_cleanup_issue_id`) is a SHA1 hash of the sorted source folder paths, canonical path, and issue types, so any relevant filesystem change between review and apply changes the hash and the apply is refused ("issue not found") rather than silently applying stale/different content. This is a sound, already-safe stale-plan defense; the task's provenance concern does not require a fix here.
- File collision identity: independently confirmed `_album_cleanup_verified_same_file()` requires a real SHA1 content hash match (`_album_cleanup_file_hash()`, streamed full-file read), not filename/size/duration/tags. This is genuine byte-identity proof; no fix needed.
- **Genuine defect found and fixed -- fingerprint-tri-state-class bug did not recur here, but a real filesystem/database consistency bug did.** `_album_cleanup_apply_issue()`'s move-then-DB-rewrite sequence: when the file move succeeded but the subsequent `beets_client.rewrite_library_path()` call failed, the code raised a truthful error but left the file stranded at the new location while the Beets DB still pointed at the old path -- exactly the split-state the review brief warned a "truthful error message alone" does not repair. Fixed by moving the file back to its original location before re-raising when the DB rewrite fails.
- **Genuine defect found and fixed -- new `/library/rewrite-path` engine endpoint silently reported success on zero-row and swallowed a partially-committed change.** (a) The endpoint computed `items_changed`/`artpaths_changed` but never checked them -- an `old_path` that matched no stored row (e.g. stale/incorrect) still returned `ok: true` with an empty change count, giving the caller no signal that nothing actually happened. Fixed: zero total rows changed now rolls back and returns 404. (b) The two `albums.artpath` UPDATE statements (text and BLOB form) were wrapped in one shared try/except that reset `changed_artpaths` to 0 on *any* exception -- if the first statement succeeded and the second raised, that first statement's row change remained part of the transaction and was still committed by the unconditional `con.commit()` immediately after, while the reported count silently said zero. Fixed by isolating each statement in its own try/except (matching the original pre-PR app.py code's per-statement isolation) so a failure in one never discards or miscounts the other's real, committed effect. Also added explicit `try/except/finally` transaction handling with `con.rollback()` on failure and `con.close()` in `finally`, replacing reliance on GC-timing for cleanup.
- **Genuine defect found and fixed -- `_review_paths_equal()` authorization fallback failed open.** This function backs `_pending_review_matches()`, which authorizes destructive Pending Review `delete_rejected`/`delete_duplicate` cleanup actions. It attempted trusted resolution on both sides, but fell back to weak raw-string (backslash/trailing-slash normalized) equality whenever *either* side failed trusted resolution -- meaning a stored Pending Review entry that itself failed containment/symlink validation could still be accepted as "equal" to a request path if their raw text happened to normalize the same way. Fixed to fail closed: return `False` whenever either side cannot be trust-resolved, never falling back to string comparison for that case.
- **Regression found and fixed -- preview-cache freshness was weakened by the security fix.** The old cache-key implementation included `size`/`mtime_ns` from `Path(source_path).stat()`; the new containment-validated implementation replaced that entirely with just the canonical path string, losing invalidation when a reviewed folder's contents change (file added/replaced/removed) without the path itself changing. Restored the same stat-based freshness signal, now computed from the already-validated canonical path rather than raw request text, so the security fix and the freshness property both hold.
- **Investigated and reverted -- album-edition-safety classification.** Initially added a blocker demoting any same-RGID multi-folder merge that introduces new (non-duplicate) audio content to "Needs review" instead of auto-"Safe," reasoning that RGID proves album-family membership but not identical track layout/edition. This directly contradicted deliberate, extensively tested pre-existing product behavior in `tests/test_album_folder_cleanup.py` (e.g. `test_source_only_audio_under_same_rgid_is_safe_move`, `test_same_rgid_two_stale_sources_plus_canonical_all_safe`), which establishes that merging non-colliding same-RGID files *is* the intended, reviewed "Safe" outcome (the product's model is that a confirmed same-RGID match legitimately arises from fragmented/partial imports splitting one album across folders, and the team has accepted the tradeoff against the rarer distinct-edition-sharing-an-RGID case). Reverted the code change and the tests that asserted it, per CLAUDE.md's "do not rewrite broad areas or user-facing behavior unless the task explicitly calls for it." The theoretical risk (a genuinely different edition sharing an RGID getting auto-merged) is real but unresolved -- recorded here as a deferred, unresolved product-design question requiring a stakeholder decision (e.g. a track-count/duration-continuity heuristic), not a unilateral code change against tested intent.
- Architecture boundary: independently re-verified `POST /library/rewrite-path` is appropriately narrow (rejects outside-root, root-self, and symlink-component paths; requires the destination to already exist as a real file) but has no per-call authorization input beyond the two paths themselves (no album/item ID, no idempotency key) -- any caller that can reach `BeetsClient.rewrite_library_path()` can rewrite an arbitrary matched item's/album's stored path. Given the only caller is the already-path-and-identity-validated album-cleanup apply flow (not directly exposed to the web-manager's own HTTP API), and the control-agent endpoint itself is internal-only (port 8338, not published), this is accepted as sufficiently bounded for the current caller set; a stronger per-operation authorization contract (expected row count enforced server-side, not just reported) would be warranted before any second caller is added.
- Alert #475 (`py/clear-text-storage-sensitive-data`) independently traced via the full SARIF code-flow (18-node thread flow): the recognized "source" is `_album_cleanup_trusted_destination()`'s return value, reached through `action.get("target")` -- CodeQL's sensitive-data heuristic evidently pattern-matches on the generic key name `"target"`, not any actual credential. The traced sink writes a filesystem destination path (a duplicate-file quarantine target inside the user's own music library) into the operational cleanup report (`ok`, `dry_run`, `root`, `summary`, `operations`, `errors`, `rollback` -- all counts, paths, and status text). No `AUTH_TOKEN`, `BEETS_API_TOKEN`, provider API key, Plex token, request header, provider URL, raw exception text, or database credential value is anywhere in this flow; `log` entries in this exact code path were already edited to use `type(exc).__name__` instead of raw exception text. The report file (`METADATA_CACHE_ROOT/album-folder-cleanup-last.json`) is read back only through the app's own authenticated status endpoint, the same pattern as other operational caches. Dismissed as `false positive` with this exact trace as justification.
- Test quality: added/kept `ReviewPathsEqualFailClosedTests` (4 tests) and `AlbumCleanupMoveDbFailureRecoveryTests` (1 test) to the new wave-4 test file, plus one new `ControlAgentLibraryRewriteTests` case for the zero-row-rewrite rejection (skipped on Windows for the same pre-existing POSIX-only-path-validator reason as the existing success-path test; real verification via Docker). 3 of these new tests independently verified to fail against the pre-review head via `git stash`/`git stash pop`. No regressions: full `tests/test_album_folder_cleanup.py` (239 tests) and the rest of the full suite pass unchanged.
- Remaining live count: not claimed here -- see the final report for the exact live count on the final reviewed/merged head.
- Priority: P1 (unchanged).
- Status: Open (SEC-002 overall). See the final report for exact reviewed head, alert disposition, and merge decision.
### Path-Injection Wave 5: album artwork save/upload -- Codex implementation, pending Claude final review

- Starting main SHA: `a92b3d4d6594fa6dc80bf2cf8e26df8f074f0ea8` (PR #60 squash). Live paginated `main` inventory on this SHA: 199 total open alerts, 182 `app.py` alerts, 158 `app.py` `py/path-injection`, all high severity. Latest Python CodeQL analysis was on this exact SHA. Creation-date distribution: 185 alerts from 2026-07-20, 8 from 2026-07-30, and 6 from 2026-08-05.
- Fixed-but-open prior-wave accounting, not subtracted from the live totals: Wave 1's 32 (`#256`-`#272`, `#274`-`#277`, `#280`-`#285`, `#417`-`#421`); Wave 2's 17 (`#156`-`#163`, `#166`-`#170`, `#174`-`#176`, `#178`); Wave 3's 3 reviewed false positives (`#292`-`#294`); and Wave 4's 11 (`#194`, `#196`, `#197`, `#205`, `#207`-`#212`, `#216`). These remain live GitHub alerts until CodeQL or Claude disposes them individually.
- Selected Wave 5 alerts: `#182` (`app.py:12788`, request-controlled `aldir` to `dest.write_bytes(data)`), `#183` (`app.py:12790`, same tainted destination to `os.chmod(dest, 0o644)`), and `#184` (`app.py:12819`, request-controlled `aldir` to `(Path(aldir) / aname).exists()`). All three remain open, high-severity `py/path-injection` alerts in `app.py`, and the SARIF source-to-sink trace is the same album-art save/upload flow.
- Functions and routes: legacy `POST /api/save-album-art`, `POST /api/albums/<aid>/art/url`, `POST /api/albums/<aid>/art/upload`, `DELETE /api/albums/<aid>/art`, `_repair_album_art()` Discogs fallback, `_album_art_candidates()`, `_usable_album_art_file()`, and new control-agent `POST/DELETE /albums/<id>/art` operations.
- Root causes fixed: clients could choose or influence album-art destinations through `aldir`, upload filename/extension/MIME header, or stored `albums.artpath`/item paths before the filesystem sink. The app now binds artwork mutation to a Beets album ID and expected `mb_releasegroupid`; it no longer uses a client-selected album folder as the destination. Legacy `aldir` is accepted only as optional disambiguation when no album ID is provided.
- Album identity policy: artwork mutation is authorized by current Beets album ID, with the route passing the current MusicBrainz Release Group ID as an expected identity. The engine re-reads the album row under the mutation lock and rejects if the release-group identity changed before replacement. Missing RGID does not become an alternate path authority; the engine still resolves the current album folder from current item rows.
- Upload validation and destination policy: app and engine both reject empty/oversized/animated/unsupported or undecodable images using Pillow, with JPEG/PNG/WebP accepted at the API boundary and normalized to fixed `albumart.jpg` in the engine. The client filename, extension, and `Content-Type` never select the destination. URL flows keep using `validate_outbound_url()` before download; redirects remain subject to the installed secure urllib policy.
- Architecture decision: Beets/media album artwork and `albums.artpath` are Beets-owned state, so new authoritative replacement/deletion moved behind `backend.beets_control_agent` and `backend.beets_client`. No media mount, local Beets package, or broad `BEET_BIN` migration was added. Existing embed/fetch command-array follow-up remains part of the repository-wide `BEET_BIN` architecture debt; this wave only documents it and does not migrate unrelated call sites.
- Engine operations added: `BeetsClient.replace_album_art()`, `BeetsClient.delete_album_art()`, and control-agent `POST /albums/<id>/art` / `DELETE /albums/<id>/art`. The engine operation uses fixed roots, stored-path validation that preserves POSIX literal-percent names, NUL/backslash/Windows/UNC rejection, canonical album-folder resolution, root-self refusal, symlink-component and existing/dangling symlink-leaf rejection, mutation lock, identity recheck, fixed filename, atomic temp write + replace, rollback on post-replace failure, and quarantine-on-delete constrained under `BEETSDIR`, explicit cross-device copy/fsync/unlink fallback, and DB clear rollback.
- Equivalent unreported fixes: multipart upload no longer writes `dest.write_bytes()` in `app.py`; URL replacement and `_repair_album_art()` Discogs fallback no longer set `artpath` separately from the file operation; `DELETE /api/albums/<aid>/art` no longer unlinks app-side candidates and no longer returns raw `BeetsError` text to public responses (PR-head alert `#485`); artwork status/read helpers now reject symlink leaves/components; the engine stored-path helper no longer converts POSIX DB paths through host `Path` text before validation and now rejects stored NUL/backslash/Windows/UNC paths.
- Tests added/updated: new `tests/test_sec002_app_path_album_art.py` covers real temporary music roots, real SQLite albums/items, real image bytes, symlink album folders and symlink artwork leaves, stale RGID rejection, fixed destination filename, dangling destination-symlink rejection, pre-backup and post-replace rollback, quarantine-root containment, cross-device quarantine, Flask legacy-save/upload/delete route behavior, upload filename traversal, and safe public error text. Updated `tests/test_pr39_codeql_alerts.py` so the existing album-art delete exception-sanitization regression covers `delete_album_art()` instead of the removed direct `clear_album_artpath()` path.
- Remaining live-count estimate before PR-head CodeQL: live `main` remains 199 total open and 158 `app.py` `py/path-injection` alerts. This wave is expected to address the 3 selected original artwork alerts plus the equivalent upload/delete/fallback flows above; exact remaining count is authoritative only after PR-head CodeQL and/or a post-merge `main` rescan. No alert was dismissed by Codex.
- Recommended Wave 6 cluster: the deferred AI/library duplicate scanning region (`#185`-`#191`) if Claude wants to stay adjacent to this artwork area, or qBittorrent hardlink repair (`#253`-`#254`) if prioritizing a smaller request-controlled filesystem mutation cluster. The repository-wide `BEET_BIN` migration remains separate architecture debt and should not be mixed into a path-injection wave.
- Priority: P1 (authenticated path-injection into media-owned artwork write/delete and Beets DB `artpath` mutation).
- Status: Open (SEC-002 overall). Codex implementation is in progress; Claude has not performed final independent review, alert disposition, or merge decision.

#### Claude final independent review (PR #61)

- Reviewed exact PR base `8ad8a85968d40e3b7f5cca1c4749ea497b32d6ad` / head `91b3034d235fcdf3212511acc5c11b1c212895e6` (5 commits, 7 files). `origin/main` had independently advanced to `27b4632eac6a0f79edfeffa42e613dd55f023164` (two unrelated deployment/CI/Beets-engine-version commits, `4d465a7` and `27b4632`) during this review; confirmed via `git diff --stat` that neither touches `app.py`, `backend/beets_client.py`, `backend/beets_control_agent.py`, `requirements.txt`, or `frontend/package.json` -- zero overlap with this PR's files or dependencies.
- Original alerts `#182`-`#184` reconstructed against `origin/main`'s pre-wave-5 `app.py`: all three trace to the same legacy `save_album_art()`/download-and-cache helper, sink lines 12788 (`dest.write_bytes(data)`), 12790 (`os.chmod(dest, 0o644)`), 12819 (`(Path(aldir) / aname).exists()`), all fed by request-body `aldir` guarded only by a **lexical** `Path(aldir).relative_to(music_root)` check (no `realpath`/symlink resolution, so a `..`-containing `aldir` such as `/data/media/music/../../etc` passes the check while resolving outside the root). Confirmed this entire vulnerable function no longer exists anywhere in the PR-head `app.py` -- the class of defect is removed, not patched in place. Disposition: **fixed by code** for all three, pending merge + `main` rescan to auto-close.
- Genuine findings independently identified and fixed (none flagged by CodeQL; found through direct code reading and cross-file tracing, matching the task's "equivalent unreported artwork vulnerabilities" charge):
  1. **Unclamped environment-configurable image-safety limits.** `ALBUM_ART_MAX_BYTES`/`ALBUM_ART_MAX_PIXELS`/`ALBUM_ART_MAX_DIMENSION` read directly from `os.environ` with no minimum/maximum clamp -- a misconfigured or compromised `.env` could set `BEETS_ALBUM_ART_MAX_PIXELS` to an unbounded value and re-enable a decompression-bomb-class resource-exhaustion vector regardless of image byte size. Fixed with a new `_env_int_clamped()` helper (mirroring `app.py`'s pre-existing, already-proven `_env_int(minimum=, maximum=)` pattern) with hard ceilings (100MB / 200MP / 20000px) the environment cannot override.
  2. **Two remaining raw-`BeetsError`-text exposure sinks.** `album_upload_art()`/`album_replace_art_from_url()` both still had `except BeetsError as ex: raise RuntimeError(str(ex)) from ex`, propagated verbatim into `job_engine.py`'s `PythonJob._run()` (`self.log.append(f"ERROR: {exc}")`) and exposed through the job-status API's `log` field -- the same class alert `#485` fixed for the delete route but did not cover here. Traced `beets_client.py`'s `_request()`: a `BeetsUnavailableError` message always includes the raw `exc.reason` and internal `self.base_url`, so this was a genuine, not theoretical, leak surface. In the current call graph both branches are shadowed by `_replace_album_art_bytes()`'s own broader `except Exception` (which already converts to a safe message before the outer handler ever sees it), making them currently-unreachable dead code -- but CodeQL's taint analysis does not require reachability to flag `str(ex)` on an exception, and a future simplification of the inner helper could silently reactivate the path. Fixed both to match the delete route's already-correct pattern (fixed generic message + `app.logger.warning(type(ex).__name__)`), as defense-in-depth, not merely dead-code removal.
  3. **`_album_art_album_dir()` picked the first resolvable item directory without checking the rest agreed.** Iterated up to 25 item rows and `return`ed on the first one that safely resolved, never comparing it against the others. A stale/corrupted database row (items genuinely split across two unrelated directories, not a legitimate multidisc `Disc N` layout) would silently scope the artwork mutation to whichever item happened to be first, rather than refusing. Fixed: the function now requires every resolvable sampled item to agree on the same directory (multidisc unification via the existing `Disc N` climb-to-parent logic is unaffected and still passes) and raises 409 `"Album items are split across conflicting directories"` on disagreement.
  4. **`_fsync_file()`/`_fsync_directory()` silently swallowed all exceptions.** Both existed as bare `except Exception: pass`, so a real fsync failure after a successful rename was invisible -- the operation still reported unqualified success. The primary content write's own fsync (before the rename) was already unguarded/non-swallowed, so file *content* durability was never actually at risk; the swallowed calls only covered the rename/directory-entry durability step. Fixed: both now return `bool` and log a warning on failure; `_replace_album_art_locked()`'s result now includes a `"durable"` field reflecting whether both post-rename fsyncs actually succeeded, rather than presenting an unqualified durability guarantee the code could not back up. Deliberately did not add rollback-after-commit for a fsync-only failure -- by that point the DB commit and file rename have already succeeded correctly; rolling back a fundamentally correct write over a narrow, low-probability durability-metadata gap would be more disruptive than informative.
  5. **Quarantine target claim was check-then-act, not atomic.** `if target.exists() or target.is_symlink(): ... ; if target.exists() or target.is_symlink(): raise` left a real race window before the later `os.replace()`/`shutil.copy2()` in `_replace_or_copy_unlink()`, both of which silently overwrite an existing destination -- exactly what the review brief warned a random UUID alone does not prevent. Fixed with a new `_claim_trash_target_exclusive()` using `os.open(O_CREAT|O_EXCL)`, a real atomic create-if-absent syscall, closing the window entirely rather than narrowing it.
- Reviewed and accepted as already correct, no fix needed: `_resolve_album_art_request_album()`'s artist+title fallback requires exact case-folded equality and `len(matches) != 1` rejection (two same-artist/same-title albums correctly block rather than picking one); client-supplied `aldir` is used only as a filter predicate against the server-computed `_album_dir_for_art()` value, never as a destination itself; `validate_outbound_url()`/`OutboundPolicyError` (the centralized SSRF policy) is reused unmodified for the URL-replace route, no weaker artwork-specific reimplementation was introduced; EXIF-transpose happens after dimension/pixel-count validation but a transpose only swaps width/height, both of which were already checked against the same symmetric limit, so no post-transpose bypass exists.
- Documented, not fixed (pre-existing, out of narrow scope, or fails safe): `_album_dir_for_art()` (pre-existing, not added by this PR) depends on `MUSIC_ROOT` being mounted in the web-manager container for its `.exists()`-based checks; the canonical production `docker-compose.yml` does not mount it. This only affects the *optional* `aldir` disambiguation narrowing in the legacy save route, and fails **closed** (every candidate gets excluded, so the route correctly refuses rather than guessing) -- not a security defect, but the `aldir` disambiguation feature is non-functional in the canonical production deployment. `RGID`-empty albums get no identity re-verification at mutation time (`if expected and expected != actual` short-circuits on empty `expected`) -- the mutation is still bound to the numeric album ID re-read fresh under lock (not name/folder matching), so this is a narrower "can't detect a content swap under the same ID between resolution and lock" residual risk, not an "authorize an unrelated album" defect; would need a secondary heuristic (track count/duration) to close fully, out of narrow scope here. The local `beet embedart` follow-up call in `album_upload_art()`/`album_replace_art_from_url()` cannot succeed in the standard deployment (`BEET_BIN` resolves to a nonexistent path in the web-manager container, which intentionally has zero local Beets install) -- already treated as best-effort/non-blocking by the existing code, left in place with an explanatory comment; embedding into file tags (as opposed to the folder-level `albumart.jpg` the engine already writes) is tracked here as future engine-side work, not part of this route's success contract, and the repository-wide `BEET_BIN` migration remains explicitly out of scope per this task's authorization.
- API compatibility: no client-visible contract change from this review's fixes. The new `"durable"` result field is additive. Response shapes, status codes, and accepted image formats are unchanged from Codex's implementation.
- Tests added: `EnvClampedArtworkLimitTests` (7 tests, direct unit coverage of `_env_int_clamped` plus a module-constant sanity check), `test_replace_album_art_blocks_when_items_point_to_conflicting_directories` / `test_replace_album_art_unifies_legitimate_multidisc_item_directories` (positive and negative coverage for finding 3), `test_replace_album_art_reports_durability_true_on_clean_write` / `..._false_without_failing_when_fsync_fails` (finding 4; the "true" assertion is POSIX-only-gated since `_fsync_directory`'s `os.O_DIRECTORY` has no Windows equivalent and this agent only ever runs in Linux containers), `test_claim_trash_target_exclusive_rejects_pre_existing_file` / `..._creates_new_file` (direct coverage of finding 5's atomic primitive) plus an end-to-end quarantine-collision contract test, `test_upload_album_art_does_not_leak_beets_error_text` / `test_replace_art_from_url_does_not_leak_beets_error_text` (finding 2, mocking the inner helper directly so the outer handler's own sanitization is what's actually exercised, not the inner helper's). All new tests independently verified via `git stash` to fail against the pre-fix code for the intended reason (`AttributeError` for the three new-helper-based fixes, wrong status code / leaked literal text for the other two).
- Validation: full suite (`python -m unittest discover -s tests -p "test_*.py"`) run 3 consecutive times, 1691 tests, OK each run, no skips beyond the pre-existing 6; targeted artwork/security modules run twice more (255 tests each) for additional determinism confidence. `py_compile`, `scripts/security_secret_scan.py`, `scripts/validate_compose_security.py` all clean. `git diff --check` clean (no whitespace issues).
- PR-scoped CodeQL on the pushed fixes flagged 2 new alerts (`#486`, `#487`, both `py/clear-text-logging-sensitive-data`, `backend/beets_control_agent.py` lines 837/850 -- the two new `print()` warnings added by finding 4's durability fix). Traced the full SARIF thread-flow for both: the reported taint source is the variable literally named `trusted` (lines 574/577, and the `resolve_safe_path()` return value at line 627 assigned to a variable of the same name) -- this codebase's established, load-bearing name for "a path-traversal-validated, safe filesystem `Path`" throughout the whole SEC-002 series, not a credential or secret of any kind. The sink logs only `path.name` -- a bare artwork filename such as `albumart.jpg`. No `BEETS_API_TOKEN`, password, session token, or other genuine credential is anywhere in this call chain. This is the identical naming-heuristic false-positive class already documented and dismissed for alert `#475` (there: the generic key name `"target"`). Both dismissed individually as `false positive` with this trace as justification, not renamed/refactored to dodge the heuristic (renaming `trusted` throughout the path-validation architecture would be a large, out-of-scope refactor of a name used consistently across every prior wave).
- Remaining live count and exact CI/CodeQL disposition: recorded in the PR's final report, not duplicated here.
- Priority: P1 (unchanged).
- Status: Open (SEC-002 overall). See the final report for exact reviewed head, alert disposition, and merge decision.

#### Main reconciliation and re-review (PR #61, second pass)

- `main` advanced again mid-review via PR #64 ("remove local db fallback, fix auth persistence and api performance", merged `36bfc7554378a9ef6bd8f9c47a7d1be553647503`), touching `app.py` and `backend/beets_client.py` -- the same two files this wave modifies -- in parallel, unaware of Wave 5's new artwork methods. Initial review correctly stopped as `blocked -- exact reviewed state changed` rather than merging against a stale comparison.
- Codex reconciled by merging current `main` into `fix/codeql-app-path-wave5`. **Corrected SHAs** (an earlier draft of this record and the PR description contained inaccurate/truncated values that do not resolve on GitHub -- `c9c4aae8bdcb0faeee2fa1fb0c83a1fa06bc6fc2` and `a85ec49f3900b12574041b61ec3f4d01b17bce72` are both wrong): the real merge commit is `a85ec495d118eecaf8a4197bc136f4ece0e700ae` (parents: `648a97f224016fb9b5e8070f6efcc925b373f778` Claude's reviewed Wave 5 head, and `36bfc7554378a9ef6bd8f9c47a7d1be553647503` main at merge time); the real follow-up/final head is `c9c4aae0e0bf429626b4eee7ff0a70dd623e806a`. Independently confirmed via `git merge-base --is-ancestor`: both current `main` and the original reviewed head are ancestors of the final head.
- Reconciliation initially reintroduced a local-SQLite fallback into `get_db_connection()` (main's PR #64 version opened `sqlite3.connect(db_path)` directly for any non-default path). Fixed in the immediate follow-up commit `c9c4aae0e0bf429626b4eee7ff0a70dd623e806a`: `get_db_connection()` now unconditionally returns `RemoteSQLiteConnection`, with zero alternate local-file code path. Test-only DB access uses a separate, explicit injection point (`mock.patch.object(APP, "_db", ...)`), never `get_db_connection()`.
- Independently verified, not assumed from a clean auto-merge: AST-level comparison of all 7 Wave-5 artwork functions in `app.py`, plus a full-file diff of `backend/beets_control_agent.py`, confirm both are **byte-identical** between the pre-reconciliation reviewed head (`648a97f`) and the final head (`c9c4aae0`) -- the reconciliation touched neither.
- Re-ran full validation directly on the final head rather than trusting the reconciliation report's claimed numbers, which were themselves inaccurate: full suite is **1717 tests, `OK (skipped=5)`**, confirmed on 3 consecutive runs (report claimed 1,730 tests / 176 skips); the focused module list is **300 tests**, confirmed on 5 consecutive runs (report claimed 114). `test_get_db_connection_never_uses_local_sqlite` -- the one test that failed in a synthetic pre-fix merge preview during the first review pass -- passes cleanly and deterministically on the corrected head.
- CI on the exact final head: all 14 required checks pass, including `python-tests` for real on the CI runner (not just locally). CodeQL: 0 open PR-scoped alerts; `#486`/`#487` remain correctly dismissed (their file is unchanged, so the trace is identical).
- Frontend (`npm ci`, `typecheck`, `lint`, `build`, `test` 15/15, `audit --audit-level=high`): all clean, 0 vulnerabilities.
- Docker: both images rebuilt `--no-cache` with `VCS_REF=c9c4aae0e0bf429626b4eee7ff0a70dd623e806a`; OCI revision labels confirmed exact; `scripts/verify_beets_engine_image.py --check-s6-supervision` passed in full including a real S6 crash/restart cycle.
- PR description corrected to remove the inaccurate SHAs, mangled backtick escaping, and inconsistent test counts; replaced with the values verified above.
- Status: Open (SEC-002 overall). See the final report for the merge decision.

### SEC-002 Wave 6: qBittorrent Hardlink Repair Path Security -- Codex implementation, pending Claude final review

- Starting main SHA: `3994d6818531e6b4855bb3717a7ef2b94fd326a4` (PR #61 merged).
- Overlap verification: PR #65 (`fix/harden-truenas-rollout`) verified open; modifies auth token persistence and deployment scripts with ZERO overlap with qBittorrent hardlink repair helpers.
- Live inventory: 198 total open alerts, 180 `app.py` open alerts, 155 `py/path-injection` open alerts in `app.py`.
- Selected Wave 6 alerts: `#253` and `#254` (`py/path-injection` at `app.py:33176` for qBittorrent hardlink repair path injection).
- Architecture boundary & refactoring: Moved direct local filesystem hardlink creation (`os.link`) out of `app.py` and behind the Beets control agent boundary (`POST /files/hardlink` in `backend/beets_control_agent.py` and `BeetsClient.create_hardlink` in `backend/beets_client.py`).
- Path safety & containment: `_path_under` updated to use `os.path.realpath` with root-parent containment check instead of `resolve(strict=False).relative_to()`, eliminating CodeQL's untrusted `resolve` path-injection source. `_qbit_map_path` updated to enforce component-boundary matching.
- Security contract: Fixed root containment (`music`, `staging`, `tmp`), source file regular/non-symlink check, same-inode idempotency check (`already_present`), cross-device link rejection (`EXDEV`), pre- and post-link identity verification under lock, and error message sanitization.
- Tests added: `tests/test_sec002_app_path_qbittorrent_hardlink.py` covers path containment, alias prefix mapping, control agent endpoint validation, idempotency, size checks, and error message sanitization.
- Status: In Progress / Ready for Draft PR and Handoff to Claude.

#### Claude final independent review (PR #66, Wave 6)

- **SHA corrections**: the handoff report's pushed-commit SHA (`9d110b411d3bfcaae9b61d36d40081d59664db80`) and its `VCS_REF` Docker claim (`3994d6818531e6b4855bb3717a7ef2b94fd326a4`, the base commit) were both wrong. Live PR head verified via `gh pr view 66`: `9d110b43573cf9a399211a95cc122f6d1239d8af`. Reported Docker/OCI validation against the base SHA did not validate this branch's code; rebuilt and reverified against the actual final head below.
- **CI infra flake**: the initial `statusCheckRollup` showed 6 duplicate check failures (CodeQL x3, frontend-lint, frontend-typecheck x2, security, python-tests) alongside successful runs for the same head SHA. Root-caused via `gh run view --log-failed`: all failed for the identical reason, `Failed to resolve action download info. Error: Service Unavailable`, a transient GitHub Actions outage unrelated to code; the parallel `push`-triggered runs on the same commit succeeded. Re-ran the affected jobs; not a code defect.
- **Alert #253/#254 mischaracterization**: both alerts trace to `_path_under()` (`app.py:33176` pre-fix), a shared helper with 17 call sites spanning album-folder cleanup, rename dedup, artwork replacement, and template-token cleanup -- **none of which are qBittorrent-specific**. The GitHub alert API does not expose full codeFlows via REST, but no `_qbit_*` function calls `_path_under()` at all. Labeling this wave "qBittorrent hardlink repair" is an approximate wave assignment, not a precise trace; fixing `_path_under()` is still the correct and sufficient fix for these two alerts.
- **Real authorization gate was untouched**: `_qbit_allowed_repair_path()` -- the actual function gating qBittorrent-derived destination paths -- called `_path_is_under()`, a separate, older, ~60-call-site helper with the exact CodeQL-flagged pattern (`resolve(strict=False).relative_to()`), left unfixed by the original patch. Changed `_qbit_allowed_repair_path()` to call the hardened `_path_under()` instead, closing the gap for this feature without touching the other 60 unrelated call sites of `_path_is_under()`.
- **Endpoint authority narrowed**: `/files/hardlink` validated both `source_path` and `target_path` against the same `["music", "staging", "tmp"]` root set in either direction -- a generic cross-root hardlink primitive, not a scoped repair operation. The only real caller (`_qbit_hardlink_missing_impl`) always uses a database-verified library file as source and a torrent/staging-side path as destination. Endpoint now hardcodes `source_roots=["music"]`, `dest_roots=["staging", "tmp"]`; `music` is no longer a valid destination and `staging`/`tmp` are no longer valid sources.
- **Source identity gap**: `_library_file_candidates_for_qbit()`/`_metadata()` selected a hardlink source from the Beets `items.path` DB column without verifying it actually resolved under `MUSIC_ROOT`. Added an explicit `_path_under(path, MUSIC_ROOT)` check before a DB row becomes source-eligible.
- **Symlink checks were dead code -- found via real Linux execution, not static reading, then corrected twice**: `resolve_safe_path()` fully dereferences symlinks via `os.path.realpath()` before returning, so the existing `safe_src.is_symlink()` / `safe_dst.is_symlink()` checks operated on the already-resolved target and could never observe a symlink whose target existed. Running the new endpoint-level tests in a `python:3.12-slim-bookworm` container (Windows dev host cannot exercise `resolve_safe_path`'s POSIX-absolute-only inputs against real files) surfaced this directly: a symlink source with an in-root target was silently linked instead of rejected. First fix: checked `os.path.islink()` on the raw, pre-resolution `source_path`/`target_path` strings. That introduced 24 new PR-scoped `py/path-injection` alerts (a fresh filesystem call fed directly by unresolved request data, flagged even though `resolve_safe_path` had already validated the same value moments earlier -- CodeQL's dataflow does not credit that as a barrier for a second, independent call on the original variable). On reflection this check was also not actually load-bearing: containment is enforced on the fully resolved path either way, so a symlink source/destination grants no capability beyond naming its resolved target directly would, and the TOCTOU window is already closed by the lock + pre-link stat + post-link inode-identity verification regardless of symlink involvement. Removed the raw-string check rather than reimplementing it in a disguised form; a symlink source with an in-root target is now a legitimate, intentionally-accepted request (see `test_symlink_source_with_in_root_target_links_the_resolved_file`), while one escaping every allowed root is still rejected by the pre-existing containment check (`test_symlink_source_escaping_every_root_is_rejected`). A pre-existing symlink destination remains safe by construction: it either matches the source (`already_present`) or differs (`409`, no overwrite) via the existing dev/ino comparison, with `os.link()` never reached in either case.
- **`expected_size` validation**: previously silently skipped the size check for any non-`int` value (string, float) and accepted `bool` (a subclass of `int`) as if it were a byte count. Now rejects missing-but-wrong-type, `bool`, zero, and negative values with 400 before any path resolution.
- **Insecure `os.link` fallback removed**: the `TypeError`-triggered fallback to a symlink-following `os.link()` call has no reachable trigger on the Linux production target (CPython raises `NotImplementedError`, not `TypeError`, when `follow_symlinks=False` is unsupported) and was dead/misleading code; replaced with a `NotImplementedError` catch that fails closed (500) instead of silently downgrading the no-follow guarantee.
- **Destination-parent TOCTOU**: added a re-validation (symlink check + `resolve_safe_path` re-containment) of `safe_dst.parent` immediately after `mkdir(parents=True, exist_ok=True)`, defending the window where a concurrent actor could swap the parent for a symlink between the locked validation and directory creation.
- **Idempotency reporting bug**: `summary["linked"]` was incremented for both newly-created and already-existing links. Fixed to increment `summary["already_present"]` (an existing, previously-unused-here counter) when `already_present` is true, so an idempotent no-op is never reported as a new link.
- **Response information trimming**: the engine's `/files/hardlink` responses included absolute container paths and raw `st_dev`/`st_ino` values that no caller consumes (`_qbit_hardlink_missing_impl` only reads `ok`/`already_present`/`error`, using its own locally-known paths for display). Trimmed to `{"ok": true, "linked"|"already_present": true}`.
- **Error-message allowlist**: `action["error"]` in the web-manager job log forwarded the control agent's `error` string verbatim. All current engine error strings are fixed, non-secret text, but the boundary should not assume every future one is; added `_qbit_hardlink_public_error()`, an explicit allowlist of the known-safe strings, with a generic fallback for anything else (including a malformed/non-dict response).
- **Tests**: added `ControlAgentHardlinkEndpointTests`, invoking `ControlAgentHandler.do_POST()` directly (the established real-endpoint pattern from `ControlAgentLibraryRewriteTests`) rather than mocking `BeetsClient._request()`/`create_hardlink()`. Covers: valid link creation, idempotency, different-file collision, outside-root source, wrong-role source/destination, invalid `expected_size` (string/float/bool/zero/negative), symlink source, symlink destination, symlinked destination parent, and no-overwrite-on-race. Success/collision/symlink tests that require real POSIX-absolute paths against real files skip on Windows (`resolve_safe_path` requires a literal leading `/`) and are exercised for real in Linux CI/Docker; the initial fixture design accidentally nested all three root categories under the OS temp directory (itself a valid `tmp` root), masking the role-separation and symlink-escape checks it was meant to prove -- corrected to place fixture roots outside the system temp directory.
- **Torrent/file identity** (deeper hardening -- filename+size+optional album/track metadata matching, no checksum/AcoustID cross-check) and **plan provenance/undo audit trail** (a bound, server-generated repair-plan record with rollback) are both genuine product/architecture gaps raised during this review but are out of scope for a narrow CodeQL-remediation wave; they require a product decision on acceptable evidence strength and a persistent audit ledger this codebase does not yet have. Recorded here as follow-up technical debt, not blocking this wave.
- **CI note**: this review's final validation pass on this PR ran during a documented GitHub-wide GitHub Actions major outage (per githubstatus.com, began 2026-08-06 15:22 UTC, resolved 2026-08-07). All failures observed were confirmed via job logs (`Failed to resolve action download info: Service Unavailable`) or GitHub-side queueing delays, not code defects; every check was independently re-verified either by CI reruns once dispatched, or directly via local/Docker-based execution (full test suite, focused suite, and real HTTP round-trips against the built engine image) documented below. The outage prevented the ordinary `lint`/`typecheck`/`security`/`unit-tests`/`node-build`/`docker-build` workflows from ever dispatching for the exact final head (`df86ca0fde490a19bedb88ce764e6e93a6cd56d6`) even after it resolved; this documentation correction commit is expected to retrigger them now that GitHub Actions reports "All Systems Operational."
- **Final independent re-verification on the exact final head** (`df86ca0fde490a19bedb88ce764e6e93a6cd56d6`): PR-scoped CodeQL alerts are **0 open** -- the 20 sanitizer-model false positives (`#493`, `#495`-`#516`, `#494` never existed as a distinct alert) were individually dismissed with the justification above (one, `#493`, dismissed directly; the remaining 19 dismissed manually by the repository owner after Claude's per-alert classification, per the auto-mode safety classifier blocking a rapid dismissal sequence as a bulk-action pattern -- each still carries its own individual, accurate justification, not a blanket one). Focused suite (`test_sec002_app_path_qbittorrent_hardlink`, `test_control_agent_field_allowlist`, `test_control_agent_json_serialization`, `test_external_beets_architecture`, `test_security_hardening`, `test_transaction_engine`, `test_transaction_integration`, `test_sec002_codeql_backlog`): **248 tests, `OK (skipped=11)`**, 5/5 consecutive runs. Full suite: **1742 tests, `OK (skipped=16)`**, 3/3 consecutive runs. Frontend (`typecheck`, `lint`, `build`, `test` 15/15, `audit --audit-level=high`): all clean, 0 vulnerabilities. Static/security (`py_compile`, secret scan, `validate_compose_security.py`, `verify_security_config.py --tracked-files-only`, `git diff --check`): all pass. Docker: both images rebuilt `--no-cache` with `VCS_REF=df86ca0fde490a19bedb88ce764e6e93a6cd56d6`; both OCI revision labels confirmed exact; `verify_beets_engine_image.py --check-s6-supervision` passed in full (S6 crash/restart recovery, clean shutdown). Disposable Linux runtime: ran the real engine container end-to-end over its actual HTTP server (not a mocked handler) -- valid music-to-staging link (200/linked), idempotent repeat (200/already_present), reversed source/destination roles (403), missing auth token (401), and invalid `expected_size` type (400) all behaved exactly as intended; all temporary resources torn down afterward.
- Status: Open (SEC-002 overall). See the final report for the merge decision.

### SEC-002 Wave 7: Library Duplicate / AI Scan Path Security -- Codex implementation, pending Claude final review

- Starting main SHA: `7c388de6613e6f25667020e303f7c0b4ada5592f` (PR #66 merged).
- Overlap verification: PR #65 (`fix/harden-truenas-rollout`) verified open; modifies auth token persistence and deployment scripts with ZERO overlap with library duplicate/AI scan path security helpers.
- Live inventory: 196 total open alerts, 153 `py/path-injection` open alerts in `app.py`.
- Selected Wave 7 alerts: `#185`, `#186`, `#187`, `#188`, `#189`, `#190`, `#191` (`py/path-injection` in `app.py` for AI scan / library duplicate evidence gathering and folder resolution).
- Path safety & containment: `_build_folder_evidence`, `_path_origin_hint`, `_import_review_folder_signature`, `_ai_suggest_folder_internal`, `_candidate_track_local_candidates` updated to use `_resolve_import_review_source_path` with strict root containment validation, eliminating untrusted path inputs.
- Symlink & file safety: Enforced non-symlink checks and restricted folder recursion to validated container paths.
- Security contract & authority: Preserved AI advisory, non-authoritative role: AI suggestions cannot mutate or delete media files or library databases. Deterministic MusicBrainz/AcoustID identity required for auto-mutations.
- Tests added: `tests/test_sec002_app_path_library_duplicates.py` covers path containment, traversal rejection, null-byte rejection, evidence gathering, signature calculation, and error sanitization.
- Verification:
  - Focused suite (`test_sec002_app_path_library_duplicates`, `test_import_matching_evidence`, `test_submission_folder_resolution`, `test_ai_auth_setup_hardening`, `test_discogs_fallback`): **99 tests**, 5/5 consecutive runs passed.
  - Full Python suite: **1735 passed, 16 skipped, 317 subtests passed**, 3/3 consecutive runs passed.
  - Frontend (`npm ci`, `typecheck`, `lint`, `build` 13 static pages, `test` 15/15 passed, `audit --audit-level=high` 0 vulnerabilities): all clean.
  - Static security: `verify_security_config.py --tracked-files-only` ok: true, `validate_compose_security.py` ok: true.
  - Docker & Supervision: `beets-engine:ci` rebuilt with commit revision label; `verify_beets_engine_image.py --check-s6-supervision` passed with clean S6 crash/restart recovery.
- Status: In Progress / Ready for Draft PR and Handoff to Claude.

## SEC-002 Wave 8: AI Batch Import / Reimport / Unmatched-Draft Path Security -- Codex implementation, pending Claude final review

- Starting main SHA: `a7ec96c12ad6f68b9569c9c7af2f5f733429065d` (PR #67 merged).
- Live inventory at start: 188 total open alerts, 143 `py/path-injection` open alerts in `app.py`.
- Historical candidate cluster (`#219`-`#252`, `#414`-`#416`, per earlier SEC-002 analysis) reconstructed against the live tree: 31 of the historical numbers remain open, mapping to `reimport_disk`, `library_import_all`, `_mark_pending_review_status`, `_ai_batch_write_state`/`_ai_batch_load_state`, `start_ai_batch_import`, `create_unmatched_draft`, `start_import`/`import_preflight`, `_scan_scope_label`, `dedup_scan`/`dedup_cleanup`, `_resolved_path`/`_folder_clean_root`, `_mb_release_tracklist_read_disk`/`write_disk`. `#417`-`#421` (`_folder_cleanup_path`/`apply_folder_placeholder_action_api`) and `#480`-`#484` (`_target_preview_source_files`/`_remaining_audio_files`/`_remove_pending_review_for_path`) are adjacent but were **not** part of the original historical mapping and were not pulled into this wave's scope without independent justification -- left for a future wave's reconstruction.
- **Two genuine vulnerabilities found and fixed**, not CodeQL sanitizer-recognition false positives:
  1. **`start_ai_batch_import()` / `ai_batch_import_recover()` (`#226` area / `#229`/`#230`/`#414` area on the pre-fix tree)**: both routes read a client- or job-state-supplied `scan_path`/`source_path` with only an `os.path.exists()` check, then passed it directly into `_ai_batch_find_audio_dirs()` -- an **unbounded recursive `os.walk()`** with zero root-containment check anywhere in the call chain. Any authenticated caller (or corrupted/tampered job state on recovery) could point AI Batch Import at an arbitrary container path (e.g. `/`, `/web-manager-data`) and every discovered audio-bearing directory would be queued for AI-driven import review. Fixed by validating in the single shared entry point both routes call into (`_start_ai_batch_job`), via the already-hardened `_resolve_import_review_source_path()` (SEC-002 Wave 7), so stored job state is revalidated at execution time rather than trusted because it was "already validated when queued." Also validated at the `start_ai_batch_import()` route level as defense-in-depth.
  2. **`create_unmatched_draft()` (`POST /api/library/unmatched-draft`, `#229`/`#230`/`#414`)**: built `MUSIC_ROOT / artist / album_folder` from entirely unsanitized client-supplied `artist`/`album`/`year` text, then called `mkdir(parents=True)` + `write_text()` on the result -- an embedded `/` or `..` in any of those three fields escapes `MUSIC_ROOT` entirely (`Path.__truediv__` re-parses embedded separators as additional path components; concatenating first and sanitizing the combined string afterward would not have caught this). This was a genuine arbitrary-directory-creation-and-file-write primitive, more severe than the read-only AI-batch-scan issue, and had **zero prior test coverage**. Fixed by sanitizing each field through the existing `_safe_path_component()` helper (already used elsewhere in `app.py` for exactly this purpose -- reused, not reinvented) before folder-name construction, plus a final `_path_is_under()` containment re-verification immediately before the destructive `mkdir`/`write_text` calls as defense-in-depth.
- **Proven false positive (existing precedent, not re-derived)**: `_ai_batch_write_state`/`_ai_batch_load_state` (`#223`/`#226` on the current tree) are the renumbered continuation of `#327`/`#328`, already proven false positive during the main-branch CodeQL backlog triage above: `_ai_batch_state_file()` reduces `batch_job_id` to `[A-Za-z0-9_.-]` before ever joining it onto `_AI_BATCH_STATE_DIR`, so no input can introduce a path separator; existing test `tests/test_sec002_codeql_backlog.py::AiBatchStateFileContainmentTests` already covers this. Recommend Claude re-confirm and dismiss under the current alert numbers.
- **Defense-in-depth, not independently exploitable**: `library_import_all()`'s alert (`#220`, a `Path(...).resolve()` used only for a deduplication key) routes every album's `aldir` through `_ai_suggest_folder_internal()` for the actual discovery/import action, which is already hardened (SEC-002 Wave 7). The flagged line itself performs no filesystem mutation and no unvalidated read beyond the dedup key computation.
- **Not yet independently traced, deferred**: `_mark_pending_review_status`, `start_import`/`import_preflight`, `_scan_scope_label`, `dedup_scan`/`dedup_cleanup`/`_resolved_path`/`_folder_clean_root`, `_mb_release_tracklist_read_disk`/`write_disk` (roughly 20 of the 31 live historical alerts). A first read of `dedup_cleanup`'s `_clean_empty_parents` helper (bounded-depth, empty-directories-only `rmdir` walk toward a computed boundary) did not surface an obvious issue, but was not traced with the same rigor as the two fixed vulnerabilities above and is not being classified as either genuine or false-positive without that proof. Recommended as the starting scope for SEC-002 Wave 9, alongside the `#417`-`#421` and `#480`-`#484` adjacent clusters noted above.
- **AI authority**: confirmed unchanged by this wave. `_ai_suggest_folder_internal()` (already advisory-only per Wave 7) is the only AI-facing function touched, and only for path validation. The pre-existing, unrelated high-confidence batch-auto-apply pathway inside `library_import_all()`'s `_do()` closure (AI suggestion accepted directly into `mb_albumid` when confidence is `high`/`medium` and `mb_valid` is true, without a discrete human-reviewed step for that specific match) predates this wave, is unmodified by this diff, and remains a genuine, separate architecture question for Claude/product to decide -- not silently folded into this narrow path-injection remediation.
- **Root/staging ownership**: `reimport_disk()`'s ad-hoc `_allowed_roots = ["/data/media/music", "/data/torrents/music"]` check (which additionally ran *after* an existence probe, functioning as a path-existence oracle for arbitrary container paths) was replaced with `_resolve_import_review_source_path(aldir, allow_music=True, expected_type="dir")`, the same shared, already-audited resolver used throughout the import-review/AI-suggest flow. This is a deliberate widening from the original 2-item hardcoded list to the full `_import_review_cleanup_roots()` set (`DOWNLOADS_ROOT`, `PLAYLIST_DOWNLOAD_ROOT`, `_DOWNLOADS_ROOTS`, `TORRENT_SOURCE_ROOTS`, plus `MUSIC_ROOT`) -- judged architecturally correct, not an accidental cross-feature leak: all of those roots represent the same "server-controlled acquisition/staging area" trust boundary already established for every sibling import-review function, and none are attacker-controlled or independently-configurable-by-an-unrelated-feature in the way `QBIT_REPAIR_ALLOWED_ROOTS` was found to be in Wave 7. Existence-oracle behavior closed as a side effect (containment is now checked before any filesystem probe).
- Tests added: `tests/test_sec002_app_path_ai_batch_replacement_staging.py` (17 tests) covers outside-root/traversal/symlink/NUL rejection for both fixed vulnerabilities, a stale-job-state-revalidation regression test for the recover-route bypass, and Unicode/literal-name/embedded-separator correctness for `create_unmatched_draft`. `tests/test_ai_batch_retry_race.py`'s shared `BehavioralTestCase` fixture updated to patch `_DOWNLOADS_ROOTS` with its own scratch directory (needed once scan-path validation became real; matches the established pattern from SEC-002 Waves 6/7 of patching trusted roots in test setup rather than relying on production accommodating a test's arbitrary path). Both new fixtures were placed outside the OS temp directory after the identical Wave 6/7 "the OS temp dir is itself already a trusted root, silently masking the containment check" bug reproduced during Linux verification.
- Unrelated frontend finding fixed as part of mandatory frontend validation (not part of the CodeQL cluster, but blocking the required `npm audit --audit-level=high` zero-vulnerabilities gate): `nanoid@3.3.16` (transitive via `postcss`) had a known high-severity advisory (GHSA-2v37-7h3g-55p8, indefinite loop on a zero `size` argument). Pinned via a new `nanoid` entry in `frontend/package.json`'s existing `overrides` block and regenerated `frontend/package-lock.json`. The lockfile was regenerated inside a Linux container (`node:22-bookworm-slim`), not on the Windows host -- a first Windows-side regeneration silently dropped Linux-only optional dependency entries (surfaced as `npm ci` failing inside the Docker build stage with "Missing: yaml@2.9.0 from lock file"); the corrected lockfile installs cleanly via `npm ci` on both platforms.
- Disposable runtime validation (real HTTP server, throwaway bind mounts, no production paths): built `beets-web-manager-codeql-wave8:latest` and `beets-engine-codeql-wave8:latest` on the exact final head, ran the web-manager image standalone with disposable temp directories bind-mounted at the container's `/data/media/music` and `/data/torrents/music` (these are not mounted into `beets-web-manager` in either shipped compose topology -- confirmed pre-existing architecture debt, not something this wave changes or relies on for production). Exercised over real HTTP: `POST /api/library/unmatched-draft` normal creation (succeeds, correct folder under music root), traversal artist (`../../../../etc`) neutralized to a literal folder name and contained under music root; `POST /api/ai-batch-import` outside-root scan_path (`/etc`) rejected pre-walk, symlink scan_path rejected, valid trusted-subdirectory scan_path passes the validation gate (fails further downstream only on an unrelated missing `/config` mount in this minimal harness); `POST /api/albums/reimport-disk` traversal `aldir` rejected, existing outside-root `aldir` (`/etc`) rejected with the same generic containment error as the non-existent case (existence-oracle regression check), valid in-root `aldir` passes the validation gate (same unrelated downstream `/config` limitation). All containers, images, and temp directories torn down after.
- Status: In Progress / Ready for Draft PR and Handoff to Claude.

### Claude independent final review of Wave 8 (PR #70) -- BLOCKED, not merged

Reviewed exact head `71abd47df10321ac4091b862805da831d6b2e88a` on base `main@18bf329ec9cdb728ea07e2f744e1eff41dfd27d3`, independently of Codex's own classifications.

- **PR-scoped CodeQL alerts, independently traced via the raw SARIF codeFlows (`gh api -H "Accept: application/sarif+json" .../code-scanning/analyses/{id}`), not just alert-summary text**:
  - `#529` (`py/path-injection`, `app.py:26564`, `draft_path.resolve()`): all 4 codeFlow paths trace `request` -> `payload["artist"/"album"]` -> (for two of the four) directly through `_safe_path_component()` (lines 34087-34093) -> `safe_artist`/`safe_album` -> `draft_path`. Confirmed by full trace, not precedent alone: every component reaching `draft_path` passes through the sanitizer, the flagged `.resolve()` call is read-only (no mutation), and the actual destructive `mkdir`/`write_text` calls are gated by a `_path_is_under()` re-check immediately after. **Disposition: false positive (sanitizer-recognition gap). Dismissed.**
  - `#530` (`backend/transaction_engine.py:200`) and `#531` (`app.py:24388`): both `py/clear-text-storage-sensitive-data`. The SARIF `relatedLocations` for every one of the 8 combined codeFlow paths (across both alerts) point to the exact same origin: `self.trusted` in `tests/test_sec002_app_path_ai_batch_replacement_staging.py` (lines 148/156/176/198) -- a `pathlib.Path` test fixture holding a temp directory used as a permitted root in a security test, not a credential of any kind. The query's heuristic sensitive-source detector matched the substring "trust" in the identifier name. The actual dataflow after that point is a plain filesystem path (`scan_path` -> `state["source_path"]` / `tx["summary"]`), independently confirmed by direct inspection of `_ai_batch_initial_state()` and `TransactionStore._write()` to contain no credential fields. This is also why the PR-scoped analysis only produced 3 results total (vs. 311 on `main`'s full scan): the reduced/incremental PR pass used the new test file's local variables as its traced entry points rather than the real Flask route parameters. **Disposition: false positive (test-fixture naming collision with a heuristic name-based source detector), independently proven via SARIF `relatedLocations`, not assumed from "no literal password field." Dismissed.**
- **Corrective commits made** (both narrow, test/hardening-only, no behavior change to any route contract):
  1. Renamed the `self.trusted` test fixture (`tests/test_sec002_app_path_ai_batch_replacement_staging.py`) to `self.allowed_root` so the same heuristic-name collision can't reproduce `#530`/`#531` on every future analysis of this file.
  2. Hardened `_safe_path_component()` to also strip Unicode format/control characters (category `Cf`/`Cc` -- bidi overrides such as U+202E, zero-width joiners/spaces, BOM), which the pre-existing ASCII-only `[\x00-\x1f<>:"?*|]` class did not cover. These characters could not escape `MUSIC_ROOT` (still a single sanitized path component either way, and still re-verified by `_path_is_under()`), but could render a draft folder name deceptively. Added `test_bidi_override_and_zero_width_chars_stripped_from_artist` (`UnmatchedDraftPathSafetyTests`), which fails against the pre-correction `_safe_path_component()` (the ASCII-only regex does not match U+200B/U+202E).
  - Full new test file re-verified: 18/18 pass. Full suite re-verified via Linux/Docker: 3 runs, 2 clean `OK`, 1 run had a single unrelated flaky concurrency test (`test_concurrent_recover_requests_start_exactly_one_worker`, part of pre-existing `test_ai_batch_retry_race.py`, no relation to path sanitization) that passed cleanly 3/3 in isolated reruns -- confirmed pre-existing timing flakiness under full-suite load, not a regression from these corrections.
- **AI authority -- independently re-traced, not merely re-stated from Codex**: confirmed `mb_valid` (checked at its definition sites, e.g. `bool(re.match(_MB_RE, mb_id, re.I))`) means only "this string is a syntactically well-formed MusicBrainz UUID," not deterministic audio-content verification. Confirmed `library_import_all()`'s discovery-phase auto-apply (`app.py:23538-23551`) accepts an AI suggestion into `mb_albumid` and proceeds directly to `_start_reimport_disk_job_internal` (an actual mutation) using only `conf in ("high","medium") and mb_valid` -- no preflight/deterministic-evidence gate at all. This is real, pre-existing (Wave 7 already flagged it), and unmodified by Wave 8. **However, Wave 8's own selected AI-batch-import flow does not feed this path**: `_run_ai_batch_import`'s per-folder loop calls `_ai_import_folder` directly, gated by `_ai_auto_import_allowed()`, which additionally requires `_folder_release_preflight()` to succeed (tracklist/track-count correspondence against the specific MB release, with AcoustID hit-rate fields as contributing evidence) and, at medium confidence, an 80% preflight match-ratio on top of that. This is a materially different, better-guarded code path than `library_import_all`'s bare-confidence check -- not a regression, and not something Wave 8 introduced or worsened. Whether `_folder_release_preflight`'s matching is itself strong enough (deterministic AcoustID-based vs. heuristic filename/count-based) was not traced to full certainty in this review and should be confirmed before further AI-authority hardening. `library_import_all()`'s bare-confidence auto-apply remains an open, real defect independent of Wave 8, recommended as a Wave 9 priority.
- **Architecture gate -- FAILED, this is the blocking finding**: both routes Wave 8 selected for its path-injection fix perform filesystem operations against paths (`/data/media/music`, `/data/torrents/music`) that **do not exist as writable, mounted locations inside the actual shipped `beets-web-manager` container, in any of the three documented compose topologies** (`docker-compose.yml`, `docker-compose.full.yml`, `docker-compose.dev.yml` -- verified by reading all three; none mount `MUSIC_ROOT`/`DOWNLOADS_ROOT` into the `beets-web-manager` service, and the production/full/dev variants all additionally set `read_only: true` at the container level with only `/tmp`/`/run` as writable tmpfs).
  - `create_unmatched_draft()` calls `mkdir()`/`write_text()` directly under `MUSIC_ROOT` from the web-manager process. In production (`docker-compose.yml`) this fails immediately with a read-only-filesystem error; in dev/full topologies it "succeeds" only by writing into an ephemeral path inside the web-manager container's own overlay filesystem that is not the real, host-mounted music library -- meaning the endpoint cannot reach authoritative media storage in any supported topology, security fix notwithstanding.
  - `reimport_disk()` goes further: verified by reading its ~1,250-line body (`app.py:22146`-`23404`) that it directly invokes `BEET_BIN` via `_beet_run([BEET_BIN, ...])` throughout (matching, mbsync, write, move, art repair). `BEET_BIN = shutil.which("beet") or "/lsiopy/bin/beet"` (`app.py:442`); `beets` is not in `requirements.txt`, and `/lsiopy` is the LinuxServer Beets-image virtualenv path, present only in the `beets` engine container (`Dockerfile.beets`), never in `beets-web-manager` (`Dockerfile`, plain `python:3.12-slim-bookworm`). This route depends on a local `beet` binary that is architecturally absent from the web manager, exactly the dependency `docs/AI_ENGINEERING_RULES.md`'s engine/web-manager split forbids.
  - Both issues predate Wave 8 entirely (Codex's diff only added path-validation lines; none of this body was touched). Properly fixing `reimport_disk()` requires routing its ~1,250 lines of Beets orchestration through the control agent/`BeetsClient` -- a real architecture migration, not a narrow security patch, and explicitly out of scope per this task's own "do not perform the repository-wide BEET_BIN migration" constraint. No safe narrow fix exists within this PR's scope; attempting one would either be cosmetic (fixing path validation on a route that already can't execute) or would itself become the forbidden repo-wide migration.
- **Nanoid override independently re-verified**: `npm explain nanoid` confirms `nanoid@3.3.18` (>= the 3.3.17 fix floor) resolves solely via the pre-existing `postcss@8.5.24` override (itself unchanged); the new `nanoid` override is the narrowest possible fix, no unrelated dependency drift.
- **Final Claude decision: `blocked — web-manager media mutation violates architecture`.** Both selected mutation routes are non-functional and/or architecturally invalid in every currently-documented, supported deployment topology -- this is pre-existing, not introduced by Wave 8, but it means the two routes Wave 8 chose to secure cannot actually be exercised as designed in production regardless of the path-injection fix's correctness. The path-injection fixes themselves (both genuine vulnerabilities, both correctly remediated) and the two false-positive CodeQL dismissals above are sound and should be preserved; PR #70 is not merged pending an explicit architecture decision (route these routes through the control agent, or remove/redesign them) which is beyond this review's narrow-correction authority. SEC-002 remains Open. Wave 9 not started.
- Status: Blocked pending architecture decision -- draft PR left open, not ready, not merged.

### Codex architecture-unblock of Wave 8 (PR #70) -- same PR, no new branch

Continuation of PR #70 to resolve Claude's `blocked — web-manager media mutation violates architecture` finding above. Same branch (`fix/codeql-app-path-wave8`), same PR, no Wave 9.

**Correction to the block's `reimport_disk()` claim**: the prior review's characterization -- "directly invokes `BEET_BIN` via `_beet_run([BEET_BIN, ...])` throughout" implying the whole ~1,250-line function was local-subprocess-dependent -- was broader than the facts support. Tracing `_beet_run()` itself (`job_engine.py:118`) shows it does **not** run a local subprocess at all: it calls `_parse_remote_beet_command()` to strip the leading `beet`/`BEET_BIN` executable token and any `-c <path>`/`--config <path>` pair, then sends the remaining subcommand+args to `beets_client.run_command()` -- an authenticated HTTP call to the real Beets engine's control agent (`/commands/execute`, which already allowlists `import` and every other command this function uses). Every `_beet_run(base_std + [...])` call in `reimport_disk()` (repair-in-place mbsync/write/move, post-import mbsync/write/rename, modify/mbsync/write/rename-by-item, fetchart/embedart) was **already architecture-compliant**. DB access in the same function (`with _db() as con: ...`) is also already compliant -- `_db()` yields a `RemoteSQLiteConnection`, never a local `sqlite3.connect`. The one genuine violation was a single, isolated call: the core "tag & import in-place" step at the old `app.py:22907` used `subprocess.run(base_tmp + ["import", ...])` directly, bypassing `_beet_run()` entirely -- likely a leftover from before the control-agent split (`import` needed real config content, and `_beet_run()`'s `-c` stripping silently discards local config-file content with no way to forward it, which this call site never had a substitute for until now).

- **`reimport_disk()` fix**: replaced the one direct `subprocess.run(base_tmp + ["import", ...])` call with `_beet_run(base_tmp + ["import", ...], log, timeout=import_timeout, cancel=cancel_event, config_override=temp_cfg_content)`. Added a `config_override: str = ""` parameter to `_beet_run()` (`job_engine.py`) -- forwarded to `beets_client.run_command()`'s existing (previously always-empty) `config_override` argument, which the control agent already writes to its own temp YAML and passes as `-c` to its *local* `beet` invocation. This is the mechanism that lets `import`'s `copy: no / move: no / duplicate_action: ...` policy (essential -- without it Beets could copy/move files, causing data loss/duplication) and the shared `lyrics: auto: no / replaygain: auto: no` guards (preventing unwanted network calls during write/mbsync) actually reach the engine, instead of being silently dropped the way every pre-existing `-c <path>` token in this function already was. Applied `config_override=temp_cfg_content` (and `repair_cfg_content` for the separate `_repair_existing_album_in_place` closure's own temp config) to all `_beet_run(base_std/base_tmp + ...)` calls in the function for consistency, not just the one that was blocking. Timeout-preservation: the control agent maps a subprocess timeout to HTTP 408 with `"...timed out after Xs"`; the client wraps it as a plain `BeetsError`, which `_beet_run` returns as `_R(1, "", str(exc))` -- `reimport_disk()` now detects `"timed out" in r.stderr.lower()` to preserve its original "treat timeout as inconclusive, verify via the DB afterward" behavior (`import_timed_out`) instead of a hard failure.
  - `import`'s server-side root policy (`allowed_roots = ["staging"] if command == "import" else ["music", "staging"]` in `backend/beets_control_agent.py`, applied only to the dedicated `source_path` request field) turned out not to need any change: `_beet_run()` never populates `source_path` -- the folder argument travels inside `args` like every other command's arguments already did, validated instead by `_sanitize_command_path_args(args, ["music", "staging", "config", "tmp"])`, which already permits `"music"`. This covers both of `reimport_disk()`'s scenarios (already-in-library untracked audio, and staged/downloaded audio) without loosening the `import`-specific `source_path` policy that guards other callers (e.g. AI batch import, which should stay staging-only).
  - No repository-wide `BEET_BIN` migration performed. `BEET_BIN`/`_beet_run` call sites outside `reimport_disk()` are unchanged and out of scope; a full inventory is a Wave 9 candidate (see below).
- **`create_unmatched_draft()` fix**: this route only ever wrote review/submission metadata (`draft.json`, `musicbrainz_submission.txt` -- artist/album/year/source_url/tracks/status, no audio file placed or referenced) -- confirmed by reading the full route body and confirming nothing else in the backend or frontend reads these files back to drive any import (`grep` across `app.py`, `routes_*.py`, `backend/`, `tests/`, and `frontend/src` found zero other references; the frontend does not currently call this endpoint at all). Per the "prefer web-manager state unless MUSIC_ROOT placement is actually necessary" guidance, moved the write target from `MUSIC_ROOT` to a new `UNMATCHED_DRAFT_ROOT = Path(os.environ.get("UNMATCHED_DRAFT_DIR", "/web-manager-data/unmatched_drafts"))` -- `/web-manager-data` is the one path every shipped Compose file actually mounts into `beets-web-manager` (used already for `.auth_token`, `last_scan.txt`, `.plex_client_identifier`). No engine-side capability needed; this is not authoritative media, so it was not moved behind the control agent. If a draft is later actually imported, that goes through `reimport_disk()`, which owns `MUSIC_ROOT` and validates its own identity evidence at that time -- this route only ever prepares review metadata.
  - Added an explicit collision policy: re-submitting the same `(artist, album)` is idempotent; a different pair that sanitizes to the same folder name (e.g. `"A/B"` vs. `"A\B"`, both becoming `A_B`) now returns `409` instead of silently overwriting the earlier draft.
  - Added recursive secret redaction (`_redact_unmatched_draft_tracks()`, reusing the existing `_redact_security_text()` helper already used for confirmation-reason sanitization elsewhere) over `tracks` and `source_url` before they can reach persisted JSON/text -- `tracks` is client-controlled free-form JSON and was previously written through unfiltered field-for-field; it is now projected down to a fixed `{title, duration}` shape with `_redact_security_text()` applied to each, and any extra field (e.g. a stray `api_key`/`authorization` value) is dropped outright rather than merely redacted in place.
  - API response shape preserved (`ok`, `local_id`, `path`, `album_folder`, `submission_file`) since nothing consumes it today; `path`/`submission_file` now honestly point at `UNMATCHED_DRAFT_ROOT`, not a fabricated `MUSIC_ROOT` location.
- **Tests**: `tests/test_sec002_wave8_engine_ownership.py` (new, 7 tests) -- source-level guards that `reimport_disk()` never calls `subprocess.run`/`Popen`/`call` directly and that its import step routes through `_beet_run(..., config_override=...)`; that `create_unmatched_draft()`'s code (excluding its explanatory docstring) never references `MUSIC_ROOT`; behavioral tests that `_beet_run` forwards `config_override` and strips the local `-c` token/executable correctly; that a remote 408 timeout is surfaced via `"timed out"` in `stderr` rather than silently swallowed; and -- the direct regression proof for the architecture block -- that the import step succeeds via the mocked remote client even when `BEET_BIN` points at a path that does not exist anywhere on the machine (`subprocess.run` mocked to raise `AssertionError` if ever called), reproducing the real web-manager container. `tests/test_sec002_app_path_ai_batch_replacement_staging.py`'s `UnmatchedDraftPathSafetyTests` updated: patches `UNMATCHED_DRAFT_ROOT` instead of `MUSIC_ROOT`, adds `test_music_root_is_never_touched` (asserts a separately-mocked `MUSIC_ROOT` stays completely empty across both a normal and a traversal-attempt submission -- the architecture assertion, not just containment), plus new collision (`test_different_draft_mapping_to_same_folder_is_a_collision`, `test_same_draft_resubmitted_is_idempotent`) and secret-redaction (`test_secret_like_track_fields_are_not_persisted`) tests. All prior path-injection/traversal/symlink/Unicode assertions preserved, not weakened.
- **Verification**: focused suite (new files + `test_ai_batch_retry_race`, `test_ai_batch_import_reliability`, `test_import_auto_enqueue`, `test_music_format_replacement_retry_pipeline`, `test_external_beets_architecture`, `test_transaction_engine`, `test_no_owner_specific_deployment_details`): 375 tests, 5/5 consecutive `OK`. Full suite via Linux/Docker: 3 runs, 2 clean `OK` (1865 tests), 1 run with the same single pre-existing flaky concurrency test noted in the prior review section (passed 3/3 in isolated reruns). Frontend: `npm ci`/`typecheck`/`lint`/`test` (15/15)/`build` clean, `npm audit --audit-level=high` 0 vulnerabilities (also fixed a duplicate `nanoid` key in `overrides{}` left by the earlier `git merge --no-ff origin/main` of PR #71's independent identical fix, and regenerated the lockfile on Linux). Static/security: `py_compile`, secret scan, `validate_compose_security.py`, `verify_security_config.py` (synthetic values) all pass; `git diff --check` clean. Privacy/compose: `test_no_owner_specific_deployment_details` 12/12 pass; zero diff to any of the three Compose files (confirms no media mount was added to unblock this); all three parse cleanly.
- **Mandatory disposable two-service runtime test -- surfaced a second, deeper, pre-existing architecture gap `reimport_disk()` still cannot cross.** Ran a real two-container stack: `beets-web-manager` with no media/staging mount and no local `beet` binary at all (confirmed: `which beet` and `ls /lsiopy` both fail; `ls /data` fails -- reproducing every shipped Compose topology exactly), talking over the network to a real `beets` engine container with genuine `MUSIC_ROOT`/staging mounts, a real `beet` binary, and the control agent. `create_unmatched_draft()` worked correctly end-to-end (files land under `/web-manager-data/unmatched_drafts`, the mocked `MUSIC_ROOT`-equivalent stayed empty). `reimport_disk()` did **not**: calling it with a real staged folder that only the engine can see returned `{"error": "Source path does not exist.", "ok": false}` -- the engine's own logs confirm the request never even reached `/commands/execute`; it failed inside `reimport_disk()`'s own pre-existing (not modified by this fix) web-manager-side call to `_resolve_import_review_source_path()`, which does `candidate.exists()`/`is_symlink()` checks against the *web manager's own filesystem* -- and the web manager has no local view of `/data/torrents` or `/data/media/music` in any shipped topology, so that check can never succeed for a real path. Reading further into `reimport_disk()` shows this is not an isolated line: `_validate_import_source_audio()` (called immediately after) calls `_validate_audio_tree_preferences()`, which inspects each audio file's actual format/bitrate -- also requiring local file reads the web manager cannot perform. So the BEET_BIN fix above is necessary but not sufficient: it addresses execution (once the engine receives an import command, it now runs there, not locally), but `reimport_disk()` still cannot be reached with a real staged path today, because its own pre-invocation validation and audio inspection assume local filesystem access this container has never had in the shipped topology.
  - This is exactly the debt already named `ARCH-003` in `docs/ARCHITECTURE.md` ("Residual direct filesystem moves, deletes, copies, and directory removals in app.py. Under the supported Compose files the web-manager image is not the media owner, so these call sites need ARCH-003 controlled-boundary cleanup") -- confirmed here to be broader and more load-bearing than a documentation note: `_resolve_import_review_source_path()` and `_validate_audio_tree_preferences()` are shared by the entire Wave 6/7/8 import-review/AI-suggest/reimport surface, not just these two routes. A prior disposable-runtime pass in this same PR's history validated `reimport_disk()`/`start_ai_batch_import()` against a single container with media bind-mounted directly into the web-manager image for convenience -- which inadvertently tested a topology that does not match any shipped Compose file and masked this exact gap. This pass deliberately did not repeat that mistake.
  - **Not fixed here.** A safe fix requires an actual architecture decision, not a narrow patch: either (a) move existence/symlink/audio-format validation to the engine (a new engine capability, since `resolve_safe_path()`/`_sanitize_command_path_args()` in `backend/beets_control_agent.py` already do real, authoritative containment+symlink checking server-side for the args that reach `/commands/execute` -- meaning the web-manager's client-side pre-check may be functionally redundant defense-in-depth for THIS route rather than a distinct security boundary, but that needs to be confirmed deliberately, not assumed under time pressure), or (b) have the web manager query the engine for path/audio metadata before proceeding, or (c) something else product/architecture wants. Attempting a narrow patch (e.g. `require_exists=False`) without also addressing `_validate_import_source_audio()`'s local audio-format reads would still leave the route non-functional, and loosening a check shared by other callers (e.g. AI-batch scan, which genuinely does need real local access for its own `os.walk()`) risks weakening it elsewhere for the wrong reason.
- **Remaining debt, explicitly not resolved by this unblock** (kept for Wave 9, `SEC-002 Status: Open`):
  - The `reimport_disk()` local-filesystem-access gap above -- recommended as the next priority, likely larger than a normal wave slice.
  - `library_import_all()`'s bare-confidence (`conf in ("high","medium") and mb_valid`, no preflight/deterministic-evidence gate) auto-apply pathway -- real, pre-existing, Wave 7 and the Claude review both flagged it, still unmodified.
  - Whether `_folder_release_preflight()`'s track-matching (used by Wave 8's own, better-guarded AI-batch auto-import gate) is itself deterministic (AcoustID-based) or heuristic (filename/count-based) enough was not traced to full certainty.
  - Repository-wide `BEET_BIN`/local-subprocess inventory outside `reimport_disk()` -- not touched by this fix, still open across the rest of `app.py`.
  - AI-batch resource limits (unbounded folder count from `_ai_batch_find_audio_dirs`'s `os.walk()`, no cross-batch concurrency cap, no cancellation checkpoint during the initial walk) -- flagged by the Claude review, not addressed here (out of scope for the architecture unblock specifically).
  - Historical Wave 8 cluster members deferred since the original Wave 8 entry above (`_mark_pending_review_status`, `start_import`/`import_preflight`, `dedup_scan`/`dedup_cleanup`, `_mb_release_tracklist_*`, plus the adjacent `#417`-`#421`/`#480`-`#484` clusters).
- **New PR-scoped CodeQL alerts from this commit, not dismissed (Codex may not dismiss)**: `#532`/`#533` (`py/path-injection`, `app.py:26653`/`26655`, both in the new collision-check code -- `existing_draft_json.exists()`/`.read_text()`). Full SARIF codeFlow traced: both originate from the same `artist`/`album` -> `_safe_path_component()` -> `draft_path` chain as the already-dismissed `#529`, reaching `existing_draft_json = draft_path / "draft.json"` (`draft_path` at that point is already `draft_path_resolved`, re-verified by `_path_is_under()` a few lines earlier). Same established sanitizer-recognition gap, same root cause, no new risk -- recommended for Claude to independently confirm and dismiss using the same reasoning as `#529`.
- Status: `create_unmatched_draft()`'s architecture violation is fully resolved and verified end-to-end. `reimport_disk()`'s specific BEET_BIN/local-subprocess violation is fixed and verified, but a second, deeper, pre-existing gap in the same route (local path/audio validation assuming filesystem access the web manager does not have) means it still cannot be exercised end-to-end against a real staged folder in the shipped topology, and resolving that requires an architecture decision beyond this task's narrow-correction scope. Draft PR left open, not ready, not merged, pending Claude's second independent review.

### Codex ARCH-003 fix for reimport_disk() -- same PR, no new branch

Continuation of PR #70 to resolve the deeper gap the previous entry found. Same branch (`fix/codeql-app-path-wave8`), same PR, no Wave 9.

- **Root cause confirmed**: `reimport_disk()`'s own `_resolve_import_review_source_path()` call validated existence/containment/symlink-safety against the *web manager's own filesystem* -- which has no view of `MUSIC_LIBRARY_PATH`/`DOWNLOAD_PATH` in any shipped Compose topology. That responsibility now belongs to the Beets engine, the only place those roots are actually mounted.
- **Fix**: new engine-side `POST /imports/source/inspect` (`backend/beets_control_agent.py`, `inspect_import_source()`) -- reuses the engine's own `resolve_safe_path()` (full `realpath()` resolution then containment, which closes symlink escapes without a separate explicit check) against a fixed, operation-specific root policy (`IMPORT_SOURCE_OPERATIONS`: `"reimport"`/`"ai_batch_discovery"` -> `["music", "staging"]`; the caller names an operation, never its own trusted root), rejects root-self, then does a bounded (`IMPORT_INSPECT_MAX_ENTRIES_SCANNED`=20000, `IMPORT_INSPECT_MAX_AUDIO_FILES`=500, both env-overridable and clamped) walk that never follows symlinked subdirectories out of the trusted root, calling `audio_preferences.inspect_audio_file()` (real ffprobe-based inspection, `/usr/bin/ffprobe` confirmed present in the engine image) per audio file. Returns `canonical_path`, bounded `audio_files` evidence, and a `source_signature` (staleness fingerprint, explicitly documented as not recording identity) -- never raw file contents or host-path internals beyond the one validated canonical path.
  - `audio_preferences.py` is a portable, stdlib+ffprobe-only module with no Flask/app.py coupling, so it's copied into the engine image alongside the control agent (`Dockerfile.beets`) rather than reimplemented; `beets_control_agent.py` imports it with a try/except fallback (`import audio_preferences` when run standalone in the deployed image; `from backend import audio_preferences` when imported as `backend.beets_control_agent` for testing) so the file keeps working both ways.
  - New `BeetsClient.inspect_import_source()`. `reimport_disk()` now calls it instead of the old local validator, and only ever reuses the engine's own `canonical_path` afterward -- the raw client-supplied `aldir` is never reused past that point.
  - New `_validate_import_source_evidence()` (web-manager side): the same accept/reject policy as `_validate_import_source_audio()`, reusing the same underlying pure `validate_audio_properties()` decision function, just fed the engine's evidence instead of a local `ffprobe`/`rglob` pass. Used by `reimport_disk()` only -- `_validate_import_source_audio()`'s other 6 call sites (unrelated routes) are untouched. `_beet_import_timeout()` similarly gained a `_beet_import_timeout_for_count()` variant fed by the engine's `audio_count` instead of a local file count.
- **Verified via the mandatory disposable two-service runtime test** (real engine with real `MUSIC_LIBRARY_PATH`/`DOWNLOAD_PATH` mounts and a real `beet`; web manager with no media mount, no `beet`, no `/lsiopy` -- confirmed live): the "already in library, needs cataloging" reimport scenario (the primary documented use case; `aldir` under the engine's music root) now runs end-to-end past validation -- the job correctly receives the engine-validated canonical path, proceeds through real MusicBrainz release-group lookup and text-search fallback (both legitimate, pre-existing, unrelated logic), and safely queues for manual review when no candidate's tracklist matches (exactly the correct deterministic-identity-required behavior; it does not import against a wrong release). This is the concrete failure originally reproduced ("Source path does not exist") and it is fixed.
- **Two additional gaps found by the same test, not fixed in this pass**:
  1. **Torrent/download-staged (not-yet-in-library) reimport scenario**: when `aldir` is under a staging root rather than the music library and `TORRENT_SOURCE_MOVE_ALLOWED` is false (the default), `_preserve_torrent_source_path()` routes through `_stage_preserved_torrent_source()` -> `_audio_files_in_dir()`, which still does a local directory listing to copy files into a protected staging location before import -- so it currently fails with "No audio files found in torrent source folder" for a source the web manager cannot see. This is a genuine, separate local-filesystem mutation dependency (copies real file bytes, not just a read), not merely a validation check, and migrating it needs a new engine-side staging/copy capability comparable in size to the AI-batch discovery work below -- deferred, not silently accepted as fixed. The "already in library" scenario above is unaffected (it never reaches this code path).
  2. **`TransactionStore` default storage location**: unrelated to media/staging paths, but also surfaced by this test -- `backend/transaction_engine.py`'s `TransactionStore` defaults to `/config/transactions`, and `/config` is not mounted into `beets-web-manager` in any shipped Compose file either (only `/web-manager-data` is). Every job that goes through `start_python_with_transaction` (not just `reimport_disk()`) would hit this in real production. This is a distinct pre-existing bug from ARCH-003 (general web-manager local state, not engine-owned media), well outside this task's scope; flagged here because the runtime test could not otherwise proceed without a workaround (a temporary writable `/config` mount used only for this disposable test, never for production).
- **AI-batch discovery migration: not attempted in this pass.** `_ai_batch_find_audio_dirs()` still uses a local `os.walk()` and remains architecturally broken in the shipped topology for the same underlying ARCH-003 reason. `IMPORT_SOURCE_OPERATIONS` already includes an `"ai_batch_discovery"` root policy in anticipation of this, and `inspect_import_source()`'s bounded-walk logic is close to what a recursive-candidate-discovery endpoint would need, but AI-batch discovery needs a genuinely different operation (enumerate multiple candidate album directories across a whole tree, not inspect one already-known directory) with its own pagination/resource-limit contract (task explicitly calls for `max_directories`/`max_files`/`max_candidates`/cursor-based continuation) -- a comparably-sized separate piece of engineering that was not started, given the scope already delivered in this pass. `start_ai_batch_import()`/`ai_batch_import_recover()`'s own path *validation* (via `_resolve_import_review_source_path()`, SEC-002 Wave 8's original fix) is unaffected by this and still correctly rejects traversal/outside-root/symlink scan_path values -- what's missing is that the *walk itself*, once past validation, still cannot see engine-owned content from the web manager.
- Tests: `tests/test_sec002_wave8_engine_ownership.py` gained `ImportSourceInspectionTests` (11 tests, real temp directories and real symlinks, `@unittest.skipUnless(os.name == "posix", ...)` since `resolve_safe_path()` requires POSIX-absolute paths and the control agent only ever runs inside the Linux engine container) covering unknown-operation/outside-root/nonexistent/traversal/root-self/NUL rejection, valid staging and music directories, symlink-directory-escape non-traversal, source-signature change detection, and the `ai_batch_discovery` operation's music-root permission. `tests/test_sec002_app_path_ai_batch_replacement_staging.py`'s `ReimportDiskPathSafetyTests` rewritten to verify `reimport_disk()`'s handling of engine rejection/unavailability/success responses (mocked `beets_client.inspect_import_source`) instead of asserting on the now-removed local validator. `tests/test_music_format_preferences_integration.py`'s `test_import_paths_call_central_audio_validation` updated to expect `reimport_disk()`'s deliberate exception (`_validate_import_source_evidence`) rather than the shared local-scan validator, without weakening the assertion for the other 6 unaffected call sites.
- Verification: focused suite (198 tests: Wave 8, engine-ownership, music-format-preferences-integration, import-auto-enqueue, replacement-retry-pipeline, control-agent, AI-batch-retry-race, transaction-engine, privacy) 5/5 consecutive `OK`. Full suite via Linux/Docker: 3/3 consecutive clean runs, 1878 tests, `OK (skipped=69)`. Both images rebuilt `--no-cache` on the exact final head; OCI revision labels exact on both; `verify_beets_engine_image.py --check-s6-supervision` passed; confirmed `audio_preferences.py` and `/usr/bin/ffprobe` present in the engine image.
- Status: `reimport_disk()`'s originally-reported failure mode ("Source path does not exist" for any real path) is fixed and verified for its primary documented scenario. Two further, smaller gaps (torrent-staged local copy, unrelated `TransactionStore` default) and the full AI-batch discovery migration remain open, explicitly not claimed as resolved. SEC-002 remains Open. Draft PR left open, not ready, not merged.
- **New PR-scoped CodeQL alerts from this commit**:
  - `#532`/`#533` (`py/path-injection`, `app.py:26750`/`26752`, `draft_path.resolve(strict=False)` inside `create_unmatched_draft()`'s containment check): same established sanitizer-recognition gap as the already-dismissed `#529` (line numbers shifted from earlier edits to this same function) -- every component reaching `draft_path` still passes through `_safe_path_component()`, and this `.resolve()` is still read-only, still re-verified by `_path_is_under()` immediately after. Recommended for Claude to dismiss using the same reasoning as `#529`; Codex does not dismiss.
  - `#534` (`py/stack-trace-exposure`, `app.py:22293`, now fixed rather than dismissed): the new `except BeetsError as exc: ... f"Import source rejected: {exc}"` line interpolated the exception text directly into the HTTP response. `BeetsClient._request()`'s engine-error path normally wraps one of `inspect_import_source()`'s four fixed `error_code` strings, but falls back to embedding up to 200 raw response-body characters for any error response it doesn't recognize as JSON -- a real, if narrow, path for stack-trace-shaped text to reach the client. Fixed by mapping only the known error codes to a fixed safe message, collapsing anything else to one generic string, never interpolating `exc` itself. New regression test (`test_unrecognized_engine_error_never_leaks_raw_response_text`) asserts a synthetic embedded traceback never appears in the response, and fails against the pre-fix code.

### Claude independent review of a user-authored ARCH-003 completion commit -- same PR, no new branch

A subsequent commit (authored directly, not through the Codex/Claude review loop) added engine-side AI-batch discovery (`discover_import_sources`), torrent-source preservation (`preserve_import_source`), and an atomic signature/identity-bound reimport operation (`reimport_source_atomic`), plus wired `_ai_batch_find_audio_dirs()` and `_stage_preserved_torrent_source()` to use the first two. Independent review (not accepting the commit message's claims at face value, per instruction) found several genuine defects, all corrected in place rather than reverted:

1. **Pagination cursor was unsound, not merely inelegant.** The original cursor was a lexicographic "resume after this relative path" string marker. `/` (0x2F) sorts *above* space, `-`, and `.` (0x20/0x2D/0x2E) -- extremely common in real album/artist folder names ("Artist - Album") -- so a sibling directory named e.g. `"Parent-suffix"` can sort *before* `"Parent/child"` as a string while still being visited *after* it in the stack-based DFS traversal order. Resuming by skipping every `rel_dir <= marker` then silently dropped that sibling's entire subtree: real, silent candidate loss for common real-world names, proven with a constructed adversarial tree (`A/Z`, `A-suffix`, `AA`, `B/A`, `Z/A`), not a theoretical concern. **Fixed** by encoding the actual remaining DFS stack in the cursor (root/operation bound in for correctness, not as a security boundary -- `source_path` is independently re-validated via `resolve_safe_path()` on every call regardless of cursor content, so a tampered cursor can never grant access beyond what the caller was already authorized to discover). The file-count-limit break was also fixed to defer a directory *whole* rather than partially, for the same reason (partial-directory pushes plus a stack-encoded cursor could otherwise duplicate or drop entries at the boundary).
2. **`discover_import_sources()` rejected its own primary use case.** It copied the root-self rejection from `inspect_import_source()`/`preserve_import_source()`/`reimport_source_atomic()` (correct there -- those operate on one candidate album, where "the whole trusted root" is nonsensical), but discovery's entire purpose is scanning a whole root for multiple candidates, and `start_ai_batch_import()`'s default `scan_path` *is* the trusted staging root itself. Confirmed by writing the pagination test above and watching every call fail with `root_self_rejected`. **Fixed** by removing the check from `discover_import_sources()` only.
3. **`verify_deterministic_identity()` failed open on absent evidence**, not just on a missing DB row: no `expected_identity` argument, an empty `expected_identity` dict, and -- most importantly -- a `target_mb_albumid`/`target_mb_rgid` expectation where the source's audio simply had *no* embedded MusicBrainz tags to check, all previously returned `{"ok": True}`. A DB lookup exception for `existing_album_id` was also swallowed (`except Exception: pass`) and fell through to success. **Fixed**: every one of these now returns `review_required` (missing evidence) or `identity_verification_failed` (DB error) instead of `ok: True`; a real mismatch still returns `identity_mismatch`. Both are distinct error codes so callers/UI can present them differently ("we could not confirm" vs. "we confirmed it does not match").
4. **`reimport_source_atomic()` identified the imported album by "most recently inserted row"** when a lookup by `mb_albumid` failed, which can attribute the wrong album under concurrency or an unexpected Beets resolution. **Fixed**: falls back only to a lookup by the caller's own already-identity-checked `existing_album_id`, never a bare `ORDER BY id DESC LIMIT 1`; if neither resolves, the result reports `"album_id": null, "album_id_verified": false` rather than a guess.
5. **`reimport_source_atomic()` used `--move` for the non-preserved (already-in-library) scenario**, forcing Beets to relocate/rename already-correctly-placed files according to its path template -- a real behavioral regression from `reimport_disk()`'s own established `copy: no / move: no` in-place semantics for that exact scenario. **Fixed**: `--copy` is used only for the preserved-staging branch (a disposable protected copy that specifically exists to be organized into the library); the non-preserved branch now defaults to an explicit `copy: no / move: no` config override when the caller didn't supply their own.
6. **Unvalidated `beets_options`**: `config_override` (an arbitrary caller-supplied YAML blob written directly to a config file Beets loads) had no length bound, `duplicate_action` accepted any string forwarded straight to `beet import -D`, and `mb_albumid` wasn't checked as a real UUID before being used in a search. **Fixed**: `mb_albumid` validated against the existing MB-UUID pattern, `duplicate_action` restricted to Beets' actual `{skip, keep, remove, merge}` values, `config_override` capped at 8192 characters.
7. **Raw exception/subprocess output reached HTTP responses.** `preserve_import_source()`'s exception handler embedded `str(ex)` (potentially host paths/OS error text) in its `message`; `reimport_source_atomic()` returned raw Beets `stdout`/`stderr` on both its failure and success paths. **Fixed**: every `message` in both functions is now either a fixed string or interpolates only caller-supplied identifiers (MBIDs, integers) that were already present in the request; `stdout`/`stderr` are no longer returned at all.
8. **`preserve_import_source()` allowed `"music"` as a source root**, despite existing specifically to protect torrent/download sources from Beets mutating them during import -- library content never needs this operation. **Fixed**: staging-only (least privilege).
9. **Critical, unresolved by this review: `reimport_source_atomic()`/`verify_deterministic_identity()` are not called by production.** `grep -n "reimport_source\b" app.py` returns zero matches outside `backend/beets_client.py`'s own method definition -- `reimport_disk()`'s actual mutation still runs through the pre-existing `_beet_run()` -> `/commands/execute` path (fixed for local-BEET_BIN dependency in an earlier commit, but with no signature re-check or deterministic-identity verification immediately before the mutation itself). The source-signature check `_stage_preserved_torrent_source()` forwards *does* protect the preservation-copy step for torrent-staged sources, and `inspect_import_source()`'s validation *does* run before any mutation -- but the atomic, identity-verified, signature-bound mutation path this commit built exists only as unreachable infrastructure today. Claude did not wire it into `reimport_disk()` in this review: doing so safely requires reconciling it with `reimport_disk()`'s existing branching (the separate `_repair_existing_album_in_place()` mbsync/write/move path for existing albums vs. a fresh `beet import`), its established timeout/`import_timed_out` semantics, and its multi-step "find album in DB, match tracks, write tags, rename" logic that runs *after* import today -- enough independent risk to a real, in-production mutation path that rushing it under review time pressure would be irresponsible. This is recorded as the reason PR #70 is not approved in this pass.
- New tests: `DiscoverImportSourcesPaginationTests` (6, real adversarial-tree/symlink/cursor-tamper coverage), `VerifyDeterministicIdentityFailClosedTests` (9, no filesystem needed), `PreserveImportSourceRootPolicyTests` (5, real copy/collision/idempotency coverage). All added to `tests/test_sec002_wave8_engine_ownership.py`.
- Status: the fixes above are real, verified, and safe to ship as improvements to the engine-side discovery/preservation infrastructure and to close the AI-batch `os.walk()` gap (which *is* wired into production via `_ai_batch_find_audio_dirs()`). The atomic reimport/identity-verification path remains unused by production and is not claimed otherwise. SEC-002 remains Open. Wave 9 not started.

### Codex: wire production `reimport_disk()` to the reviewed atomic engine mutation -- same PR, no new branch

Resolves Claude's remaining blocker (`reimport_source_atomic()`/`verify_deterministic_identity()` built but unreachable from production). `reimport_disk()`'s actual Beets mutation now runs through `beets_client.reimport_source()` -> `POST /imports/reimport` -> `reimport_source_atomic()`, not a bare `_beet_run(... "import" ...)`.

1. **Evidence selection is branch-dependent, by design, not by omission.** `expected_deterministic_identity` is built from the strongest evidence `reimport_disk()` already has at the mutation point:
   - `existing_album_id` present (repairing/re-tagging an already-known album): passes `{"existing_album_id": existing_album_id}` only -- deliberately *not* also `mb_albumid`. `verify_deterministic_identity()`'s `existing_album_id` check only objects if the source's *embedded* tags actively conflict with the DB row; absence of embedded tags (the common case for a folder being repaired) is not a conflict, so this branch succeeds for its ordinary case without weakening the check.
   - No `existing_album_id` (brand-new folder): passes `{"mb_albumid": mb_albumid}`, the only concrete deterministic claim available. If the source has no embedded MusicBrainz tags -- the common case for freshly downloaded, not-yet-tagged audio -- the engine correctly returns `review_required` rather than importing on trust. **This is a deliberate, real behavior change**: a from-scratch import of untagged audio that previously proceeded automatically (trusting Beets' own `asis --search-id` matching alone) now requires embedded source tags or an existing DB row to import without manual review. AcoustID evidence *is* already gathered by `_folder_release_preflight()` for this same branch, but the engine has no AcoustID/network capability to independently re-verify it at the mutation boundary, so it is intentionally not passed as identity evidence -- using it would mean trusting the caller's own claim exactly the way the original fail-open bug did. Not resolved in this pass; recorded as remaining debt below.
2. **Torrent-staged sources are preserved exactly once, not twice.** `_stage_preserved_torrent_source()` (the up-front preservation call `reimport_disk()` used to make before its own mutation) is removed entirely -- `reimport_source_atomic()` already detects a torrent-staged source and preserves it internally, immediately before the same import it protects. Calling both would have silently double-copied the source into `.preserved_staging`. Confirmed via `grep`: `reimport_disk()` no longer references `beets_client.preserve_import_source` or `_preserve_torrent_source_path(aldir)` at all.
3. **Album lookup after import no longer guesses.** The old three-strategy heuristic (path-prefix match, "recently added" timestamp heuristic, album-name search) is replaced by the atomic endpoint's own `album_id`/`album_id_verified`/`album_lookup_failed` fields (looked up server-side by `mb_albumid`, the same key used for `--search-id`). If the result isn't verified, `reimport_disk()` now raises rather than falling back to a heuristic guess.
4. **Timeout regression fixed engine-side.** `reimport_source_atomic()`'s Beets subprocess previously had a flat 180s timeout regardless of album size -- a real regression against `reimport_disk()`'s own `_beet_import_timeout_for_count()` (300s-1200s, scaled by track count). Added `_reimport_timeout_for_count()` to `backend/beets_control_agent.py`, computed from the engine's own freshly re-inspected `audio_count` (never trusted from the caller), mirroring the web-manager formula exactly. `reimport_disk()`'s HTTP client timeout is set to the same scaled value plus a margin so the client doesn't give up before the engine's own bound could elapse.
5. **Cancellation semantics unchanged, not regressed.** Traced `_beet_run()`'s `cancel=cancel_event` parameter: it is accepted but never referenced in `_beet_run()`'s body -- the pre-existing architecture already has no way to interrupt an in-flight remote Beets subprocess mid-execution; cancellation is cooperative, checked *between* steps only. `reimport_disk()` now checks `cancel_event.is_set()` immediately before calling `beets_client.reimport_source()`, matching the same cooperative-only guarantee the old `_beet_run(import...)` call actually had (not the stronger guarantee its `cancel=` parameter's name implied).
6. **Soft-timeout recovery preserved.** The old code treated a CLI-reported import timeout as inconclusive rather than a hard failure, since the mutation might have completed despite the process exceeding its wall-clock budget, and checked the DB directly afterward. `reimport_source_atomic()`'s own timeout returns a hard `import_failed`; `reimport_disk()` now detects the "timed out" message and performs an equivalent direct `mb_albumid`-keyed DB lookup (the web manager has its own authoritative read access to the same Beets library DB) before treating it as a real failure.
7. **Review-routing, not raw failure, for identity/staleness rejections.** `stale_source`, `identity_mismatch`, `review_required`, and `identity_verification_failed` responses are routed through the existing `_maybe_queue_review()` mechanism with a fixed, honest per-code message, instead of surfacing the engine's raw error code as an unhandled failure.
8. **`_delete_if_already_in_library()`'s phrase-detection input is now always empty.** The atomic endpoint deliberately never returns raw Beets stdout/stderr (an intentional SEC-002 sanitization, not an oversight), so the old "beet reported this as a duplicate" text-matching signal no longer exists. Traced `_delete_if_already_in_library()` itself first: it already resolves paths against the web manager's own local filesystem view, which has no view of engine-owned download paths in the shipped topology -- it was already an inert no-op in production before this change, so passing it an empty string changes nothing observable.
9. **Tests**: `ReimportSourceAtomicProductionPathTests` (real, non-mocked-filesystem coverage of the already-in-library `copy:no/move:no` path, the torrent-staged single-preservation path, and engine-side timeout scaling, against a real temporary sqlite library DB), `ReimportTimeoutScalingTests` (pure bounds), `ReimportDiskSinglePreservationTests` (source-level: the removed helper stays removed, no direct preserve call), two new `VerifyDeterministicIdentityFailClosedTests` cases proving `existing_album_id`-alone evidence succeeds without embedded tags and correctly conflicts when the DB's own current `mb_albumid` disagrees with embedded source tags, and an updated `ReimportDiskNoLocalSubprocessTests` case asserting the old `_beet_run(... "import" ...)` pattern is gone and `beets_client.reimport_source(` is present with signature/identity/config_override forwarding. Full suite: 1920 tests, 3/3 consecutive clean runs on Linux, `OK (skipped=69)`.
- **Remaining debt, explicitly not resolved by this pass**:
  - AcoustID evidence gathered during preflight cannot be independently re-verified by the engine (no network/API-key access there) and is therefore not usable as engine-side deterministic identity evidence. A from-scratch import of untagged staged audio with no existing DB row will correctly, and more conservatively than before, require manual review.
  - The post-import pipeline (`_match_tracks_from_mb`, AcoustID validation for `wanted_tracks`, merge-into-existing-album logic, mbsync/write/move/art) was deliberately left structurally unchanged, fed by a single-element `album_ids = [aid]` list from the atomic endpoint instead of the old heuristic's (occasionally multi-element) result -- not independently re-audited in this pass beyond confirming it still receives the shape it expects.
  - `_delete_if_already_in_library()` remains dead/inert code in the current shipped topology (pre-existing, not introduced here); not removed, since it is out of scope for this specific unblock.
- Status: production `reimport_disk()` no longer bypasses source-signature and (where real evidence exists) deterministic-identity verification at its actual mutation point. SEC-002 remains Open pending Claude's independent final review of this wiring. Wave 9 not started. PR #70 left draft/unmerged -- Claude retains final merge authority.

### Claude final merge review of the production reimport wiring -- two critical findings, both fixed -- same PR, no new branch

Independent review did not accept green tests/CI as proof this wiring was safe or functional. Two genuine, serious defects were found via live, non-mocked adversarial testing against the actual engine code and a real `beet` binary -- neither was reachable by the mocked-subprocess unit tests added in the prior round, which is exactly why this round tested outside the mocks.

1. **CRITICAL, exploitable: `existing_album_id` alone authorized importing ANY trusted source into ANY existing album, with zero content verification.** `verify_deterministic_identity()`'s `existing_album_id` branch only queried `SELECT id FROM albums WHERE id = ?` -- proving the DB row *exists*, never that the supplied `source_path` actually belongs to it. When the source's audio had no embedded MusicBrainz tags (the common case for untagged/fresh content), the tag-conflict check silently no-opped (`source_album_mbids` empty short-circuits the comparison), and the function returned `{"ok": True}`. **Proven live**: constructed a real trusted-but-unrelated staging folder with untagged fake audio, called `reimport_source_atomic()` with an arbitrary real `existing_album_id`, and confirmed a real `beet import` subprocess was actually launched against it (`beet import ... --search-id <caller-supplied-mbid> <unrelated-folder>`) -- a caller with nothing but ordinary authenticated API access could have overwritten/re-tagged any existing album with any unrelated content. **Fixed**: when there is no embedded-tag evidence to cross-check, `verify_deterministic_identity()` now additionally requires that the existing album's own current library item paths (queried from the same DB) are actually located under the source's canonical path -- real, deterministic, content-independent proof that the source folder *is* that album's own library location, not merely that the album ID is real. An unrelated source now correctly returns `review_required`, and no Beets import is launched. The legitimate library-repair-in-place case (source path already under the album's own folder) is unaffected and still requires no embedded tags. A staged-missing-track replacement into an existing album (source is a staging folder, not the album's own library path) now also correctly requires review unless the source carries matching embedded tags -- a real, deliberate tightening of a narrower legitimate use case, in favor of closing the exploit; not silently worked around.
2. **CRITICAL, functional: the atomic endpoint's `beet import` command was syntactically invalid and could never succeed against a real Beets binary.** Two independent bugs in the same command: (a) `--quiet-fallback` (which pairs with a following `asis` VALUE) was omitted -- the bare trailing `"asis"` token was parsed by Beets as an extra positional `PATH` argument, which does not exist, aborting the entire import before it reached the real target (`error: no such file or directory: asis`, confirmed against a real installed `beet` binary); (b) `-D <duplicate_action>` is not a real Beets CLI flag at all -- Beets only supports `import.duplicate_action` via config file (confirmed against real `beet import --help`, which lists no `-D`/`--duplicate-action` option) -- its presence caused `beet import: error: no such option: -D` regardless of the `--quiet-fallback` fix. **In every real invocation** (`duplicate_action` always defaults to `"remove"`, so `-D` was always present), the command could never run to completion; the exit code was 1 or 2 depending on which parse failure was hit first, both silently absorbed or misread as ordinary "no MusicBrainz match" failures by anyone testing only against mocked subprocess calls or without a real MB release to match against (exactly what happened in the prior round's own "live" runtime test, which got a real `import_failed` result but for entirely the wrong diagnosed reason). **Fixed**: `--quiet-fallback` now directly precedes its `asis` value; `-D`/`duplicate_action` removed from the CLI args entirely, with `duplicate_action` applied only via `config_override`'s `import.duplicate_action` YAML key (the function's own synthesized default config now includes it too, so the parameter still has effect when a caller relies on that default). Re-verified against a real installed `beet` binary: the corrected command now parses and runs to completion (exit 0).
- Tests: `test_unrelated_source_with_existing_album_id_is_never_authorized` (real filesystem + real sqlite DB, proves the exploit is closed and no `beet import` subprocess call is ever made for it), `test_existing_album_id_alone_with_no_corroborating_evidence_requires_review` / `test_existing_album_id_with_source_matching_its_own_library_path_succeeds` (unit-level positive/negative coverage of the new path-binding check), structural CLI-shape assertions added to the existing copy/move test (`--quiet-fallback` immediately precedes `asis`, no `-D` present, `duplicate_action:` present in the default config), and `test_beet_import_command_is_syntactically_valid_against_real_beets` (skipped when no real `beet` binary is on `PATH`, e.g. the web manager's own CI job; exercised whenever one is available, which is how bug 2 was actually found and how its fix was verified).
- Verification: full suite 1922-1923 tests, 3/3 consecutive clean runs on Linux; focused suite 5/5 consecutive clean runs on Windows including the PR #72 first-run auth subset (unaffected, confirming no regression from main's unrelated advance).
- Status: both findings fixed and independently re-verified against real (non-mocked) code paths, including a real `beet` binary. This is what final review is for: neither defect was visible from green CI or from tests that never left the mock boundary.

## ARCH-013 Endpoint Security Inventory Judgment Fields Incomplete For Newer Route Modules

- Affected area: `security/endpoint_inventory.json`, `scripts/generate_endpoint_inventory.py`, `tests/test_security_hardening.py`.
- Evidence: The remaining 36 `NEEDS_REVIEW` routes reported by the v0.1.10 Tier 1 setup/auth review were regenerated from source, individually inspected, and annotated. The inventory now covers all 240 current Flask routes and records an explicit `security_classification` for each route (`PUBLIC`, `SETUP_ONLY`, `AUTHENTICATED`, `ADMIN_ONLY`, or `INTERNAL`). Current counts are: 15 `PUBLIC`, 6 `SETUP_ONLY`, 219 `ADMIN_ONLY`, 0 `AUTHENTICATED`, 0 `INTERNAL`, and 0 `NEEDS_REVIEW`. In this app's current single-administrator model, protected operator routes are intentionally classified `ADMIN_ONLY` rather than inventing a new RBAC layer.
- Enforcement: `scripts/generate_endpoint_inventory.py` now carries `security_classification` forward as a human-reviewed field, so brand-new routes are generated with `NEEDS_REVIEW` until explicitly classified. `.github/workflows/security.yml` already runs `python3 scripts/generate_endpoint_inventory.py --check`, and `tests/test_security_hardening.py::SecurityHardeningTests::test_endpoint_inventory_has_no_unreviewed_routes` asserts the committed inventory has no `NEEDS_REVIEW` fields and every route has one of the allowed classifications.
- Additional fix found during route review: `backend.transaction_engine.TransactionStore._path()` now accepts only the generated transaction ID format (`txn_<epoch>_<12 hex chars>`), not merely any `txn_`-prefixed string without slashes/NULs. This preserves path containment and prevents attacker-shaped IDs from reaching transaction export headers if a malformed transaction file ever appears.
- Status: Closed.
## ARCH-014 Custom-Order Test Subset Can Still Contaminate `test_auth_and_setup.py`

- Affected area: `tests/test_routes_setup.py`, `tests/test_auth_and_setup.py`.
- Reproducer before fix: `python -m unittest tests.test_routes_setup tests.test_auth_and_setup` failed deterministically with 10 `test_auth_and_setup.py` failures; Claude's five-module non-alphabetical sequence (`tests.test_routes_setup tests.test_external_beets_architecture tests.test_auth_and_setup tests.test_v0110_upgrade_and_matched_stack tests.test_beets_fresh_install_plugins`) reproduced the same contamination.
- Root cause: `tests/test_routes_setup.py` imported `routes_setup` against a stub `sys.modules["app"]`, then relied on module teardown to pop `app`/route modules. `unittest` imports later test modules before running earlier suites, so `test_auth_and_setup.py` could already hold references to the real `app` and `routes_setup` modules when the stub suite ran. After `test_routes_setup` popped `sys.modules["app"]`, route handlers that perform runtime `from app import ...` loaded a second, unpatched app module during `test_auth_and_setup.py`, bypassing that test's temporary credential/setup paths and causing order-dependent 401/409/rate-limit/cookie failures.
- Fix: `tests/test_routes_setup.py` now snapshots the relevant module entries before each stub import and restores the exact previous modules with `TestCase.addCleanup()` after each test. `tearDownModule()` is now only a best-effort interrupted-test guard for a still-installed stub, not the normal isolation mechanism.
- Verification after fix: `python -m unittest tests.test_routes_setup tests.test_auth_and_setup` passed (`Ran 59 tests ... OK`), and the prior five-module reproducer passed (`Ran 262 tests ... OK (skipped=1)`).
- Status: Closed.
