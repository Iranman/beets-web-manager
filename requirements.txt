# Core web app
Flask==3.1.0
Werkzeug==3.1.3
waitress==3.0.2

# Beets + plugins referenced in config.yaml.example. fetchart, embedart, convert,
# scrub, replaygain, lastgenre, chroma, lyrics, mbsync, musicbrainz, ftintitle,
# fromfilename, duplicates, missing, smartplaylist, mbsubmit, unimported are
# bundled with beets core. The packages below are the separate PyPI extras
# those plugins need at runtime (verified present on PyPI as of 2026-07-16).
beets==2.2.0
typing_extensions==4.16.0
pyacoustid==1.3.0
python3-discogs-client==2.8
deezer-python==2.1.0
pylistenbrainz==0.5.1
Pillow==10.4.0
beautifulsoup4==4.12.3
requests==2.32.3

# NOTE: config.yaml.example's plugin list also includes "plexsync", a
# third-party beets plugin not published under a confirmed PyPI name. If you
# use plexsync, install it manually (commonly via `pip install git+<repo-url>`
# for the specific fork you use) and add it to this file with a pinned commit.

# yt-dlp download pipeline (installed here, not at runtime - _pip_install() in
# app.py is intentionally a no-op; see Dockerfile comment)
yt-dlp[default,curl-cffi]==2024.11.4
bgutil-ytdlp-pot-provider==1.3.1

# Demo-mode library seeding (scripts/seed_demo_library.py writes ID3 tags
# directly, independent of beets, to synthesize a self-contained demo library)
mutagen==1.48.1
