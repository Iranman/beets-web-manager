# Core web app
Flask==3.1.0
Werkzeug==3.1.3
waitress==3.0.2

# Additional metadata & integration libraries
pylast==7.1.0
typing_extensions==4.16.0
deezer-python==2.1.0
pylistenbrainz==0.5.1
Pillow==10.4.0
beautifulsoup4==4.12.3
requests==2.32.5

# yt-dlp download pipeline
yt-dlp[default,curl-cffi]==2024.11.4
bgutil-ytdlp-pot-provider==1.3.1

# Tag reading & demo-mode metadata helper
mutagen==1.48.1

# SEC-002 Wave 19: authoritative MusicBrainz tag read/write for
# backend/transaction_engine.py's album_mb_track_repair_v1 mutation family.
# This is the standalone package Beets itself depends on for tag I/O
# (beets.mediafile is a deprecated shim over the same code as of Beets
# 2.x) -- installing it directly here, rather than pulling in the whole
# `beets` package, keeps the Web Manager image dependency-light while
# still letting the shared unit-test suite (which runs
# backend/transaction_engine.py's tests without the full `beets` engine
# installed) exercise the real reader/writer instead of only reaching a
# silent ImportError fallback.
mediafile==0.17.0