# Core web app
Flask==3.1.0
Werkzeug==3.1.3
waitress==3.0.2

# Beets plus the extras required by config.yaml.example's default plugin set.
# Beets 2.12.0 is the pinned metadata-source plugin model used by both the
# standalone image and Arr deployment.
beets[chroma,discogs,embedart,fetchart,lastgenre,lastimport,lyrics,scrub]==2.12.0
pylast==7.1.0
typing_extensions==4.16.0
deezer-python==2.1.0
pylistenbrainz==0.5.1
Pillow==10.4.0
beautifulsoup4==4.12.3
requests==2.32.5

# yt-dlp download pipeline (installed here, not at runtime - _pip_install() in
# app.py is intentionally a no-op; see Dockerfile comment)
yt-dlp[default,curl-cffi]==2024.11.4
bgutil-ytdlp-pot-provider==1.3.1

# Demo-mode library seeding (scripts/seed_demo_library.py writes ID3 tags
# directly, independent of beets, to synthesize a self-contained demo library)
mutagen==1.48.1